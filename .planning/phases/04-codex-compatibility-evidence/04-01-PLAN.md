---
phase: 04-codex-compatibility-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/codex-compatibility.md
  - privguard/codex.py
  - tests/test_codex_compatibility.py
autonomous: true
requirements:
  - CDX-01
  - CDX-02
must_haves:
  truths:
    - "Developer can read a Codex compatibility assessment that states which prompt and tool interception options were verified."
    - "Developer can see every relevant Codex surface labeled conservatively with evidence, privacy action, and remaining gaps."
    - "No Codex surface is labeled rewrite-capable or automatic masking unless the matrix records verified outbound payload replacement proof."
  artifacts:
    - path: "docs/codex-compatibility.md"
      provides: "Human-readable Codex assessment and compatibility matrix"
      contains: "## Compatibility Matrix"
    - path: "privguard/codex.py"
      provides: "Machine-readable Codex compatibility rows"
      exports: ["CodexCompatibilityRow", "CODEX_COMPATIBILITY", "get_codex_compatibility"]
    - path: "tests/test_codex_compatibility.py"
      provides: "Automated CDX-01/CDX-02 matrix checks"
      contains: "test_codex_matrix_rows_are_complete_and_conservative"
  key_links:
    - from: "docs/codex-compatibility.md"
      to: "privguard/codex.py"
      via: "same surface names and labels"
      pattern: "UserPromptSubmit prompt"
    - from: "tests/test_codex_compatibility.py"
      to: "privguard.codex.CODEX_COMPATIBILITY"
      via: "direct import"
      pattern: "from privguard.codex import"
    - from: "privguard/codex.py"
      to: "privguard.policy.SurfaceCapability"
      via: "row surface_capability values"
      pattern: "SurfaceCapability"
---

<objective>
Create the Phase 04 Codex compatibility source of truth.

Purpose: CDX-01 and CDX-02 require honest Codex interception documentation and a compatibility matrix with conservative labels. This plan creates both a human-readable assessment and a machine-readable matrix so future claims can be tested instead of living only in prose.
Output: `docs/codex-compatibility.md`, `privguard/codex.py`, and matrix coverage tests.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-codex-compatibility-evidence/04-CONTEXT.md
@.planning/phases/04-codex-compatibility-evidence/04-RESEARCH.md
@.planning/phases/04-codex-compatibility-evidence/04-PATTERNS.md
@privguard/policy.py
@privguard/masking.py
@tests/test_policy.py

<interfaces>
From `privguard/policy.py`:
```python
class SurfaceCapability:
    REWRITE_CAPABLE = "rewrite-capable"
    BLOCK_ONLY = "block-only"
    OBSERVE_ONLY = "observe-only"
    UNSUPPORTED = "unsupported"
    UNKNOWN = "unknown"
    EXTERNAL = "external"
```

Codex row contract to create in `privguard/codex.py`:
```python
@dataclass(frozen=True)
class CodexCompatibilityRow:
    surface: str
    support_label: str
    surface_capability: str
    privacy_action: str
    evidence: tuple[str, ...]
    tested_version_or_docs_date: str
    automatic_masking: bool
    gaps: tuple[str, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create machine-readable Codex compatibility matrix</name>
  <files>privguard/codex.py, tests/test_codex_compatibility.py</files>
  <read_first>
    - privguard/codex.py if it exists; otherwise confirm it is absent with `Test-Path privguard/codex.py`
    - privguard/policy.py
    - tests/test_policy.py
    - .planning/phases/04-codex-compatibility-evidence/04-CONTEXT.md
    - .planning/phases/04-codex-compatibility-evidence/04-RESEARCH.md
    - .planning/phases/04-codex-compatibility-evidence/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: every `CODEX_COMPATIBILITY` row has non-empty `surface`, `support_label`, `surface_capability`, `privacy_action`, `evidence`, `tested_version_or_docs_date`, and `gaps`.
    - Test 2: every `surface_capability` is a member of `SurfaceCapability.ALL`.
    - Test 3: rows with `automatic_masking is False` are never `SurfaceCapability.REWRITE_CAPABLE`.
    - Test 4: the matrix includes rows for `UserPromptSubmit prompt`, `PreToolUse Bash`, `PreToolUse apply_patch/Edit/Write`, `PreToolUse MCP tool call`, `PermissionRequest`, `PostToolUse`, `WebSearch and non-shell/non-MCP tools`, and `Automatic Codex masking rewrite`.
  </behavior>
  <action>Create `privguard/codex.py` with `CodexCompatibilityRow`, `CODEX_SUPPORT_LABELS`, `CODEX_COMPATIBILITY`, and `get_codex_compatibility()`. Use `SurfaceCapability` from `privguard.policy`; do not create new internal capability strings. Populate conservative rows exactly: `UserPromptSubmit prompt` support_label `experimental block-only`, capability `block-only`, privacy_action `block_sensitive_prompt_when_hook_observed`, evidence including `OpenAI Codex hooks docs 2026-05-03` and `local codex-cli 0.128.0 version observed`, automatic_masking `False`, gaps `no verified outbound payload rewrite` and `no no-provider synthetic interception proof recorded`; `PreToolUse Bash`, `PreToolUse apply_patch/Edit/Write`, and `PreToolUse MCP tool call` support_label `experimental block-only`, capability `block-only`, automatic_masking `False`, with gaps for incomplete shell coverage, payload-shape uncertainty, or variable MCP schemas; `PermissionRequest` support_label `experimental observe-only`, capability `observe-only`; `PostToolUse` support_label `observe-only`, capability `observe-only`; `WebSearch and non-shell/non-MCP tools` support_label `unsupported`, capability `unsupported`; `Automatic Codex masking rewrite` support_label `unsupported`, capability `unsupported`, privacy_action `do_not_claim_masking`, gap `requires proof outbound payload equals verified masked payload before provider submission`. Add `tests/test_codex_compatibility.py` with the behavior tests above and a policy test showing sensitive synthetic hits against all non-masking Codex rows resolve to block for `block-only`, `observe-only`, and `unsupported` capabilities via `decide_policy()`.</action>
  <acceptance_criteria>
    - `privguard/codex.py` contains `@dataclass(frozen=True)` and `class CodexCompatibilityRow`.
    - `privguard/codex.py` contains all exact surface strings listed in the action.
    - `privguard/codex.py` contains no `automatic_masking=True` row.
    - `tests/test_codex_compatibility.py` contains `test_codex_matrix_rows_are_complete_and_conservative`.
    - `tests/test_codex_compatibility.py` imports `CODEX_COMPATIBILITY` from `privguard.codex`.
    - `python -m pytest tests/test_codex_compatibility.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_codex_compatibility.py -q</automated>
  </verify>
  <done>The Codex matrix is importable, complete for Phase 04 surfaces, maps to existing policy capabilities, and rejects any rewrite-capable/automatic masking row without proof.</done>
</task>

<task type="auto">
  <name>Task 2: Write human-readable Codex compatibility assessment</name>
  <files>docs/codex-compatibility.md, tests/test_codex_compatibility.py</files>
  <read_first>
    - docs/codex-compatibility.md if it exists; otherwise confirm it is absent with `Test-Path docs/codex-compatibility.md`
    - privguard/codex.py
    - tests/test_codex_compatibility.py
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/04-codex-compatibility-evidence/04-CONTEXT.md
    - .planning/phases/04-codex-compatibility-evidence/04-RESEARCH.md
  </read_first>
  <action>Create `docs/codex-compatibility.md` with headings `# Codex Compatibility`, `## Assessment Summary`, `## Evidence Standard`, `## Compatibility Matrix`, `## Unsupported Automatic Masking`, and `## Remaining Gaps`. State the current assessment date as `2026-05-03`, installed local Codex as `codex-cli 0.128.0`, official docs evidence as `OpenAI Codex hooks docs checked 2026-05-03`, and npm evidence as `@openai/codex 0.128.0 modified 2026-05-01`. The compatibility table must include columns `Surface`, `Support label`, `SurfaceCapability`, `Evidence`, `Privacy action`, and `Remaining gaps`, and must mirror every `CODEX_COMPATIBILITY` surface. Use the conservative labels from D-03/D-05/D-06: `experimental block-only`, `observe-only`, and `unsupported`; do not use a broad `Codex supported` claim and do not present `UserPromptSubmit prompt` as proven block-only unless execution adds a no-provider synthetic interception proof. Add or extend `tests/test_codex_compatibility.py` so it reads `docs/codex-compatibility.md` and asserts each matrix surface appears in the document, `automatic Codex masking is unsupported until verified outbound payload replacement is proven` appears, and no protected file contents are read.</action>
  <acceptance_criteria>
    - `docs/codex-compatibility.md` contains `# Codex Compatibility`.
    - `docs/codex-compatibility.md` contains `## Compatibility Matrix`.
    - `docs/codex-compatibility.md` contains `codex-cli 0.128.0`.
    - `docs/codex-compatibility.md` contains `automatic Codex masking is unsupported until verified outbound payload replacement is proven`.
    - `docs/codex-compatibility.md` contains no phrase `Codex masks prompts automatically`.
    - `python -m pytest tests/test_codex_compatibility.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_codex_compatibility.py -q</automated>
  </verify>
  <done>The developer-facing assessment documents current Codex interception evidence, labels each surface with evidence and gaps, and is checked against the machine-readable matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer documentation -> user behavior | Overstated support language could cause users to paste real CPF/CNPJ/secrets into Codex. |
| Codex surface label -> policy interpretation | Matrix rows must map to existing fail-closed `SurfaceCapability` values. |
| Local evidence -> docs/tests | Synthetic probes and docs must not read `.env` or `data_sensivel/**`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | `docs/codex-compatibility.md` | mitigate | State automatic Codex masking is unsupported and label unproven surfaces conservatively per D-03/D-05/D-07. |
| T-04-01-02 | Tampering | `privguard/codex.py` | mitigate | Tests require every row to map to `SurfaceCapability.ALL` and reject rewrite-capable rows without `automatic_masking=True` proof fields. |
| T-04-01-03 | Information Disclosure | `tests/test_codex_compatibility.py` | mitigate | Use synthetic strings and matrix/doc reads only; do not open `.env` or `data_sensivel/**`. |
| T-04-01-04 | Repudiation | compatibility evidence | mitigate | Each row records `evidence`, `tested_version_or_docs_date`, and `gaps` so support labels are auditable. |
</threat_model>

<verification>
Run `python -m pytest tests/test_codex_compatibility.py -q`.
</verification>

<success_criteria>
- CDX-01 is satisfied by `docs/codex-compatibility.md` documenting Codex interception options and block/rewrite status.
- CDX-02 is satisfied by `privguard/codex.py` and the Markdown matrix using explicit support labels with evidence.
- No automatic Codex masking claim appears in this plan's outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md`
</output>
