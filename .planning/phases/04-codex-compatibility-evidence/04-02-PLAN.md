---
phase: 04-codex-compatibility-evidence
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/test_codex_claim_gate.py
autonomous: true
requirements:
  - CDX-03
must_haves:
  truths:
    - "Developer cannot encounter unsupported automatic Codex masking claims in safe repository text."
    - "Any future Codex automatic masking claim must be backed by a matrix row with verified outbound replacement proof."
    - "Claim-gate diagnostics do not expose synthetic raw values, protected paths, or prompt snippets."
  artifacts:
    - path: "tests/test_codex_claim_gate.py"
      provides: "Repository claim-prevention gate for CDX-03"
      contains: "test_codex_automatic_masking_claims_require_verified_matrix_proof"
  key_links:
    - from: "tests/test_codex_claim_gate.py"
      to: "privguard.codex.CODEX_COMPATIBILITY"
      via: "direct import"
      pattern: "automatic_masking"
    - from: "tests/test_codex_claim_gate.py"
      to: "docs/codex-compatibility.md"
      via: "safe text scan"
      pattern: "codex-compatibility.md"
---

<objective>
Add the CDX-03 regression gate that prevents unsupported Codex automatic masking claims.

Purpose: The core risk in Phase 04 is false confidence. This plan makes that risk testable by scanning safe repository text for unsupported Codex masking/rewrite claims and requiring any future positive claim to be backed by matrix proof.
Output: `tests/test_codex_claim_gate.py`.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-codex-compatibility-evidence/04-CONTEXT.md
@.planning/phases/04-codex-compatibility-evidence/04-RESEARCH.md
@.planning/phases/04-codex-compatibility-evidence/04-PATTERNS.md
@privguard/codex.py
@docs/codex-compatibility.md
@tests/test_claude_phase_gate.py

<interfaces>
From `privguard/codex.py` created by Plan 04-01:
```python
CODEX_COMPATIBILITY: tuple[CodexCompatibilityRow, ...]
```

The claim gate should treat a Codex masking claim as allowed only when at least one row has:
```python
row.automatic_masking is True
row.surface_capability == SurfaceCapability.REWRITE_CAPABLE
"verified outbound payload replacement" in " ".join(row.evidence + row.gaps)
```
Current Phase 04 rows are expected to have `automatic_masking is False`, so positive automatic masking language must fail unless it is explicitly allowlisted safe wording that says the feature is unsupported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository Codex masking claim gate</name>
  <files>tests/test_codex_claim_gate.py</files>
  <read_first>
    - tests/test_codex_claim_gate.py if it exists; otherwise confirm it is absent with `Test-Path tests/test_codex_claim_gate.py`
    - tests/test_claude_phase_gate.py
    - privguard/codex.py
    - docs/codex-compatibility.md
    - pyproject.toml
    - .planning/phases/04-codex-compatibility-evidence/04-CONTEXT.md
    - .planning/phases/04-codex-compatibility-evidence/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: scanning safe repository text finds no unsupported phrases such as `Codex masks prompts automatically`, `Codex automatic masking`, `automatic Codex masking`, `Codex rewrite-capable`, or `Codex rewrites prompts before submission`.
    - Test 2: the safe unsupported sentence `automatic Codex masking is unsupported until verified outbound payload replacement is proven` is allowed.
    - Test 3: if a synthetic text sample contains `Codex masks prompts automatically` and no matrix row has verified automatic masking proof, the helper returns a violation.
    - Test 4: claim-gate scan excludes `.env`, `data_sensivel/**`, `.git/**`, `__pycache__/**`, `.pytest_cache/**`, `pytest-cache-files-*`, and `.planning/**`.
  </behavior>
  <action>Create `tests/test_codex_claim_gate.py` with `FORBIDDEN_CLAIM_PATTERNS`, `ALLOWED_NEGATED_CLAIMS`, `_safe_text_files(root: pathlib.Path)`, `_has_verified_codex_masking_proof()`, `_find_unsupported_claims(text: str)`, and `test_codex_automatic_masking_claims_require_verified_matrix_proof`. Scan only safe text/code targets: `docs/**/*.md`, `privguard/**/*.py`, `tests/**/*.py`, `pyproject.toml`, and `AGENTS.md`; do not recursively scan all repo files. Exclude any path whose parts include `.git`, `.planning`, `data_sensivel`, `__pycache__`, `.pytest_cache`, or whose name starts with `pytest-cache-files-`; also exclude `.env` and `.env.*` exactly. Implement the gate so a positive claim passes only when `_has_verified_codex_masking_proof()` returns `True`; because Plan 04-01 has no such row, current positive claims must fail. Allow only negated/unsupported wording matching the exact lowercase sentence `automatic codex masking is unsupported until verified outbound payload replacement is proven`.</action>
  <acceptance_criteria>
    - `tests/test_codex_claim_gate.py` contains `FORBIDDEN_CLAIM_PATTERNS`.
    - `tests/test_codex_claim_gate.py` contains `ALLOWED_NEGATED_CLAIMS`.
    - `tests/test_codex_claim_gate.py` contains `test_codex_automatic_masking_claims_require_verified_matrix_proof`.
    - `tests/test_codex_claim_gate.py` contains exclusions for `.env`, `data_sensivel`, `.planning`, `.git`, and `pytest-cache-files-`.
    - `python -m pytest tests/test_codex_claim_gate.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_codex_claim_gate.py -q</automated>
  </verify>
  <done>Unsupported automatic Codex masking and rewrite-capable claims fail the test suite unless future matrix proof exists.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 04 evidence and hygiene gates together</name>
  <files>tests/test_codex_claim_gate.py</files>
  <read_first>
    - tests/test_codex_claim_gate.py
    - tests/test_codex_compatibility.py
    - privguard/codex.py
    - docs/codex-compatibility.md
    - tests/test_claude_phase_gate.py
  </read_first>
  <action>Run the focused Phase 04 checks together and fix only issues in `tests/test_codex_claim_gate.py` if the claim gate has false positives against the approved unsupported wording. Do not weaken the forbidden patterns to make unsupported automatic masking claims pass. Confirm the test output does not contain raw synthetic fixture values from `tests/test_claude_phase_gate.py`: `123.456.789-09`, `sk-test-abcdefghijklmnopqrstuvwxyz`, `data_sensivel/synthetic.csv`, or `analise o cadastro`.</action>
  <acceptance_criteria>
    - `python -m pytest tests/test_codex_compatibility.py tests/test_codex_claim_gate.py -q` passes.
    - No test failure output includes `123.456.789-09`.
    - No test failure output includes `sk-test-abcdefghijklmnopqrstuvwxyz`.
    - No test failure output includes `data_sensivel/synthetic.csv`.
    - No task code reads `.env` or any file under `data_sensivel/**`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_codex_compatibility.py tests/test_codex_claim_gate.py -q</automated>
  </verify>
  <done>CDX-03 claim prevention works with the Phase 04 matrix and document without exposing raw synthetic values or protected paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository text -> developer trust | Markdown, CLI-visible strings, and package code can accidentally imply unsupported Codex protection. |
| Claim gate -> protected filesystem | The test scans local files and must not probe `.env` or `data_sensivel/**`. |
| Test diagnostics -> terminal output | Assertion failures must not print raw synthetic secrets, prompt snippets, or protected path examples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | `tests/test_codex_claim_gate.py` | mitigate | Fail on unsupported automatic Codex masking/rewrite claims unless `CODEX_COMPATIBILITY` has verified masking proof. |
| T-04-02-02 | Information Disclosure | safe file scanner | mitigate | Scan only explicit safe globs and exclude `.env`, `.env.*`, `data_sensivel/**`, `.planning/**`, `.git/**`, caches, and generated pytest cache directories. |
| T-04-02-03 | Spoofing | Codex support language | mitigate | Require matrix proof with `automatic_masking=True` and `SurfaceCapability.REWRITE_CAPABLE` before accepting positive masking language. |
| T-04-02-04 | Information Disclosure | pytest failure output | mitigate | Use sanitized assertion messages with paths/line numbers only; do not include matched raw line content in failure messages. |
</threat_model>

<verification>
Run `python -m pytest tests/test_codex_compatibility.py tests/test_codex_claim_gate.py -q`.
</verification>

<success_criteria>
- CDX-03 is satisfied by a focused pytest gate that blocks unsupported automatic Codex masking claims.
- The gate is grounded in `privguard.codex.CODEX_COMPATIBILITY` rather than hardcoded trust.
- The gate never reads protected files or emits raw sensitive fixture values.
</success_criteria>

<output>
After completion, create `.planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md`
</output>
