---
phase: 999.4-cpf-leniency-mode
verified: 2026-05-21T03:00:00Z
status: passed
score: 12/12
overrides_applied: 0
---

# Phase 999.4: CPF Leniency Mode — Verification Report

**Phase Goal:** Implement optional lenient CPF detection mode — any DDD.DDD.DDD-DD pattern masked when PII_GUARD_LENIENT=true or lenient=True, regardless of checksum. Strict mode (default) unchanged. CLI --lenient flag exposed on scan/mask/policy-check.
**Verified:** 2026-05-21T03:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | detect('456.789.123-45', lenient=True) returns a BR_CPF hit | VERIFIED | Programmatic check: hit found, score=0.75, reason_code='lenient_pattern' |
| 2 | detect('456.789.123-45') (no param, no env var) returns no BR_CPF hit — strict is the default | VERIFIED | Programmatic check: empty result; test_strict_mode_is_default_no_env_var passes |
| 3 | Lenient BR_CPF hits carry score=0.75 and reason_code='lenient_pattern' | VERIFIED | detection.py lines 227-230 appends Hit with _LENIENT_SCORES['BR_CPF']=0.75 and 'lenient_pattern' |
| 4 | Bare 11-digit CPF strings (no dots/dash) are NOT emitted as lenient hits | VERIFIED | Format guard ("." in value and "-" in value) at detection.py lines 225-230; test_lenient_bare_11digit_cpf_stays_strict passes |
| 5 | mask_text('456.789.123-45', lenient=True) returns verified=True with '<BR_CPF>' in output | VERIFIED | Programmatic check: verified=True, '<BR_CPF>' in result.text; test_lenient_cpf_masks_correctly passes |
| 6 | PII_GUARD_LENIENT=true env var activates leniency when lenient=None (the default) | VERIFIED | _lenient_default() at detection.py line 199 reads env var; CLI env-var passthrough confirmed |
| 7 | privguard scan --lenient '456.789.123-45' reports BR_CPF | VERIFIED | CLI returns 'detections=1 counts={\'BR_CPF\': 1}', exit 0 |
| 8 | privguard mask --lenient '456.789.123-45' outputs '<BR_CPF>' and exits 0 | VERIFIED | CLI outputs '<BR_CPF>', exit 0 |
| 9 | privguard policy-check --lenient '456.789.123-45' sees a CPF hit in its decision | VERIFIED | CLI returns hit_count=1 in decision payload, exit 2 (block — correct for fail-closed surface) |
| 10 | Absent --lenient flag passes lenient=None (not False) — env var PII_GUARD_LENIENT is not suppressed | VERIFIED | `lenient = True if args.lenient else None` at cli.py lines 48, 64, 85; env-var passthrough confirmed via subprocess test |
| 11 | All 8 new lenient tests pass (7 detection + 1 masking minimum) | VERIFIED | 17 lenient/invalid_checksum tests pass; 171 total tests pass (up from 164 before Plan 01) |
| 12 | Existing test_invalid_checksum_lookalikes_are_below_default_threshold still passes unchanged | VERIFIED | test passes; strict default confirmed working |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `privguard/detection.py` | _lenient_default(), _LENIENT_KINDS, _LENIENT_SCORES, updated detect() and analyze_text() | VERIFIED | Lines 198-254: all four items present and substantive |
| `privguard/masking.py` | updated mask_text() with lenient parameter threaded to detect() | VERIFIED | Lines 81-108: lenient param at line 85, threaded at line 88 |
| `privguard/cli.py` | --lenient flag on scan, mask, policy-check subparsers | VERIFIED | Lines 131-165: three --lenient add_argument registrations; lenient=True if args.lenient else None in all three command functions |
| `tests/test_detection.py` | 7+ new tests covering lenient detection behavior (section 999.4) | VERIFIED | Two sections: Phase 999.4 (8 tests at lines 259-403) and CPF Leniency Mode (7 tests at lines 349-403) |
| `tests/test_masking.py` | 1+ new test: test_lenient_cpf_masks_correctly | VERIFIED | 3 new masking tests at lines 155-182; test_lenient_cpf_masks_correctly at line 157 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| privguard/detection.py detect() | privguard/masking.py mask_text() | lenient parameter passed through when hits is None | WIRED | masking.py line 88: `detect(text, min_score=min_score, lenient=lenient)` |
| privguard/detection.py _lenient_default() | os.environ | PII_GUARD_LENIENT env var | WIRED | detection.py line 199: `os.environ.get("PII_GUARD_LENIENT", "").lower() in ("1", "true", "yes")` |
| cli.py cmd_scan | detection.analyze_text() | lenient=True if args.lenient else None | WIRED | cli.py lines 48-49: assignment + `analyze_text(_read_text(args), lenient=lenient)` |
| cli.py cmd_mask | masking.mask_text() | lenient=True if args.lenient else None | WIRED | cli.py lines 64-65: assignment + `mask_text(_read_text(args), lenient=lenient)` |
| cli.py cmd_policy_check | detection.detect() | lenient=True if args.lenient else None | WIRED | cli.py lines 85-86: assignment + `detect(text, lenient=lenient)` |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies a detection/masking library, not a UI or dashboard. Data flow is verified via test suite execution (171 tests pass) and behavioral spot-checks below.

### Behavioral Spot-Checks

| Behavior | Result | Status |
|----------|--------|--------|
| `privguard scan --lenient "456.789.123-45"` outputs BR_CPF | detections=1 counts={'BR_CPF': 1}, exit 0 | PASS |
| `privguard mask --lenient "456.789.123-45"` outputs `<BR_CPF>` and exits 0 | `<BR_CPF>`, exit 0 | PASS |
| `privguard scan "456.789.123-45"` (no flag, no env) outputs no detections | detections=0 counts={}, exit 0 | PASS |
| `PII_GUARD_LENIENT=true privguard scan "456.789.123-45"` outputs BR_CPF | detections=1 counts={'BR_CPF': 1} | PASS |
| `python -m pytest tests/ -x -q` (all 171 tests) | 171 passed | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DET-01 | 999.4-01, 999.4-02 | Guard can detect synthetic Brazilian CPF values with checksum validation | SATISFIED | Lenient mode extends DET-01 to cover formatted CPFs with invalid checksums as an opt-in feature; strict checksum validation remains the default |

### Anti-Patterns Found

No blockers or stubs found. Code inspection:

- `_lenient_default()` reads env var and returns a live boolean (not hardcoded)
- `detect()` lenient branch produces real hits with actual score/reason_code values
- `mask_text()` threads lenient to detect() with guard that verify_mask() stays strict (defense-in-depth)
- CLI command functions all use the correct `True if args.lenient else None` pattern (None, not False, preserving env-var semantics)
- Test bodies use `monkeypatch.setenv`/`monkeypatch.delenv` for env isolation (no global mutation leak)
- Module-level `_INVALID_CPF_FORMATTED` and `_VALID_CPF_FORMATTED` constants are synthetic values (not real PII — passes TEST-01)

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| tests/test_detection.py lines 265, 276, 286, etc. | `os.environ.pop("PII_GUARD_LENIENT", None)` in test bodies (not monkeypatched) | Info | Acceptable — direct pop is safe for setup isolation in these non-monkeypatched tests; env is restored by the pop itself |

### Human Verification Required

None. All must-haves are verifiable programmatically.

### Gaps Summary

No gaps. All 12 must-haves across Plan 01 and Plan 02 are verified:

- Detection engine changes (detection.py, masking.py) are substantive and correctly wired
- CLI wiring (cli.py) correctly implements the `True if args.lenient else None` pattern across all three subcommands
- Test coverage is complete (171 total passing tests, 17 lenient-specific tests)
- Behavioral spot-checks confirm end-to-end functionality

Phase goal achieved: Optional lenient CPF detection mode is implemented, strict mode unchanged, CLI --lenient flag exposed on scan/mask/policy-check.

---

_Verified: 2026-05-21T03:00:00Z_
_Verifier: Claude (gsd-verifier)_
