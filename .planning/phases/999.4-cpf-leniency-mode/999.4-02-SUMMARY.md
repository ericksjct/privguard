---
phase: 999.4-cpf-leniency-mode
plan: "02"
subsystem: cli
tags: [python, cli, argparse, detection, masking, cpf, leniency, tdd]

requires:
  - phase: 999.4-cpf-leniency-mode
    plan: "01"
    provides: "detect(), analyze_text(), mask_text() with lenient: bool | None = None parameter"

provides:
  - "--lenient flag on scan, mask, and policy-check subparsers"
  - "cmd_scan threads lenient=True if args.lenient else None to analyze_text()"
  - "cmd_mask threads lenient=True if args.lenient else None to mask_text()"
  - "cmd_policy_check threads lenient=True if args.lenient else None to detect()"
  - "# --- CPF Leniency Mode (999.4) --- test section with _INVALID_CPF_FORMATTED/_VALID_CPF_FORMATTED constants"
  - "test_strict_mode_is_default (lenient=False explicit override)"
  - "test_lenient_cpf_env_var (monkeypatch.setenv isolation)"
  - "test_lenient_env_var_false_when_absent (monkeypatch.delenv isolation)"

affects:
  - hooks
  - cli-users

tech-stack:
  added: []
  patterns:
    - "Absent --lenient passes None (not False) via `True if args.lenient else None` to preserve PII_GUARD_LENIENT env-var semantics"
    - "monkeypatch.setenv/delenv for env-var isolation in tests (no global os.environ mutation)"
    - "Module-level test constants _INVALID_CPF_FORMATTED and _VALID_CPF_FORMATTED for DRY test fixtures"

key-files:
  created: []
  modified:
    - privguard/cli.py
    - tests/test_detection.py

key-decisions:
  - "Absent --lenient passes lenient=None (not False) so PII_GUARD_LENIENT env var is not suppressed by CLI invocations without --lenient"
  - "Test name collision with Plan 01 tests resolved by _v2 suffix on 4 duplicated test names; 3 truly new tests have exact plan-specified names"
  - "monkeypatch used for env-var tests in the 999.4 section to ensure proper teardown and avoid cross-test pollution"

patterns-established:
  - "Bool|None passthrough in CLI: True if args.store_true_flag else None preserves env-var override semantics"

requirements-completed:
  - DET-01

duration: 5min
completed: 2026-05-22
---

# Phase 999.4 Plan 02: CPF Leniency Mode CLI and Tests Summary

**--lenient flag wired into scan/mask/policy-check CLI subcommands with absent-flag=None semantics, plus 7 new test_detection.py tests (monkeypatch-isolated env-var tests, module-level CPF constants, and explicit lenient=False coverage)**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-22T01:15:00Z
- **Completed:** 2026-05-22T01:20:42Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `--lenient` argument (store_true, default=False) to `scan`, `mask`, and `policy-check` subparsers in `cli.py`
- Threaded `lenient = True if args.lenient else None` through `cmd_scan`, `cmd_mask`, and `cmd_policy_check` — absent flag passes `None` (preserves `PII_GUARD_LENIENT` env-var semantics)
- Added `# --- CPF Leniency Mode (999.4) ---` section to `test_detection.py` with module-level constants `_INVALID_CPF_FORMATTED` and `_VALID_CPF_FORMATTED`
- Added 7 new tests: `test_strict_mode_is_default`, `test_lenient_cpf_env_var`, `test_lenient_env_var_false_when_absent`, plus 4 v2 variants using module-level constants
- All 171 tests pass (no regressions from 164 before Plan 01)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add --lenient flag to CLI and thread through command functions** - `af0ce67` (feat)
2. **Task 2: Add CPF leniency mode tests section** - `c305acd` (feat)

**Plan metadata:** (committed with SUMMARY below)

## Files Created/Modified

- `privguard/cli.py` - Added `--lenient` to three subparsers; threaded `lenient = True if args.lenient else None` in `cmd_scan`, `cmd_mask`, `cmd_policy_check`
- `tests/test_detection.py` - Added `# --- CPF Leniency Mode (999.4) ---` section with module constants and 7 new tests

## Decisions Made

- Absent `--lenient` passes `None` (not `False`) via `True if args.lenient else None` pattern — this preserves `PII_GUARD_LENIENT` env-var semantics when the CLI flag is omitted
- Tests from Plan 01 (RED phase) already covered most behaviors but with slightly different names; Plan 02 section adds the exact names from spec where they differ, using `_v2` suffix to avoid Python duplicate-name syntax errors for the 4 overlapping test names
- `monkeypatch` used for env-var tests in the 999.4 section to ensure proper teardown (pytest auto-restores env state) instead of raw `os.environ` mutation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test name collision with Plan 01 RED-phase tests**
- **Found during:** Task 2 (test additions)
- **Issue:** Plan 01's TDD RED phase already added tests named `test_lenient_cpf_detects_invalid_checksum_formatted`, `test_lenient_hit_has_lower_score_than_strict`, `test_lenient_reason_code`, `test_lenient_does_not_shadow_valid_strict_hit` — Python would silently overwrite these with duplicate definitions if added again with exact names
- **Fix:** Added `_v2` suffix to the 4 conflicting test names in the new `# --- CPF Leniency Mode (999.4) ---` section; the 3 truly new tests (`test_strict_mode_is_default`, `test_lenient_cpf_env_var`, `test_lenient_env_var_false_when_absent`) use exact plan-specified names
- **Files modified:** `tests/test_detection.py`
- **Verification:** 171 tests pass; grep confirms all 3 new exact-named tests exist
- **Committed in:** c305acd (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - naming collision)
**Impact on plan:** Minimal — 3 of 7 specified test names are exact; 4 use `_v2` suffix to avoid silent overwrites. All behavioral coverage is present.

## Issues Encountered

- The installed `privguard` package in the main project directory shadows the worktree version when running `python -m privguard.cli` from the main project directory. Verification commands must be run from within the worktree directory (`cd .claude/worktrees/agent-afa3e795f7f5b85b6`), or `pytest` must be run from the main project directory pointing to the worktree test files.

## User Setup Required

None - no external service configuration required. `--lenient` is opt-in; existing deployments are unaffected.

## Next Phase Readiness

- `privguard scan --lenient`, `privguard mask --lenient`, and `privguard policy-check --lenient` are all functional
- `PII_GUARD_LENIENT=true` env var activates leniency even without `--lenient` CLI flag (absent flag passes `None`)
- Phase 999.4 (CPF leniency mode) is now complete: engine (Plan 01) + CLI exposure (Plan 02) both done
- 171 tests pass with zero regressions

---
*Phase: 999.4-cpf-leniency-mode*
*Completed: 2026-05-22*

## Self-Check: PASSED

- FOUND: privguard/cli.py
- FOUND: tests/test_detection.py
- FOUND: .planning/phases/999.4-cpf-leniency-mode/999.4-02-SUMMARY.md
- FOUND commit: af0ce67 (feat: Task 1 - --lenient CLI flag)
- FOUND commit: c305acd (feat: Task 2 - leniency test section)
- All 171 tests pass (python -m pytest tests/ -x -q)
