---
phase: 10-test-hardening
plan: 02
type: test
wave: 2
depends_on: ["10-01"]
files_modified:
  - pyproject.toml
  - tests/test_checksum_properties.py
  - tests/test_checksum_edges.py
  - tests/test_false_positive_corpus.py
  - tests/test_fail_closed_injection.py
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "Hypothesis properties hold: generated checksum-valid CPF/CNPJ always detected; random 11-digit invalid-checksum strings never detected in strict mode; mask(mask(x)) == mask(x); lenient hits are a superset of strict hits"
    - "Checksum edge cases are pinned by tests: repeated-sequence CPFs/CNPJs, DV=0 boundary, SUS definitive vs provisional ranges, old-plate vs Mercosul overlap"
    - "A synthetic false-positive corpus exists with a measured, asserted FP rate, and overlap resolution is deterministic across runs"
    - "Mutation testing has run over checksum validators and block/allow decision logic; surviving mutants in those modules are converted to new tests; the score is recorded in the SUMMARY"
    - "Coverage gate --cov-fail-under (branch) is set in pyproject.toml from the 10-01 Task 0 measured baseline, not guessed"
    - "The pre-existing suite still passes — no logic bug fixes in this plan; bugs found are recorded as RISCO/DECISAO items"
  artifacts:
    - path: "tests/test_checksum_properties.py"
      provides: "P5 hypothesis property-based validator and masking tests"
    - path: "tests/test_checksum_edges.py"
      provides: "P6 checksum boundary and repeated-sequence tests"
    - path: "tests/test_false_positive_corpus.py"
      provides: "P7 synthetic FP corpus with measured FP rate and overlap determinism"
    - path: "pyproject.toml"
      provides: "enforced branch-coverage gate"
      contains: "cov-fail-under"
---

<objective>
Tier 2 of the imported hardening handoff (`.planning/handoff_lacunas.md`): catch the bugs
that pass green — property-based validator testing, checksum boundary cases, a measured
false-positive corpus, mutation testing over the decision core, and the enforced
branch-coverage gate. Depends on 10-01 (dev-deps installed, baseline measured, Tier 1 green).
</objective>

<execution_context>
@.planning/handoff_lacunas.md
@AGENTS.md
</execution_context>

<context>
Same inherited non-negotiables as 10-01: synthetic fixtures only (reuse canonical constants),
never touch `_PROTECTED` paths, fail-closed on ambiguity, no logic bug fixes or production
code in this plan (RISCO/DECISAO instead), extend existing suites where the invariant already
has a test.

Project history note: a sign-inversion bug was once masked by a green test — mutation testing
(Task 3 here) exists precisely to catch that class of failure.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0 — P5 property-based validators</name>
  <files>tests/test_checksum_properties.py</files>
  <action>
  Hypothesis properties over the validators and masking:
  (a) generated checksum-valid synthetic CPF/CNPJ values are always detected in strict mode;
  (b) random 11-digit strings with invalid checksums are never detected as CPF in strict mode;
  (c) `mask_text` idempotence: masking already-masked text changes nothing;
  (d) lenient detection is a superset of strict on the same input (never masks less).
  Cap examples/deadline so the suite stays fast and deterministic in CI (fixed seed profile).
  </action>
  <verify>pytest tests/test_checksum_properties.py -q</verify>
  <done>All four properties pass without false counterexamples; commit `test(10-02): P5 hypothesis properties for validators and masking`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1 — P6 checksum edge cases</name>
  <files>tests/test_checksum_edges.py</files>
  <action>
  Pin the boundary behavior: repeated-sequence CPFs (`111.111.111-11` … `000.000.000-00`) —
  document whether strict detection accepts them (they satisfy the classic DV algorithm but
  are invalid documents); if they pass as valid, record RISCO recommending an explicit
  blacklist (no production change here). CNPJ analog; DV=0 boundary cases; SUS card
  definitive (1,2) vs provisional (7,8,9) leading-digit ranges; old plate vs Mercosul
  format ambiguity/overlap.
  </action>
  <verify>pytest tests/test_checksum_edges.py -q</verify>
  <done>Each edge has a test pinning current behavior; blacklist gap (if present) is a RISCO item; commit `test(10-02): P6 checksum boundary and blacklist edges`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — P7 false-positive corpus + overlap determinism</name>
  <files>tests/test_false_positive_corpus.py</files>
  <action>
  Build a synthetic corpus of legitimate PT-BR text (code snippets, docs prose, version
  numbers, dates, issue IDs) that should produce zero or near-zero hits; measure and assert
  the FP rate, recording the measured number in the SUMMARY. Test overlap resolution
  determinism: inputs where spans compete (CPF-like vs phone, boleto vs PIS/PASEP) must
  produce the same winner on every run.
  </action>
  <verify>pytest tests/test_false_positive_corpus.py -q</verify>
  <done>FP rate measured, asserted, and recorded; overlap winner deterministic across repeated runs; commit `test(10-02): P7 FP corpus and deterministic overlap resolution`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — P4 mutation testing + coverage gate</name>
  <files>pyproject.toml, tests/test_checksum_properties.py, tests/test_checksum_edges.py, tests/test_fail_closed_injection.py (survivor tests append to the suites created in 10-01/10-02 Tasks 0-2 — no new test files)</files>
  <action>
  Run `mutmut` scoped to checksum validators and block/allow decision code (`detection.py`,
  `policy.py`, `masking.py`, decision branches of `hooks.py`). KNOWN RISK: mutmut has poor
  Windows support (fork-based workers) — if it fails to run on win32, record a DECISAO to run
  it under WSL/CI and capture the attempted command + error in the SUMMARY instead of
  skipping silently. Record the mutation score; convert surviving mutants in checksum/decision
  code into new tests appended to the Tier 1/2 suites. Finally set `--cov-fail-under` (branch)
  in `pyproject.toml` addopts/config using the 10-01 Task 0 measured baseline.
  </action>
  <verify>pytest --cov=privguard --cov-branch -q</verify>
  <done>Mutation score (or WSL/CI DECISAO with evidence) in SUMMARY; survivors in decision code have tests; coverage gate active and passing; commit `test(10-02): P4 mutation report, survivor tests, coverage gate`.</done>
</task>

</tasks>

<verification>
- Full suite green including 10-01 suites and the pre-existing baseline.
- `pytest --cov=privguard --cov-branch --cov-fail-under=<baseline>` passes with the gate enforced from config.
- SUMMARY records: mutation score (or DECISAO), measured FP rate, and the consolidated RISCO/DECISAO list for the phase.
</verification>

<success_criteria>
Handoff "critério de pronto" items 1–5 complete for Tiers 1–2 combined: baseline recorded
(10-01), Tier 1 and Tier 2 green, `--cov-fail-under` active, mutation score reported with
survivors handled, and every pass-through surfaced as RISCO/DECISAO, never silenced.
</success_criteria>
