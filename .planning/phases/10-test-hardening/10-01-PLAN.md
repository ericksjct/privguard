---
phase: 10-test-hardening
plan: 01
type: test
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_fail_closed_injection.py
  - tests/test_evasion_adversarial.py
  - tests/test_redos_size_guard.py
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "Branch-coverage baseline is measured with pytest --cov=privguard --cov-branch and recorded in the SUMMARY before any new test is written"
    - "Every injected detector failure (exception, missing Presidio extra, timeout, oversized input, invalid pyproject config) results in a block decision — zero paths where exception leads to allow"
    - "Every adversarial evasion vector has a test documenting current behavior; pass-throughs are labeled RISCO in the SUMMARY, never silenced"
    - "No synthetic input drives the detector past the ReDoS latency bound; current input-size behavior is documented (no production code added in this plan)"
    - "The pre-existing suite still passes at its baseline — no logic bug fixes in this plan; bugs found are recorded as RISCO/DECISAO items"
  artifacts:
    - path: "tests/test_fail_closed_injection.py"
      provides: "P1 fail-closed failure-injection suite"
    - path: "tests/test_evasion_adversarial.py"
      provides: "P2 adversarial evasion vectors with documented current behavior"
    - path: "tests/test_redos_size_guard.py"
      provides: "P3 ReDoS latency bound and input-size behavior tests"
    - path: "pyproject.toml"
      provides: "dev optional-dependencies group (pytest-cov, hypothesis, mutmut)"
---

<objective>
Prove the fail-closed promise instead of assuming it: the existing suite exercises the happy
path ("well-formed PII gets masked"); this plan adds Tier 1 of the imported hardening handoff
(`.planning/handoff_lacunas.md`) — failure injection, adversarial evasion, and ReDoS
robustness. Tier 2 (properties, edges, FP corpus, mutation, coverage gate) is plan 10-02.
Tiers 3–4 remain in the handoff as follow-up candidates.
</objective>

<execution_context>
@.planning/handoff_lacunas.md
@AGENTS.md
</execution_context>

<context>
Inherited non-negotiables (from AGENTS.md and PROJECT.md, restated by the handoff):

- Never read or write `.env`, `data_sensivel/`, or any `_PROTECTED` path from `privguard/cleanup.py`.
- Synthetic fixtures only — reuse the canonical constants from `tests/test_v1_regression_gate.py`
  and `tests/test_claude_hooks.py` (e.g. `RAW_CPF = "123.456.789-09"`); do not invent new
  realistic Brazilian PII.
- Fail-closed: when a test exposes ambiguity about what a surface should do, the expected
  behavior is block.
- **Do not fix logic bugs and do not add production code in this plan.** A test that exposes
  a bug or a missing guard documents the current behavior, is marked RISCO/DECISAO in the
  SUMMARY, and the fix goes to a separate thread.
- Where an invariant already has a test in `tests/test_v1_regression_gate.py` or
  `tests/test_claude_hooks.py`, EXTEND that suite instead of duplicating the assert in a new
  file (Phase 5 overlap warning from the import conflict report). New files are only for new
  vectors: injection, evasion, ReDoS.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0 — Baseline + dev-deps</name>
  <files>pyproject.toml</files>
  <action>
  Add a `dev` group under `[project.optional-dependencies]` with `pytest-cov`, `hypothesis`,
  and `mutmut`. Then run `pytest --cov=privguard --cov-report=term-missing --cov-branch` and
  record in the plan SUMMARY: the branch-coverage percentage, the missing-branch list for
  `detection.py`, `policy.py`, `masking.py`, `hooks.py`, and the pre-existing pass/skip counts.
  </action>
  <verify>pip install -e ".[dev]" && pytest --cov=privguard --cov-branch -q</verify>
  <done>Baseline % and missing branches recorded in SUMMARY; suite green at pre-existing baseline; commit `test(10-01): add dev-deps and record branch-coverage baseline`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1 — P1 fail-closed failure injection</name>
  <files>tests/test_fail_closed_injection.py</files>
  <action>
  Prove detector failure never becomes silent pass-through. Scenarios, each asserting the
  block decision (hook exit code 2 or explicit deny) and sanitized output:
  (a) monkeypatched `detect()` raising an exception → hook blocks;
  (b) Presidio/`[full]` import-failure path → block, not allow;
  (c) simulated detector timeout (monkeypatched slow detect) → block;
  (d) 10 MB synthetic input → explicit block or rejection, no crash/OOM;
  (e) malformed `[tool.privguard]` section in a tmp pyproject → sanitized fail-closed error.
  Reuse the `assert_no_prompt_derived_text` pattern from `tests/test_claude_hooks.py` to
  assert no raw synthetic value appears in any output.
  </action>
  <verify>pytest tests/test_fail_closed_injection.py -q</verify>
  <done>All five scenarios assert block; zero exception-to-allow paths; no raw fixture value in output; commit `test(10-01): P1 fail-closed failure-injection suite`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — P2 adversarial evasion</name>
  <files>tests/test_evasion_adversarial.py</files>
  <action>
  One test per evasion vector, each documenting current behavior (mask, block, or
  pass-through) with an explicit assertion of that behavior:
  homoglyph digits (fullwidth/cyrillic), zero-width chars inside a CPF, combining chars,
  CPF fragmented across lines, whitespace-injected digits, base64/hex/URL-encoded secret,
  code concatenation (`"123.456" + ".789-09"`, f-string), PII inside code fence, markdown
  link, and comment. Vectors that pass through undetected get a `# RISCO:` comment on the
  test and an entry in the SUMMARY RISCO list — never xfail-and-forget, never silenced.
  </action>
  <verify>pytest tests/test_evasion_adversarial.py -q</verify>
  <done>Every vector has a passing test pinning current behavior; RISCO list in SUMMARY matches the `# RISCO:` comments; commit `test(10-01): P2 adversarial evasion vectors with RISCO labeling`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — P3 ReDoS + input-size behavior</name>
  <files>tests/test_redos_size_guard.py</files>
  <action>
  Craft backtracking-hostile inputs against the detection regexes (long digit runs,
  repeated near-match prefixes) and assert a latency bound per `detect()` call.
  Test and document CURRENT behavior for oversized inputs — do NOT add a size guard or any
  production code in this plan; if no guard exists, record it as a DECISAO item in the
  SUMMARY (candidate fix thread). If any regex proves backtracking-risky, record re2
  migration as a DECISAO item likewise.
  </action>
  <verify>pytest tests/test_redos_size_guard.py -q</verify>
  <done>Latency bound asserted for hostile inputs; oversized-input behavior pinned by test; missing guard / re2 evaluation captured as DECISAO if applicable; commit `test(10-01): P3 ReDoS latency bound and input-size behavior`.</done>
</task>

</tasks>

<verification>
- `pytest -q` green: new suites pass and the pre-existing baseline (re-measured at Task 0) is intact.
- SUMMARY records: branch-coverage baseline, missing-branch list, RISCO list (evasion pass-throughs), DECISAO list (size guard / re2 / bugs found).
- Zero raw synthetic sensitive values in any test output (existing regression-gate invariant).
</verification>

<success_criteria>
Handoff "critério de pronto" items 1 and 2 restricted to Tier 1: baseline measured and
recorded; P1–P3 implemented and green; every evasion pass-through surfaced as RISCO/DECISAO,
never silenced. Tier 2 and the coverage/mutation gates complete in plan 10-02.
</success_criteria>
