---
phase: 09-milestone-v1-0-audit-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.en.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "README.md (PT) warn-vs-block FAQ documents the PII_GUARD_MODE selector (block/warn/mask) instead of declaring warn-only out of scope"
    - "README.en.md (EN) warn-vs-block FAQ documents the same PII_GUARD_MODE selector with content parity to PT"
    - "Both FAQs state block is the default fail-closed mode (exit 2)"
    - "Both FAQs label warn as opt-in and non-protective (local_development_non_protective)"
    - "Both FAQs describe mask as block + show-masked-for-copy-paste (exit 2), not automatic masking"
    - "Neither FAQ claims privguard warns by default or automatically masks"
  artifacts:
    - path: "README.md"
      provides: "Rewritten PT warn-vs-block FAQ documenting PII_GUARD_MODE"
      contains: "PII_GUARD_MODE"
    - path: "README.en.md"
      provides: "Rewritten EN warn-vs-block FAQ documenting PII_GUARD_MODE"
      contains: "PII_GUARD_MODE"
  key_links:
    - from: "README.md FAQ"
      to: "README.en.md FAQ"
      via: "content parity — same three-mode semantics in both languages"
      pattern: "PII_GUARD_MODE"
---

<objective>
Rewrite the "Why does it block instead of warn?" FAQ in both README files to document the
shipped `PII_GUARD_MODE` selector (block default / warn opt-in non-protective / mask) that
Phase 8 delivered, replacing the now-false "warn-only is explicitly out of scope" claim. This
closes the README/Phase-8 documentation drift flagged by the audit (DOC-01 functional drift).

Purpose: Closes ROADMAP Phase 9 success criterion 3. The default behavior does NOT change —
this is a doc-only edit. The default is still fail-closed block.
Output: Two updated README files (PT default + EN) with content parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-milestone-v1-0-audit-cleanup/09-RESEARCH.md
@README.md
@README.en.md

<interfaces>
<!-- PII_GUARD_MODE behavior VERIFIED from privguard/hooks.py (research, read 2026-06-11). -->
<!-- The FAQ rewrite MUST faithfully match this table — do NOT overclaim. -->

| Mode  | Env value      | Behavior on PII detected                                                                 | Exit | Protective? |
|-------|----------------|------------------------------------------------------------------------------------------|------|-------------|
| block | unset or block | Blocks prompt; sanitized `[PII-GUARD BLOQUEADO]` diagnostic to stderr                     | 2    | Yes (fail-closed) |
| warn  | warn           | Allows prompt through; emits warn action; tagged `mode_scope=local_development_non_protective` | 0    | No — opt-in, non-protective |
| mask  | mask           | STILL blocks (exit 2); shows a masked version in stderr to copy-paste; if mask unverifiable → block with `mask_verification_failed` | 2 | Yes (block + show-masked) |

Key facts (do NOT contradict):
- Default is fail-closed block; the default did NOT change.
- warn is explicitly non-protective, intended for local development only.
- mask never auto-forwards a sanitized payload (Claude Code UserPromptSubmit v2.1.150 has no
  prompt-replacement field) — document it as "blocks AND prints a masked version you can copy-paste".
- Do NOT claim privguard "warns by default" or "automatically masks".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the warn-vs-block FAQ in README.md (PT) and README.en.md (EN) with parity</name>
  <files>README.md, README.en.md</files>
  <read_first>
    - README.md (PT default; the FAQ anchor at lines 244-251 — "Por que ele bloqueia em vez de avisar?")
    - README.en.md (EN; the FAQ anchor at lines 214-216 — "Why does it block instead of warn?")
    - .planning/phases/09-milestone-v1-0-audit-cleanup/09-RESEARCH.md (Anchors 11 and 12 verbatim; "PII_GUARD_MODE behavior" table; Pitfall 4 re: parity; Security Domain "README overclaim" mitigation)
  </read_first>
  <action>
Two parallel edits (one per file), keeping substance identical across languages (research Pitfall 4).

EDIT 1 — README.md (Portuguese, research Anchor 11). Replace this entire FAQ block:
OLD:
```
### Por que ele bloqueia em vez de avisar?

O modo somente-aviso está explicitamente fora do escopo (consulte
[O que o privguard NÃO faz](#o-que-o-privguard-não-faz)). O valor central do privguard é impedir
que dados sensíveis alcancem um provedor externo de LLM — um aviso que o usuário pode ignorar não
satisfaria esse objetivo. O comportamento fail-closed estrito é o padrão para fluxos de trabalho
com provedores externos; a reescrita é usada apenas em superfícies onde a substituição de payload
de saída é verificada.
```
NEW:
```
### Por que ele bloqueia em vez de avisar?

Bloquear é o comportamento **padrão** e fail-closed — o valor central do privguard é impedir que
dados sensíveis alcancem um provedor externo de LLM. Mas o modo é configurável via a variável de
ambiente `PII_GUARD_MODE`:

| Modo | `PII_GUARD_MODE` | Comportamento ao detectar PII | Código de saída | Protege? |
|------|------------------|-------------------------------|-----------------|----------|
| `block` (padrão) | não definida ou `block` | Bloqueia o prompt; diagnóstico sanitizado `[PII-GUARD BLOQUEADO]` no stderr | 2 | Sim (fail-closed) |
| `warn` | `warn` | Deixa o prompt passar; registra ação de aviso; marcado `mode_scope=local_development_non_protective` | 0 | **Não** — opt-in, não protetivo |
| `mask` | `mask` | Continua bloqueando (saída 2); mostra uma versão mascarada no stderr para você copiar e reenviar manualmente; se a máscara não puder ser verificada → bloqueia com `mask_verification_failed` | 2 | Sim (bloqueia + mostra mascarado) |

O modo `warn` é **explicitamente não protetivo** e destina-se apenas a desenvolvimento local: um
aviso que o usuário pode ignorar não satisfaz a garantia de privacidade. O modo `mask` nunca
encaminha automaticamente um payload sanitizado — o esquema `UserPromptSubmit` do Claude Code não
tem campo de substituição de prompt, então bloquear e imprimir a versão mascarada para cópia
manual é o único caminho seguro. O padrão permanece fail-closed block.
```

EDIT 2 — README.en.md (English, research Anchor 12). Replace this entire FAQ block:
OLD:
```
### Why does it block instead of warn?

Warning-only mode is explicitly out of scope (see [What privguard does NOT do](#what-privguard-does-not-do)). The core value of privguard is preventing sensitive data from reaching an external LLM provider — a warning that the user can ignore would not satisfy that goal. Strict fail-closed behavior is the default for external-provider workflows; rewrite is only used on surfaces where outbound payload replacement is verified.
```
NEW:
```
### Why does it block instead of warn?

Blocking is the **default**, fail-closed behavior — the core value of privguard is preventing sensitive data from reaching an external LLM provider. But the mode is configurable via the `PII_GUARD_MODE` environment variable:

| Mode | `PII_GUARD_MODE` | Behavior on PII detected | Exit code | Protective? |
|------|------------------|--------------------------|-----------|-------------|
| `block` (default) | unset or `block` | Blocks the prompt; sanitized `[PII-GUARD BLOQUEADO]` diagnostic to stderr | 2 | Yes (fail-closed) |
| `warn` | `warn` | Lets the prompt through; emits a warn action; tagged `mode_scope=local_development_non_protective` | 0 | **No** — opt-in, non-protective |
| `mask` | `mask` | Still blocks (exit 2); shows a masked version in stderr for you to copy-paste and resubmit manually; if the mask cannot be verified → blocks with `mask_verification_failed` | 2 | Yes (block + show-masked) |

The `warn` mode is **explicitly non-protective** and intended for local development only: a warning the user can ignore does not satisfy the privacy guarantee. The `mask` mode never auto-forwards a sanitized payload — Claude Code's `UserPromptSubmit` schema has no prompt-replacement field, so blocking and printing the masked version for manual copy-paste is the only safe path. The default remains fail-closed block.
```

Do NOT touch the adjacent FAQ entries ("What if a CPF is missed?" / "How do I extend the cleanup patterns?"). Do NOT edit `.claude/worktrees/**` README copies (git worktrees, not canonical). Keep the two language versions substantively identical (same three-mode table).
  </action>
  <verify>
    <automated>grep -c "PII_GUARD_MODE" README.md; grep -c "PII_GUARD_MODE" README.en.md; grep -c "fora do escopo" README.md; grep -c "out of scope" README.en.md; grep -c "local_development_non_protective" README.md; grep -c "local_development_non_protective" README.en.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PII_GUARD_MODE" README.md` returns at least 1 match (selector documented in PT)
    - `grep "PII_GUARD_MODE" README.en.md` returns at least 1 match (selector documented in EN)
    - In the warn-vs-block FAQ, README.md no longer contains "está explicitamente fora do escopo" (the out-of-scope claim is removed)
    - In the warn-vs-block FAQ, README.en.md no longer contains "Warning-only mode is explicitly out of scope"
    - README.md contains `local_development_non_protective`
    - README.en.md contains `local_development_non_protective`
    - Both files describe `mask` as still blocking (exit 2) and showing a masked version — NEITHER claims automatic masking or warn-by-default
    - Both FAQs state block is the default
  </acceptance_criteria>
  <done>Both README warn-vs-block FAQs document the three-mode PII_GUARD_MODE selector with content parity; the "out of scope" claim is gone; the default-is-block message and the non-protective/no-auto-mask honesty constraints are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (doc-only) | This plan edits only `README.md` and `README.en.md`. No runtime code is touched; the default block behavior is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Repudiation / false assurance | README warn-vs-block FAQ | mitigate | The rewrite must NOT claim privguard "warns by default" or "automatically masks" — both would be overclaims contradicting verified hooks.py behavior (research Security Domain). FAQ states block=default, warn=non-protective opt-in, mask=block+show-masked. Acceptance criteria grep-enforce the absence of overclaim wording. |
| T-09-04 | Information Disclosure | README content | accept | Doc-only; no secrets, paths, or runtime values added. The masked-version description references behavior already shipped in hooks.py; no new attack surface. |

No code behavior change: the README rewrite is documentation only and the default stays fail-closed block.
</threat_model>

<verification>
- Both README FAQs document the PII_GUARD_MODE three-mode selector with parity.
- The "out of scope" / "explicitamente fora do escopo" warn-only claim is removed from both.
- No overclaim: neither file says warn-by-default or automatic masking.
- `.claude/worktrees/**` README copies are untouched.
</verification>

<success_criteria>
ROADMAP Phase 9 success criterion 3 is met: README.md (PT) and README.en.md (EN) document the
PII_GUARD_MODE selector (block default / warn opt-in non-protective / mask) instead of declaring
warn-only out of scope, with faithful, non-overclaiming behavior descriptions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-milestone-v1-0-audit-cleanup/09-02-SUMMARY.md`
</output>
