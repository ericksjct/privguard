---
phase: 01-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - privguard/__init__.py
  - privguard/cli.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
must_haves:
  truths:
    - "Developer can run `python -m pip install -e .` from the repo root."
    - "Developer can run `privguard info` after editable install."
    - "`privguard info` reports the package name, version, and lightweight detector tier without importing Presidio or spaCy."
  artifacts:
    - path: "pyproject.toml"
      provides: "setuptools build metadata, editable-install support, console script"
      contains: "[project.scripts]"
    - path: "privguard/__init__.py"
      provides: "importable package identity"
      contains: "__version__"
    - path: "privguard/cli.py"
      provides: "`privguard info` command"
      contains: "def main"
  key_links:
    - from: "pyproject.toml"
      to: "privguard.cli:main"
      via: "[project.scripts]"
      pattern: "privguard = \"privguard.cli:main\""
    - from: "privguard/cli.py"
      to: "importlib.metadata.version"
      via: "installed distribution metadata"
      pattern: "version\\(\"privguard\"\\)"
---

<objective>
Create the installable package foundation for `privguard`.

Purpose: Satisfy D-01, D-05, D-06, and D-07 by making the repo installable with setuptools and exposing the locked `privguard` CLI name.
Output: `pyproject.toml`, importable `privguard` package identity, and a sanitized `privguard info` command.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-PATTERNS.md
@AGENTS.md

<interfaces>
From `01-CONTEXT.md`:
```text
D-01: Package name is `privguard` (Python import: `import privguard`, CLI: `privguard`).
D-05: Use `pyproject.toml` with setuptools backend.
D-06: Intended install flow is `git clone <repo>` -> `pip install -e .`.
D-07: Phase 1 exposes at minimum `privguard info`.
```

From `01-RESEARCH.md`:
```toml
[project.scripts]
privguard = "privguard.cli:main"

[project.optional-dependencies]
full = [
  "presidio-analyzer==2.2.362; python_version < '3.14'",
  "presidio-anonymizer==2.2.362",
  "spacy==3.8.14",
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setuptools package metadata</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-package-foundation/01-CONTEXT.md
    - .planning/phases/01-package-foundation/01-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - pyproject.toml if it already exists
  </read_first>
  <action>Create `pyproject.toml` with `[build-system] requires = ["setuptools >= 77.0.3"]`, `build-backend = "setuptools.build_meta"`, `[project] name = "privguard"`, `version = "0.1.0"`, `requires-python = ">=3.10"`, `dependencies = []`, `[project.optional-dependencies].full` containing `presidio-analyzer==2.2.362; python_version < '3.14'`, `presidio-anonymizer==2.2.362`, and `spacy==3.8.14`, `[project.scripts] privguard = "privguard.cli:main"`, and `[tool.setuptools.packages.find] include = ["privguard"]`. Do not add a `privacy-guard` script alias because D-01 locks the CLI name to `privguard`.</action>
  <verify>
    <automated>python -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); assert data['project']['name']=='privguard'; assert data['project']['dependencies']==[]; assert data['project']['scripts']['privguard']=='privguard.cli:main'; assert 'full' in data['project']['optional-dependencies']"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains exact string `name = "privguard"`.
    - `pyproject.toml` contains exact string `dependencies = []`.
    - `pyproject.toml` contains exact string `privguard = "privguard.cli:main"`.
    - `pyproject.toml` does not contain exact string `privacy-guard =`.
    - `pyproject.toml` contains exact string `presidio-analyzer==2.2.362; python_version < '3.14'` only under `[project.optional-dependencies]`.
  </acceptance_criteria>
  <done>Editable-install metadata exists and default dependencies remain empty.</done>
</task>

<task type="auto">
  <name>Task 2: Add package identity and `privguard info`</name>
  <files>privguard/__init__.py, privguard/cli.py</files>
  <read_first>
    - .planning/phases/01-package-foundation/01-CONTEXT.md
    - .planning/phases/01-package-foundation/01-PATTERNS.md
    - privguard/__init__.py if it already exists
    - privguard/cli.py if it already exists
    - ollama_local_demo.py
  </read_first>
  <action>Create `privguard/__init__.py` with `__version__ = "0.1.0"` and `__all__ = ["__version__"]`. Create `privguard/cli.py` using stdlib `argparse` and `importlib.metadata`. Implement `main(argv: list[str] | None = None) -> int`, `cmd_info(_args: argparse.Namespace) -> int`, and `if __name__ == "__main__": raise SystemExit(main())`. The `info` command must print exactly three sanitized lines: `privguard 0.1.0` or installed metadata version, `detectors: lightweight`, and `optional_full: available via privguard[full]`. Do not import `presidio_analyzer`, `presidio_anonymizer`, `spacy`, or demo modules from `privguard/__init__.py` or `privguard/cli.py`.</action>
  <verify>
    <automated>python -m pip install -e .; if ($LASTEXITCODE -ne 0) { exit 1 }; privguard info; if ($LASTEXITCODE -ne 0) { exit 1 }; python -c "import privguard; import privguard.cli; assert privguard.__version__ == '0.1.0'; assert privguard.cli.main(['info']) == 0"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `privguard/__init__.py` contains exact string `__version__ = "0.1.0"`.
    - `privguard/cli.py` contains exact string `subparsers.add_parser("info")`.
    - `privguard/cli.py` contains exact string `detectors: lightweight`.
    - `privguard/cli.py` contains no string `presidio`.
    - `privguard/cli.py` contains no string `spacy`.
    - Running `privguard info` exits `0` and stdout contains `privguard`, `detectors: lightweight`, and `optional_full: available via privguard[full]`.
  </acceptance_criteria>
  <done>`privguard info` works after editable install and emits only installation/detector metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> package installer | Local install command reads package metadata and creates console script wrappers. |
| CLI stdout -> terminal/logs | Diagnostic text may be copied into tickets or prompts. |
| package import path -> optional extras | Default import path must not import optional Presidio/spaCy modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `privguard info` | mitigate | Print only package version, detector tier, and optional-extra availability; never scan text or print matched values in this plan. |
| T-01-01-02 | Denial of Service | `pyproject.toml` dependencies | mitigate | Keep `[project].dependencies = []`; place Presidio/spaCy only under `[project.optional-dependencies].full` with the Python `<3.14` marker for `presidio-analyzer`. |
| T-01-01-03 | Spoofing | console script name | mitigate | Use only `[project.scripts] privguard = "privguard.cli:main"` per D-01; do not add `privacy-guard`. |
| T-01-01-04 | Information Disclosure | implementation process | mitigate | Do not read `.env` or `data_sensivel/**`; all verification uses metadata and imports only. |
</threat_model>

<verification>
Run:
```powershell
python -m pip install -e .
privguard info
python -c "import privguard; import privguard.cli; assert privguard.cli.main(['info']) == 0"
python -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); assert data['project']['dependencies']==[]"
```
</verification>

<success_criteria>
PKG-01 and PKG-02 are satisfied for the locked `privguard` name: editable install works, the console script exists, and CLI diagnostics are sanitized and lightweight.
</success_criteria>

<source_audit>
GOAL covered: installable reusable local Python tool via `pyproject.toml` and `privguard info`.
REQ covered: PKG-01, PKG-02, PKG-03.
RESEARCH covered: setuptools backend, empty default dependencies, optional `[full]`, argparse CLI, importlib metadata.
CONTEXT covered: D-01, D-03, D-04, D-05, D-06, D-07.
Deferred excluded: additional CLI subcommands beyond `info`; Presidio/spaCy full detection implementation.
</source_audit>

<output>
After completion, create `.planning/phases/01-package-foundation/01-package-foundation-01-SUMMARY.md`.
</output>
