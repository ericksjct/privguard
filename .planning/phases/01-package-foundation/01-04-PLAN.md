---
phase: 01-package-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - demos/test_presidio.py
  - demos/test_presidio_br.py
  - demos/reversible_demo.py
  - demos/ollama_local_demo.py
  - test_presidio.py
  - test_presidio_br.py
  - reversible_demo.py
  - ollama_local_demo.py
autonomous: true
requirements:
  - PKG-04
must_haves:
  truths:
    - "Root-level Presidio/Ollama demo scripts are no longer in the production package surface."
    - "Existing demos live under `demos/`."
    - "Running demo defaults does not print raw original sensitive-looking sample values."
  artifacts:
    - path: "demos/test_presidio.py"
      provides: "separated generic Presidio demo"
      contains: "def main"
    - path: "demos/test_presidio_br.py"
      provides: "separated Brazilian Presidio demo"
      contains: "def main"
    - path: "demos/reversible_demo.py"
      provides: "separated reversible anonymization demo"
      contains: "def main"
    - path: "demos/ollama_local_demo.py"
      provides: "separated local Ollama demo"
      contains: "def main"
  key_links:
    - from: "repo root"
      to: "demos/"
      via: "file move"
      pattern: "demos/test_presidio.py"
    - from: "demo stdout"
      to: "sanitized examples"
      via: "redacted default printing"
      pattern: "<BR_CPF>|<EMAIL>|<API_KEY>|<PII>"
---

<objective>
Separate demo scripts from production package behavior and sanitize their default output.

Purpose: Satisfy D-08 and PKG-04 by moving current root demos into `demos/` and removing default raw-value printing patterns from demo output.
Output: `demos/` directory containing the four existing demos with safe default display behavior, and no root-level demo scripts.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-PATTERNS.md
@AGENTS.md
@test_presidio.py
@test_presidio_br.py
@reversible_demo.py
@ollama_local_demo.py

<interfaces>
From `01-CONTEXT.md`:
```text
D-08: Move existing demo scripts to `demos/` or archive. They must not print raw sensitive values by default if left runnable.
```

From `01-PATTERNS.md`:
```text
Avoid production/demo default patterns like `print(f"Original : {text}")`, `print(f"    {text}")`, and raw prompt printing.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move root demos into `demos/`</name>
  <files>test_presidio.py, test_presidio_br.py, reversible_demo.py, ollama_local_demo.py, demos/test_presidio.py, demos/test_presidio_br.py, demos/reversible_demo.py, demos/ollama_local_demo.py</files>
  <read_first>
    - test_presidio.py
    - test_presidio_br.py
    - reversible_demo.py
    - ollama_local_demo.py
    - .planning/phases/01-package-foundation/01-CONTEXT.md
    - .planning/phases/01-package-foundation/01-PATTERNS.md
    - demos/ directory listing if it already exists
  </read_first>
  <action>Create `demos/` and move `test_presidio.py`, `test_presidio_br.py`, `reversible_demo.py`, and `ollama_local_demo.py` into it as `demos/test_presidio.py`, `demos/test_presidio_br.py`, `demos/reversible_demo.py`, and `demos/ollama_local_demo.py`. Update local demo imports that reference sibling files so they work from the `demos/` directory, specifically keep `reversible_demo.py` importing `build_br_recognizers` from the moved `demos/test_presidio_br.py` via its existing local `sys.path.insert(0, str(pathlib.Path(__file__).parent))` pattern.</action>
  <verify>
    <automated>python -c "from pathlib import Path; assert Path('demos/test_presidio.py').exists(); assert Path('demos/test_presidio_br.py').exists(); assert Path('demos/reversible_demo.py').exists(); assert Path('demos/ollama_local_demo.py').exists(); assert not Path('test_presidio.py').exists(); assert not Path('test_presidio_br.py').exists(); assert not Path('reversible_demo.py').exists(); assert not Path('ollama_local_demo.py').exists()"</automated>
  </verify>
  <acceptance_criteria>
    - `demos/test_presidio.py` exists.
    - `demos/test_presidio_br.py` exists.
    - `demos/reversible_demo.py` exists.
    - `demos/ollama_local_demo.py` exists.
    - Root `test_presidio.py` does not exist.
    - Root `test_presidio_br.py` does not exist.
    - Root `reversible_demo.py` does not exist.
    - Root `ollama_local_demo.py` does not exist.
  </acceptance_criteria>
  <done>Demo files are outside the production root/package surface.</done>
</task>

<task type="auto">
  <name>Task 2: Remove raw-value default demo printing</name>
  <files>demos/test_presidio.py, demos/test_presidio_br.py, demos/reversible_demo.py, demos/ollama_local_demo.py</files>
  <read_first>
    - demos/test_presidio.py
    - demos/test_presidio_br.py
    - demos/reversible_demo.py
    - demos/ollama_local_demo.py
    - .planning/phases/01-package-foundation/01-PATTERNS.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Edit the moved demo files so default stdout never prints the raw original sample text, raw prompt, raw CPF/CNPJ/email/API-key-like value, or raw encrypted/decrypted roundtrip source text. Replace lines like `print(f"Original : {text}")`, `print(f"    {text}")`, and raw prompt prints with redacted markers or metadata such as sample number, character length, detected entity kinds, and anonymized/redacted output. Keep demos runnable as direct scripts with `main()` and `if __name__ == "__main__":` guards. Do not read `.env` or any `data_sensivel/**` file while editing or verifying.</action>
  <verify>
    <automated>python -m py_compile demos\test_presidio.py demos\test_presidio_br.py demos\reversible_demo.py demos\ollama_local_demo.py; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "Original : \\{text\\}|print\\(f?\\\"\\s*\\{text\\}\\\"\\)|529\\.982\\.247-25|11\\.222\\.333/0001-81|sk-ant-fake|AKIAIOSFODNN7EXAMPLE" demos && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile demos\test_presidio.py demos\test_presidio_br.py demos\reversible_demo.py demos\ollama_local_demo.py` exits `0`.
    - `rg -n "Original : \{text\}" demos` returns no matches.
    - `rg -n "529\.982\.247-25|11\.222\.333/0001-81|sk-ant-fake|AKIAIOSFODNN7EXAMPLE" demos` returns no matches.
    - Each moved demo still contains exact string `if __name__ == "__main__"`.
  </acceptance_criteria>
  <done>Demos are separated and their default source/output no longer includes the known raw sensitive-looking sample values from the root demo era.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo source -> repository history | Demo examples are committed and may be copied into prompts or docs. |
| demo stdout -> terminal/logs | Demo output may be captured by terminals, CI logs, or agents. |
| root files -> production package boundary | Root scripts can be mistaken for production behavior or tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | moved demo source | mitigate | Remove known raw sensitive-looking literals from moved demo files and replace default display with redacted markers or metadata. |
| T-01-04-02 | Information Disclosure | demo stdout | mitigate | Remove default prints of `Original : {text}`, raw prompt text, and decrypted/source text. |
| T-01-04-03 | Spoofing | root `test_*.py` demos | mitigate | Move root demos to `demos/` so they are not confused with production package modules or test suite files. |
| T-01-04-04 | Information Disclosure | implementation/tests | mitigate | Verification compiles files and greps source only; it does not read `.env` or `data_sensivel/**`. |
</threat_model>

<verification>
Run:
```powershell
python -m py_compile demos\test_presidio.py demos\test_presidio_br.py demos\reversible_demo.py demos\ollama_local_demo.py
if ($LASTEXITCODE -ne 0) { exit 1 }
python -c "from pathlib import Path; assert not Path('test_presidio.py').exists(); assert Path('demos/test_presidio.py').exists()"
rg -n "Original : \{text\}|529\.982\.247-25|11\.222\.333/0001-81|sk-ant-fake|AKIAIOSFODNN7EXAMPLE" demos
```
The final `rg` command must return no matches.
</verification>

<success_criteria>
PKG-04 is satisfied: demos are clearly separated from production code and default demo source/output avoids known raw sensitive-looking sample values.
</success_criteria>

<source_audit>
GOAL covered: existing demos are separated from production-safe behavior.
REQ covered: PKG-04.
RESEARCH covered: root demo quarantine, no raw default output, no root `test_*.py` demo files.
CONTEXT covered: D-08.
Deferred excluded: preserving or maintaining demo feature completeness beyond safe separation.
</source_audit>

<output>
After completion, create `.planning/phases/01-package-foundation/01-package-foundation-04-SUMMARY.md`.
</output>
