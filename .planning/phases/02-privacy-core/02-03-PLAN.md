---
phase: 02-privacy-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - privguard/policy.py
  - privguard/diagnostics.py
  - tests/test_policy.py
autonomous: true
requirements:
  - DET-05
  - MASK-04
  - POL-01
  - POL-02
  - POL-03
  - POL-04
must_haves:
  truths:
    - "User can classify protected path strings without reading protected file contents."
    - "Unknown, unclassified, external, unsupported, and unverified surfaces fail closed by default."
    - "Rewrite-capable surfaces allow masked content only after verification."
    - "Policy decisions expose sanitized reason codes, capability labels, and decision status."
  artifacts:
    - path: "privguard/policy.py"
      provides: "PathClassification, SurfaceCapability, PolicyMode, PolicyDecision, and fail-closed decision function"
      contains: "PolicyDecision"
    - path: "tests/test_policy.py"
      provides: "Synthetic policy, path normalization, and sanitized decision tests"
      contains: "test_"
  key_links:
    - from: "privguard/policy.py"
      to: "privguard/masking.py"
      via: "MaskResult verification status"
      pattern: "verified"
    - from: "privguard/policy.py"
      to: "privguard/diagnostics.py"
      via: "sanitized decision metadata"
      pattern: "reason_codes"
---

<objective>
Implement protected-path classification and shared fail-closed policy decisions for the Phase 2 core.

Purpose: Downstream integrations need one package-owned decision model that distinguishes rewrite-capable, block-only, observe-only, unsupported, unknown, and external surfaces without making unsafe protection claims.
Output: `privguard.policy` classifies path strings, models capabilities, and returns sanitized allow/block/pause decisions using detection and mask verification status.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-privacy-core/02-CONTEXT.md
@.planning/phases/02-privacy-core/02-RESEARCH.md
@.planning/phases/02-privacy-core/02-PATTERNS.md
@privguard/policy.py
@privguard/masking.py
@privguard/diagnostics.py

<interfaces>
Consumes Plan 02-02 target contract:
```python
@dataclass(frozen=True)
class MaskResult:
    text: str
    changed: bool
    verified: bool
    verification_status: str
    reason_codes: tuple[str, ...]
    hits: tuple[Hit, ...]
```

Target policy contract:
```python
@dataclass(frozen=True)
class PathClassification:
    is_protected: bool
    category: str
    reason_code: str

class SurfaceCapability:
    REWRITE_CAPABLE = "rewrite-capable"
    BLOCK_ONLY = "block-only"
    OBSERVE_ONLY = "observe-only"
    UNSUPPORTED = "unsupported"
    UNKNOWN = "unknown"
    EXTERNAL = "external"

@dataclass(frozen=True)
class PolicyDecision:
    action: str  # allow, block, pause
    allow: bool
    reason_codes: tuple[str, ...]
    capability: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand protected-path classification</name>
  <files>privguard/policy.py, privguard/diagnostics.py, tests/test_policy.py</files>
  <read_first>
    - `.planning/phases/02-privacy-core/02-CONTEXT.md` decisions D-02, D-13
    - `.planning/phases/02-privacy-core/02-RESEARCH.md` DET-05 and protected path hygiene
    - `privguard/policy.py`
  </read_first>
  <behavior>
    - `.env`, `.env.*`, protected dataset directories, dumps, credential-like files, and secret-like filenames classify by path string only.
    - Windows paths, mixed separators, quoted paths, relative traversal, and project-root-relative paths normalize consistently.
    - Classifications include category and reason code, not raw protected contents.
    - The classifier never opens, stats, reads, or glob-expands protected paths.
  </behavior>
  <action>
    Create `tests/test_policy.py` first with path strings only. Replace the boolean-only path logic with `PathClassification` and `classify_path(path: str)`, while preserving `is_sensitive_path(path: str) -> bool` as a compatibility wrapper. Add reason categories for env files, protected data directories, dumps, credentials, secret-like filenames, and unknown safe paths. Do not inspect `.env` or `data_sensivel/**` contents.
  </action>
  <verify>
    <automated>python -m pytest tests/test_policy.py -q</automated>
    <automated>python -c "from privguard.policy import classify_path, is_sensitive_path; c=classify_path('safe/example.txt'); assert hasattr(c, 'is_protected'); assert is_sensitive_path('safe/example.txt') is False"</automated>
    <automated>python -c "import pathlib; text=pathlib.Path('privguard/policy.py').read_text(encoding='utf-8'); assert '.read_text(' not in text and '.open(' not in text"</automated>
  </verify>
  <acceptance_criteria>
    DET-05 is implemented with normalized path-string classification and synthetic tests that prove no protected content is read.
  </acceptance_criteria>
  <done>Protected path classification returns sanitized categories/reason codes and keeps compatibility with `is_sensitive_path()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add surface capability and fail-closed decisions</name>
  <files>privguard/policy.py, privguard/diagnostics.py, tests/test_policy.py</files>
  <read_first>
    - `.planning/phases/02-privacy-core/02-CONTEXT.md` decisions D-07, D-08, D-09, D-10
    - `.planning/phases/02-privacy-core/02-RESEARCH.md` MASK-04 and POL-01 through POL-03
    - `privguard/masking.py`
    - `privguard/policy.py`
  </read_first>
  <behavior>
    - Strict/fail-closed mode is default for external-provider workflows.
    - Rewrite-capable surfaces allow only when no sensitive hits exist or masking is verified.
    - Block-only surfaces block when sensitive data is found.
    - Observe-only and unsupported clients are not represented as automatic protection.
    - Unknown, unclassified, and external targets are treated as unsafe by default and block unless verified masked output exists.
    - Incomplete mask verification returns pause or block decision, never allow.
  </behavior>
  <action>
    Add enum-like constants or `Enum` values for `SurfaceCapability`, `PolicyMode`, and `PolicyAction`. Add `PolicyDecision` and `decide_policy(...)` accepting capability, detection hits/report, optional `MaskResult`, and strict/default mode. Encode D-08 through D-10 exactly: unknown/external fail closed, rewrite-capable requires verified masking, block-only blocks on hits, and observe-only/unsupported cannot claim protection. Keep decisions sanitized with reason codes and counts only.
  </action>
  <verify>
    <automated>python -m pytest tests/test_policy.py -q</automated>
    <automated>python -c "from privguard.policy import SurfaceCapability, decide_policy; assert hasattr(SurfaceCapability, 'UNKNOWN') or 'unknown' in str(SurfaceCapability)"</automated>
    <automated>python -m compileall privguard</automated>
  </verify>
  <acceptance_criteria>
    MASK-04, POL-01, POL-02, and POL-03 are implemented in package policy with strict fail-closed defaults and safe handling for incomplete masking.
  </acceptance_criteria>
  <done>Shared policy decisions produce allow/block/pause actions from capability, detection, and verification state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ensure policy diagnostics remain sanitized</name>
  <files>privguard/policy.py, privguard/diagnostics.py, tests/test_policy.py</files>
  <read_first>
    - `.planning/phases/02-privacy-core/02-CONTEXT.md` decisions D-11, D-12, D-13
    - `.planning/phases/02-privacy-core/02-RESEARCH.md` POL-04 and security domain
    - `privguard/diagnostics.py`
    - `privguard/policy.py`
  </read_first>
  <behavior>
    - Policy decisions contain capability, action, counts, reason codes, and sanitized classifications.
    - Policy decisions do not contain raw matched values, original snippets, protected file contents, or full sensitive path strings.
    - Human-readable and JSON diagnostic serializers can render policy decisions without reaching into raw text.
  </behavior>
  <action>
    Add or update policy summary helpers so `privguard.diagnostics` can serialize `PolicyDecision` and `PathClassification` safely. Keep existing `summarize_hits()` and `format_hit_summary()` compatibility if they still live in policy, but ensure they delegate to sanitized metadata and never touch `Hit.value`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_policy.py tests/test_masking.py -q</automated>
    <automated>python -c "from privguard.policy import PolicyDecision, PathClassification; from privguard.diagnostics import to_dict, to_json; decision=PolicyDecision(action='block', allow=False, reason_codes=('sensitive_path',), capability='external'); path=PathClassification(True, 'env_file', 'protected_path_env'); rendered=to_json({'decision': to_dict(decision), 'path': to_dict(path)}); assert 'sensitive_path' in rendered and 'segredo' not in rendered and '529.982.247-25' not in rendered"</automated>
    <automated>python -c "import pathlib; text=pathlib.Path('tests/test_policy.py').read_text(encoding='utf-8'); assert 'data_sensivel/' not in text or 'classify_path' in text"</automated>
  </verify>
  <acceptance_criteria>
    POL-04 is covered for policy/path decisions; all serializable policy state is sanitized and reason-code based.
  </acceptance_criteria>
  <done>Policy and path decisions are safe to serialize and covered by leak-focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> path classifier | Untrusted path strings are normalized and classified without file IO. |
| client adapter -> policy core | Downstream integrations pass capability labels and mask/detection state into shared fail-closed logic. |
| policy core -> diagnostics | Only sanitized decisions and reason codes cross to stdout/JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | `classify_path()` | mitigate | Use string normalization only; tests assert no file reads are introduced. |
| T-02-03-02 | Spoofing | surface capability labels | mitigate | Define explicit labels from D-09 and fail closed for unknown/external. |
| T-02-03-03 | Information Disclosure | `decide_policy()` | mitigate | Block or pause when masking is absent or unverified for unsafe surfaces. |
| T-02-03-04 | Repudiation | diagnostics | mitigate | Include action, capability, counts, and reason codes without raw values. |
</threat_model>

<verification>
Run after all tasks:
```powershell
python -m pytest tests/test_policy.py tests/test_masking.py tests/test_detection.py -q
python -m compileall privguard
```
</verification>

<success_criteria>
- DET-05, MASK-04, POL-01, POL-02, POL-03, and POL-04 are covered.
- Unknown/external/unclassified targets fail closed by default.
- Protected path classification never reads protected files.
- Policy decisions are sanitized and downstream-ready for Phase 3 and Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/02-privacy-core/02-03-SUMMARY.md`.
</output>
