---
phase: 03-claude-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - privguard/hooks.py
  - tests/test_claude_hooks.py
autonomous: true
requirements:
  - CLD-01
  - CLD-04
must_haves:
  truths:
    - "Claude UserPromptSubmit blocks synthetic sensitive prompts by default when rewrite is unavailable."
    - "Prompt hook output is metadata-only and never includes prompt-derived text."
    - "Malformed hook JSON follows the project hook contract without exposing input."
  artifacts:
    - path: "privguard/hooks.py"
      provides: "Claude prompt hook block-only enforcement and sanitized denial formatting"
      contains: "main_user_prompt"
    - path: "tests/test_claude_hooks.py"
      provides: "Synthetic UserPromptSubmit regression tests"
      contains: "UserPromptSubmit"
  key_links:
    - from: "hooks/pii_guard.py"
      to: "privguard/hooks.py"
      via: "main_user_prompt adapter import"
      pattern: "from privguard.hooks import main_user_prompt"
    - from: "privguard/hooks.py"
      to: "privguard.diagnostics"
      via: "metadata-only hit summaries"
      pattern: "format_hit_summary|to_dict|summarize_hits"
---

<objective>
Harden the Claude `UserPromptSubmit` enforcement path so sensitive prompts are blocked by default and hook feedback cannot leak prompt-derived material.

Purpose: Implements D-01, D-02, D-08, and D-09 from Phase 03 context while preserving the thin adapter contract from Phase 01.
Output: Updated prompt hook handler plus synthetic tests proving block behavior, fail-open malformed JSON behavior where required by project conventions, and sanitized stdout/stderr.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-claude-enforcement/03-CONTEXT.md
@.planning/phases/03-claude-enforcement/03-RESEARCH.md
@.planning/phases/03-claude-enforcement/03-PATTERNS.md
@privguard/hooks.py
@privguard/diagnostics.py
@hooks/pii_guard.py
@tests/test_cli.py
@tests/test_policy.py

<interfaces>
From `hooks/pii_guard.py`:
```python
from privguard.hooks import main_user_prompt
```

From `privguard.diagnostics`:
```python
def summarize_hits(hits: Any) -> list[dict[str, object]]
def to_json(value: Any) -> str
def format_hit_summary(hits: Any) -> str
```

From `privguard.hooks`:
```python
def main_user_prompt() -> int
```
</interfaces>
</context>

<source_audit>
GOAL covered: Claude Code prompt hooks enforce strict outbound privacy with sanitized output.
REQ covered: CLD-01, CLD-04.
RESEARCH covered: Plan 03-01 candidate split, metadata-only denial, removal of `redacted=...`, block-only default.
CONTEXT covered: D-01, D-02, D-08, D-09.
Deferred excluded: Codex claims and broader IDE integrations.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prompt hook synthetic sanitation tests</name>
  <files>tests/test_claude_hooks.py</files>
  <read_first>
    - tests/test_cli.py
    - tests/test_policy.py
    - privguard/hooks.py
    - .planning/phases/03-claude-enforcement/03-CONTEXT.md
  </read_first>
  <behavior>
    - Synthetic prompt containing a valid synthetic CPF exits `2` in default mode.
    - Captured stderr/stdout include reason codes, entity category/count/offset metadata, and policy action.
    - Captured stderr/stdout do not include the raw synthetic CPF, a redacted prompt, `<BR_CPF>`, original prompt snippets, or secret-looking substrings.
    - Clean prompt exits `0` with no diagnostic output.
    - Malformed JSON exits `0` per existing project hook fail-open convention and emits no raw input.
    - `PII_GUARD_MODE=warn` or `scrub`, if still accepted, is explicitly labeled non-protective/local-development and never outputs prompt-derived text.
  </behavior>
  <action>Create `tests/test_claude_hooks.py` with pytest helpers that monkeypatch `sys.stdin`, invoke `privguard.hooks.main_user_prompt()`, and capture stdout/stderr. Use only synthetic literals already appropriate for tests; do not read `.env`, dumps, credentials, or `data_sensivel/**`. Tests must fail against the current `redacted=` behavior before implementation.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_hooks.py -q</automated>
  </verify>
  <acceptance_criteria>Tests prove default prompt blocking and output hygiene for CLD-01/CLD-04 without touching protected files.</acceptance_criteria>
  <done>`tests/test_claude_hooks.py` exists, uses synthetic fixtures only, and includes negative assertions for raw values, prompt snippets, and placeholder/redacted prompt output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace prompt-derived hook output with sanitized metadata</name>
  <files>privguard/hooks.py</files>
  <read_first>
    - privguard/hooks.py
    - privguard/diagnostics.py
    - privguard/policy.py
    - tests/test_claude_hooks.py
  </read_first>
  <behavior>
    - `main_user_prompt()` defaults to blocking sensitive prompts on Claude block-only surfaces per D-01.
    - No code path writes `redact(prompt, hits)`, prompt snippets, or raw `Hit.value` to stdout/stderr/JSON.
    - Non-blocking modes are absent or explicitly marked `local_development_non_protective` and still sanitized per D-02.
    - Malformed JSON handling remains consistent with the project hook convention in AGENTS.md.
  </behavior>
  <action>Update `privguard/hooks.py` so prompt denials are generated from metadata-only diagnostics: reason codes, policy action, detection categories/counts/offsets, and concise remediation. Remove the prompt `redact()` usage from hook output entirely. Keep `hooks/pii_guard.py` unchanged as a thin adapter. Use `privguard.diagnostics` helpers instead of hand-serializing `Hit.value` or prompt text.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_hooks.py -q</automated>
  </verify>
  <acceptance_criteria>Sensitive synthetic prompt exits `2`, clean prompt exits `0`, malformed JSON follows the existing fail-open convention, and no prompt-derived text appears in hook outputs.</acceptance_criteria>
  <done>`privguard/hooks.py` no longer includes `redacted=` or masked prompt output in `main_user_prompt()` diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code -> `hooks/pii_guard.py` | Untrusted hook JSON and prompt text enter local enforcement. |
| `privguard.hooks` -> hook stdout/stderr | Diagnostic output may be consumed by Claude/user and must not leak prompt-derived material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | `main_user_prompt()` | mitigate | Block sensitive prompts by default on Claude block-only surfaces and emit only sanitized metadata. |
| T-03-01-02 | Information Disclosure | prompt hook diagnostics | mitigate | Remove `redacted=` and any prompt-derived text from stdout/stderr/JSON; test raw synthetic values and placeholders are absent. |
| T-03-01-03 | Denial of Service | malformed hook input | accept | Preserve existing fail-open convention for malformed JSON with no sensitive echo, matching project hook requirements. |
</threat_model>

<verification>
Run `python -m pytest tests/test_claude_hooks.py -q`.
Run `python -m pytest tests -q` if the local pytest collection configuration already restricts collection to tests.
</verification>

<success_criteria>
CLD-01 and CLD-04 are satisfied for the prompt hook: default block behavior works, output is metadata-only, and tests prove no raw or redacted prompt material is emitted.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-enforcement/03-01-SUMMARY.md`.
</output>
