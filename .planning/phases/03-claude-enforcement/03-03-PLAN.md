---
phase: 03-claude-enforcement
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - privguard/diagnostics.py
  - privguard/cli.py
  - tests/test_claude_doctor.py
autonomous: true
requirements:
  - CLD-04
  - CLD-05
must_haves:
  truths:
    - "Developer can run a Claude diagnostic that validates hook wiring and effective policy with synthetic fixtures only."
    - "Doctor output includes an audit-visible synthetic-data marker."
    - "Doctor output is sanitized and never includes synthetic raw values, protected path strings, or secret-looking substrings."
  artifacts:
    - path: "privguard/cli.py"
      provides: "`privguard claude doctor` command"
      contains: "doctor"
    - path: "privguard/diagnostics.py"
      provides: "Metadata-only doctor serialization support"
      contains: "synthetic_data"
    - path: "tests/test_claude_doctor.py"
      provides: "Synthetic CLI doctor regression tests"
      contains: "synthetic_data"
  key_links:
    - from: "privguard/cli.py"
      to: ".claude/settings.json"
      via: "metadata-only hook wiring inspection"
      pattern: "settings.json"
    - from: "privguard/cli.py"
      to: "privguard.hooks"
      via: "synthetic hook probes or pure helper calls"
      pattern: "main_user_prompt|main_pre_tool|classify"
---

<objective>
Add a safe Claude validation diagnostic so developers can verify hook installation and policy behavior without reading protected files.

Purpose: Implements D-05, D-06, D-07, D-08, and D-09 while preserving the project privacy boundary.
Output: `privguard claude doctor` CLI command, metadata-only diagnostic schema, and synthetic tests.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-claude-enforcement/03-CONTEXT.md
@.planning/phases/03-claude-enforcement/03-RESEARCH.md
@.planning/phases/03-claude-enforcement/03-PATTERNS.md
@.planning/phases/03-claude-enforcement/03-01-SUMMARY.md
@.planning/phases/03-claude-enforcement/03-02-SUMMARY.md
@privguard/cli.py
@privguard/diagnostics.py
@privguard/hooks.py
@privguard/policy.py
@.claude/settings.json
@tests/test_cli.py

<interfaces>
From `privguard.cli`:
```python
def main(argv: list[str] | None = None) -> int
```

Doctor output contract to create:
```json
{
  "command": "claude doctor",
  "synthetic_data": true,
  "checks": [
    {"name": "hook_wiring", "result": "pass", "reason_codes": []}
  ]
}
```
</interfaces>
</context>

<source_audit>
GOAL covered: Developer can validate Claude hook installation and effective policy without protected file reads.
REQ covered: CLD-04, CLD-05.
RESEARCH covered: Plan 03-03 candidate split, synthetic doctor probe, metadata-only diagnostics.
CONTEXT covered: D-05, D-06, D-07, D-08, D-09.
Deferred excluded: Codex compatibility evidence and v2 enterprise telemetry.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Claude doctor CLI tests</name>
  <files>tests/test_claude_doctor.py</files>
  <read_first>
    - tests/test_cli.py
    - privguard/cli.py
    - .planning/phases/03-claude-enforcement/03-CONTEXT.md
    - .claude/settings.json
  </read_first>
  <behavior>
    - `main(["claude", "doctor", "--json"])` returns `0` when hook wiring and synthetic policy probes pass.
    - JSON output includes `synthetic_data: true` at the top level and on synthetic probe checks where useful.
    - Human output includes `synthetic_data=true`.
    - Output reports hook wiring, effective prompt policy, synthetic prompt block, synthetic protected path block, and synthetic command block.
    - Output does not contain raw synthetic CPF/secret values, `.env`, `data_sensivel/synthetic.csv`, prompt snippets, command snippets, protected file contents, or redacted prompt text.
  </behavior>
  <action>Create `tests/test_claude_doctor.py` following existing CLI test patterns. Tests must use only synthetic payload constants inside the test file and assert those constants do not appear in stdout/stderr. Do not read `.env`, dumps, credentials, or `data_sensivel/**`; reading `.claude/settings.json` metadata is allowed.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_doctor.py -q</automated>
  </verify>
  <acceptance_criteria>Tests define the complete CLD-05 doctor behavior and synthetic-data audit marker before implementation.</acceptance_criteria>
  <done>`tests/test_claude_doctor.py` exists and fails until `privguard claude doctor` is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement metadata-only Claude doctor</name>
  <files>privguard/diagnostics.py, privguard/cli.py</files>
  <read_first>
    - privguard/cli.py
    - privguard/diagnostics.py
    - privguard/hooks.py
    - privguard/policy.py
    - .claude/settings.json
    - tests/test_claude_doctor.py
  </read_first>
  <behavior>
    - `privguard claude doctor` and `privacy-guard claude doctor` work through `privguard.cli:main`.
    - Doctor checks hook command wiring in `.claude/settings.json` by metadata only.
    - Doctor runs synthetic prompt/path/command probes through pure package logic or in-process hook calls.
    - Doctor emits `synthetic_data=true` in text mode and `"synthetic_data": true` in JSON mode.
    - Doctor returns non-zero when a required check fails, while still keeping output sanitized.
  </behavior>
  <action>Add a nested `claude doctor` argparse command to `privguard/cli.py`. Add small metadata-only helpers in `privguard/diagnostics.py` if needed for consistent JSON/text rendering. The doctor may inspect `.claude/settings.json` configuration metadata and run synthetic probes, but it must not open or read protected files. Do not print synthetic raw values, synthetic protected path strings, command strings, or redacted prompt text.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_doctor.py tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>Developer can validate Claude hook installation/effective policy with synthetic-only diagnostics and an explicit synthetic-data marker.</acceptance_criteria>
  <done>`privguard claude doctor --json` emits parseable sanitized JSON with `synthetic_data: true`; human mode emits `synthetic_data=true`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer CLI -> `privguard.cli` | CLI arguments and local project metadata enter diagnostic code. |
| `.claude/settings.json` -> doctor | Hook wiring metadata is read; protected file contents must not be read. |
| Synthetic probes -> diagnostic output | Probe data must remain synthetic and omitted from output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | `claude doctor` probes | mitigate | Use synthetic fixtures only and omit raw fixture values/path strings from output. |
| T-03-03-02 | Information Disclosure | config inspection | mitigate | Read only `.claude/settings.json` metadata; never read `.env`, dumps, credentials, or `data_sensivel/**`. |
| T-03-03-03 | Spoofing | diagnostic result | mitigate | Include explicit `synthetic_data=true` / `"synthetic_data": true` marker and named checks. |
</threat_model>

<verification>
Run `python -m pytest tests/test_claude_doctor.py tests/test_cli.py -q`.
</verification>

<success_criteria>
CLD-05 and CLD-04 are satisfied for diagnostics: developers can validate Claude hook policy using synthetic-only metadata, and outputs remain sanitized.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-enforcement/03-03-SUMMARY.md`.
</output>
