---
phase: 03-claude-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - privguard/policy.py
  - privguard/hooks.py
  - .claude/settings.json
  - tests/test_policy_commands.py
  - tests/test_claude_hooks.py
autonomous: true
requirements:
  - CLD-02
  - CLD-03
  - CLD-04
must_haves:
  truths:
    - "Claude PreToolUse blocks reads, searches, edits, writes, and shell commands that reference protected paths."
    - "Claude PreToolUse blocks command exfiltration patterns with protected paths, network tools, archive tools, encoding tools, and clipboard commands."
    - "Tool hook denials are sanitized and never include protected path strings or command snippets."
  artifacts:
    - path: "privguard/policy.py"
      provides: "Protected command category classification without file reads"
      contains: "classify_command"
    - path: "privguard/hooks.py"
      provides: "Claude PreToolUse orchestration for path, command, and unknown tool handling"
      contains: "main_pre_tool"
    - path: ".claude/settings.json"
      provides: "Claude PreToolUse matcher coverage"
      contains: "pre_tool_guard.py"
    - path: "tests/test_policy_commands.py"
      provides: "Synthetic command category regression tests"
      contains: "synthetic"
    - path: "tests/test_claude_hooks.py"
      provides: "Synthetic PreToolUse hook regression tests"
      contains: "PreToolUse"
  key_links:
    - from: "hooks/pre_tool_guard.py"
      to: "privguard/hooks.py"
      via: "main_pre_tool adapter import"
      pattern: "from privguard.hooks import main_pre_tool"
    - from: "privguard/hooks.py"
      to: "privguard/policy.py"
      via: "path and command policy helpers"
      pattern: "classify_path|classify_command|is_sensitive_path"
    - from: ".claude/settings.json"
      to: "hooks/pre_tool_guard.py"
      via: "PreToolUse command hook"
      pattern: "pre_tool_guard.py"
---

<objective>
Expand Claude `PreToolUse` enforcement so protected paths and exfiltration-style commands are denied before any protected content can be read, copied, archived, encoded, placed on the clipboard, or sent over a network.

Purpose: Implements D-03, D-04, D-08, and D-09 using Phase 2 path classification and sanitized diagnostics.
Output: Command classification helpers, hardened PreToolUse dispatcher, broader Claude matcher configuration, and synthetic tests.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-claude-enforcement/03-CONTEXT.md
@.planning/phases/03-claude-enforcement/03-RESEARCH.md
@.planning/phases/03-claude-enforcement/03-PATTERNS.md
@.planning/phases/03-claude-enforcement/03-01-SUMMARY.md
@privguard/policy.py
@privguard/hooks.py
@hooks/pre_tool_guard.py
@.claude/settings.json
@tests/test_policy.py
@tests/test_claude_hooks.py

<interfaces>
From `privguard.policy`:
```python
def classify_path(path: str) -> PathClassification
def is_sensitive_path(path: str) -> bool
def decide_policy(...) -> PolicyDecision
```

New contract to create in `privguard.policy`:
```python
@dataclass(frozen=True)
class CommandClassification:
    is_blocked: bool
    category: str
    reason_code: str

def classify_command(command: str) -> CommandClassification
```

From `privguard.hooks`:
```python
def main_pre_tool() -> int
```
</interfaces>
</context>

<source_audit>
GOAL covered: Claude Code tool hooks enforce strict protected-path and exfiltration blocking with sanitized output.
REQ covered: CLD-02, CLD-03, CLD-04.
RESEARCH covered: Plan 03-02 candidate split, command category expansion, matcher broadening, path-only validation.
CONTEXT covered: D-03, D-04, D-08, D-09.
Deferred excluded: Codex support, local proxy, and other IDE integrations.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command category and PreToolUse tests</name>
  <files>tests/test_policy_commands.py, tests/test_claude_hooks.py</files>
  <read_first>
    - tests/test_policy.py
    - tests/test_claude_hooks.py
    - privguard/policy.py
    - privguard/hooks.py
    - .planning/phases/03-claude-enforcement/03-CONTEXT.md
  </read_first>
  <behavior>
    - Pure command classification blocks read, copy, archive, encoding/decoding, clipboard, and network-style commands referencing synthetic protected path strings.
    - Cases include PowerShell forms such as `Get-Content`, `Copy-Item`, `Compress-Archive`, `certutil -encode`, `Set-Clipboard`, `Invoke-WebRequest`, and common shell forms such as `cat`, `cp`, `tar`, `zip`, `base64`, `curl`, `wget`, `nc`.
    - PreToolUse blocks `Read`, `Grep`, `Glob`, `Edit`, `Write`, `MultiEdit`, `NotebookEdit`, and shell-capable tools when protected path references are present.
    - Outputs contain reason code/category/count/action metadata but not `.env`, `data_sensivel/synthetic.csv`, command snippets, raw synthetic PII, or prompt/path snippets.
  </behavior>
  <action>Create `tests/test_policy_commands.py` for pure policy command classification and extend `tests/test_claude_hooks.py` with PreToolUse payload tests. Use synthetic path strings only; do not create, glob, open, or read those paths. Tests should fail until command classification and tool dispatch are implemented.</action>
  <verify>
    <automated>python -m pytest tests/test_policy_commands.py tests/test_claude_hooks.py -q</automated>
  </verify>
  <acceptance_criteria>Tests cover every strict command category from D-03 and prove sanitized hook output for CLD-02/CLD-03/CLD-04.</acceptance_criteria>
  <done>`tests/test_policy_commands.py` exists and `tests/test_claude_hooks.py` covers protected path/tool denials with synthetic fixtures only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement protected command classification and PreToolUse dispatch</name>
  <files>privguard/policy.py, privguard/hooks.py</files>
  <read_first>
    - privguard/policy.py
    - privguard/hooks.py
    - privguard/diagnostics.py
    - tests/test_policy_commands.py
    - tests/test_claude_hooks.py
  </read_first>
  <behavior>
    - `privguard.policy.classify_command()` performs string-only classification and never reads files.
    - `privguard.hooks.main_pre_tool()` uses policy helpers for path and command decisions while keeping `hooks/pre_tool_guard.py` thin.
    - Tool denials are metadata-only and do not echo raw protected path strings or command text.
    - Unknown or future file/network-capable tools are treated conservatively according to strict fail-closed privacy requirements.
  </behavior>
  <action>Add `CommandClassification` and `classify_command()` to `privguard/policy.py` beside existing path policy helpers. Cover protected path tokens, quoted paths, Windows separators, relative traversal, archive/copy/encoding/clipboard/network categories, and inline PII in command text. Refactor `privguard/hooks.py` PreToolUse handling to call these helpers for `Bash` and `PowerShell`, support `MultiEdit` and notebook path fields, and return sanitized denial output.</action>
  <verify>
    <automated>python -m pytest tests/test_policy_commands.py tests/test_claude_hooks.py tests/test_policy.py -q</automated>
  </verify>
  <acceptance_criteria>Protected path and command exfiltration patterns are blocked before execution, with no protected path contents or path strings emitted.</acceptance_criteria>
  <done>`classify_command()` exists, PreToolUse tests pass, and `privguard/policy.py` still has no `.read_text(` or `.open(` usage.</done>
</task>

<task type="auto">
  <name>Task 3: Broaden Claude PreToolUse matcher safely</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json
    - .planning/phases/03-claude-enforcement/03-RESEARCH.md
    - tests/test_claude_hooks.py
  </read_first>
  <action>Update `.claude/settings.json` so `PreToolUse` invokes `hooks/pre_tool_guard.py` for the broad Claude tool surface. Prefer matcher `"*"` for strict privacy per research; if retaining an explicit matcher, include `Read|Bash|Grep|Glob|Edit|Write|MultiEdit|NotebookEdit|NotebookRead|PowerShell` and document the reason in the plan summary. Preserve the existing `CLAUDE_PROJECT_DIR` command shape.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_hooks.py -q</automated>
  </verify>
  <acceptance_criteria>Claude hook configuration covers current and likely file/tool exfiltration surfaces without changing hook adapter paths.</acceptance_criteria>
  <done>`.claude/settings.json` routes broadened PreToolUse events to `hooks/pre_tool_guard.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code -> `hooks/pre_tool_guard.py` | Untrusted tool names and tool input arrive before local command/file execution. |
| Shell command string -> policy classifier | User/agent-supplied command text may attempt exfiltration. |
| Hook diagnostics -> Claude/user | Denial output must not reveal protected paths, command snippets, or raw values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | path tools | mitigate | Classify protected path strings before `Read`, `Grep`, `Glob`, `Edit`, `Write`, `MultiEdit`, and notebook tools execute. |
| T-03-02-02 | Information Disclosure | shell commands | mitigate | Block read/copy/archive/encoding/clipboard/network command categories when protected path strings are present. |
| T-03-02-03 | Tampering | edit/write tools | mitigate | Deny edits/writes involving protected paths before tool execution. |
| T-03-02-04 | Information Disclosure | denial output | mitigate | Emit reason/category metadata only; tests assert path strings and command snippets are absent. |
</threat_model>

<verification>
Run `python -m pytest tests/test_policy_commands.py tests/test_claude_hooks.py tests/test_policy.py -q`.
</verification>

<success_criteria>
CLD-02, CLD-03, and CLD-04 are satisfied for PreToolUse: protected paths and exfiltration commands block strictly, without reading protected files or leaking protected strings in output.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-enforcement/03-02-SUMMARY.md`.
</output>
