---
phase: 03-claude-enforcement
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - pyproject.toml
  - tests/test_claude_phase_gate.py
autonomous: true
requirements:
  - CLD-01
  - CLD-02
  - CLD-03
  - CLD-04
  - CLD-05
must_haves:
  truths:
    - "The full Phase 03 Claude enforcement surface passes as one synthetic-only regression gate."
    - "Pytest collection can be run with `python -m pytest tests` without touching inaccessible cache directories."
    - "No Phase 03 hook or doctor output leaks raw synthetic values, protected path strings, prompt snippets, command snippets, or redacted prompt text."
  artifacts:
    - path: "pyproject.toml"
      provides: "Pytest collection hygiene if needed"
      contains: "tool.pytest.ini_options"
    - path: "tests/test_claude_phase_gate.py"
      provides: "Cross-surface output hygiene gate"
      contains: "CLD"
  key_links:
    - from: "tests/test_claude_phase_gate.py"
      to: "privguard.hooks"
      via: "prompt and tool hook calls"
      pattern: "main_user_prompt|main_pre_tool"
    - from: "tests/test_claude_phase_gate.py"
      to: "privguard.cli"
      via: "doctor command call"
      pattern: "claude.*doctor"
---

<objective>
Add the Phase 03 regression gate that runs all Claude enforcement checks together and locks collection/output hygiene for downstream execution.

Purpose: Provides final CLD-01 through CLD-05 coverage across prompt hook, tool hook, command policy, doctor diagnostics, and sanitized output.
Output: Optional pytest collection configuration and an end-to-end synthetic phase gate test.
</objective>

<execution_context>
@C:/Users/Erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-claude-enforcement/03-CONTEXT.md
@.planning/phases/03-claude-enforcement/03-RESEARCH.md
@.planning/phases/03-claude-enforcement/03-PATTERNS.md
@.planning/phases/03-claude-enforcement/03-01-SUMMARY.md
@.planning/phases/03-claude-enforcement/03-02-SUMMARY.md
@.planning/phases/03-claude-enforcement/03-03-SUMMARY.md
@pyproject.toml
@tests/test_claude_hooks.py
@tests/test_policy_commands.py
@tests/test_claude_doctor.py
</context>

<source_audit>
GOAL covered: All Claude Code prompt/tool/diagnostic surfaces enforce strict privacy with sanitized outputs.
REQ covered: CLD-01, CLD-02, CLD-03, CLD-04, CLD-05.
RESEARCH covered: Plan 03-04 candidate split, pytest collection hygiene, no raw synthetic values in outputs.
CONTEXT covered: D-01 through D-09 through cross-surface verification.
Deferred excluded: Codex compatibility evidence, additional IDE agents, local proxy, and enterprise telemetry.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest collection hygiene only if needed</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/03-claude-enforcement/03-RESEARCH.md
  </read_first>
  <action>Run `python -m pytest tests --collect-only -q`. If collection is already clean, leave `pyproject.toml` unchanged and record that in the summary. If root collection hygiene is needed for reliable local use, add `[tool.pytest.ini_options]` with `testpaths = ["tests"]` and any necessary norecursedirs for local cache directories. Do not change runtime dependencies.</action>
  <verify>
    <automated>python -m pytest tests --collect-only -q</automated>
  </verify>
  <acceptance_criteria>`python -m pytest tests` is the preferred reliable verification command and collection does not inspect protected data directories.</acceptance_criteria>
  <done>Pytest collection under `tests/` succeeds; `pyproject.toml` changes are limited to pytest collection config if required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-surface Phase 03 output hygiene gate</name>
  <files>tests/test_claude_phase_gate.py</files>
  <read_first>
    - tests/test_claude_hooks.py
    - tests/test_policy_commands.py
    - tests/test_claude_doctor.py
    - privguard/hooks.py
    - privguard/cli.py
  </read_first>
  <behavior>
    - Prompt hook blocks a synthetic sensitive prompt and emits no raw fixture or redacted prompt text.
    - Tool hook blocks a synthetic protected path access and command exfiltration pattern and emits no protected path or command snippet.
    - `privguard claude doctor --json` reports `synthetic_data: true`.
    - A shared forbidden-output assertion covers raw synthetic CPF, fake secret-like strings, `.env`, `data_sensivel/synthetic.csv`, command snippets, prompt snippets, and placeholder/redacted prompt text.
  </behavior>
  <action>Create `tests/test_claude_phase_gate.py` as a focused integration-style pytest module. Reuse helper patterns from the Phase 03 tests where appropriate. Keep fixtures synthetic and do not read protected files. This test should assert the final observable truths for CLD-01 through CLD-05 in one place.</action>
  <verify>
    <automated>python -m pytest tests/test_claude_phase_gate.py -q</automated>
  </verify>
  <acceptance_criteria>One phase gate proves all Claude enforcement surfaces meet strict blocking and sanitized-output requirements.</acceptance_criteria>
  <done>`tests/test_claude_phase_gate.py` passes and covers prompt, tool, command, and doctor outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full synthetic Claude enforcement suite</name>
  <files>tests/test_claude_phase_gate.py, pyproject.toml</files>
  <read_first>
    - tests/test_claude_hooks.py
    - tests/test_policy_commands.py
    - tests/test_claude_doctor.py
    - tests/test_claude_phase_gate.py
    - pyproject.toml
  </read_first>
  <action>Run the full test suite using the safe command `python -m pytest tests -q`. If failures are from Phase 03 logic, fix the owning module/test in the smallest relevant file. If failures are unrelated pre-existing issues, record them in the summary without weakening Phase 03 assertions.</action>
  <verify>
    <automated>python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>All tests under `tests/` pass with synthetic fixtures only, or unrelated pre-existing failures are clearly documented with Phase 03 focused tests passing.</acceptance_criteria>
  <done>`python -m pytest tests -q` result is recorded in the plan summary and Phase 03 focused tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> hook/CLI outputs | Synthetic sensitive-looking data enters enforcement and must not be echoed. |
| Test runner -> repository tree | Collection must stay within safe test paths and avoid protected data reads. |
| Phase gate -> release confidence | Missing coverage could allow a privacy regression through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | phase regression tests | mitigate | Use synthetic-only fixtures and assert forbidden values/path strings never appear in outputs. |
| T-03-04-02 | Information Disclosure | pytest collection | mitigate | Prefer `python -m pytest tests`; add collection config only if needed to avoid unsafe/inaccessible root collection. |
| T-03-04-03 | Repudiation | phase verification | mitigate | Add a single phase gate covering CLD-01 through CLD-05 and record test command results in the summary. |
</threat_model>

<verification>
Run `python -m pytest tests -q`.
</verification>

<success_criteria>
All CLD-01 through CLD-05 requirements have automated synthetic coverage, and Phase 03 can be validated with one safe pytest command.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-enforcement/03-04-SUMMARY.md`.
</output>
