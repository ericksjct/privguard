---
phase: 999.3-masking-gaps-cnpj-email-pix
verified: 2026-05-21T23:55:00Z
status: passed
score: 6/6
overrides_applied: 0
---

# Phase 999.3: Masking Gaps — CNPJ/Email/PIX Verification Report

**Phase Goal:** Fix 3 regex detection gaps (BR_RG/CNPJ collision, BR_PHONE +55 no-space, BR_PLACA_OLD false positive on transaction IDs) and add regression tests
**Verified:** 2026-05-21T23:55:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `12.345.678/0001-90` does not generate BR_RG hit | VERIFIED | Line 177: `r"\b\d{1,2}\.\d{3}\.\d{3}-[\dXx]\b"` — suffix mandatory; `test_rg_does_not_match_cnpj_prefix` PASSED |
| 2 | `+5516988887777` generates BR_PHONE hit | VERIFIED | Line 178: `r"(?:\+55\s?)?\(?\d{2}\)?\s?..."` — `\b` removed before DDD; `test_phone_detected_without_space_after_country_code` PASSED |
| 3 | `TXN-2026-0315-887766` does not generate BR_PLACA_OLD hit | VERIFIED | Line 181: `r"\b[A-Z]{3}-?\d{4}(?![-\d])\b"` — negative lookahead added; `test_transaction_id_not_classified_as_plate` PASSED |
| 4 | `ABC-1234` (real plate) still generates BR_PLACA_OLD | VERIFIED | Lookahead does not block end-of-string/word; `test_real_plate_still_detected` PASSED |
| 5 | `12.345.678-9` (real RG) still generates BR_RG | VERIFIED | Pattern requires `-[\dXx]` suffix — matches `12.345.678-9`; `test_rg_still_detected_with_suffix` PASSED |
| 6 | `uv run pytest -q` passes 141+ passed, 1 skipped | VERIFIED | Actual result: 146 passed, 1 skipped (exceeds the 141+ threshold from plan) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `privguard/detection.py` | Regex fixes applied at lines 177, 178, 181 | VERIFIED | All three patterns match plan spec exactly |
| `tests/test_detection.py` | 5 regression tests added | VERIFIED | Lines 161-198: 5 new test functions present and passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `test_rg_does_not_match_cnpj_prefix` | `BR_RG` pattern line 177 | `detect()` call | WIRED | Test imports and calls `detect`; assertion on `h.kind` |
| `test_phone_detected_without_space_after_country_code` | `BR_PHONE` pattern line 178 | `detect()` call | WIRED | Test imports and calls `detect`; assertion on `h.kind` |
| `test_transaction_id_not_classified_as_plate` | `BR_PLACA_OLD` pattern line 181 | `detect()` call | WIRED | Test imports and calls `detect`; assertion on `h.kind` |
| `test_real_plate_still_detected` | `BR_PLACA_OLD` pattern line 181 | `detect()` call | WIRED | Regression guard for positive case |
| `test_rg_still_detected_with_suffix` | `BR_RG` pattern line 177 | `detect()` call | WIRED | Regression guard for positive case |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces no components that render dynamic data. All artifacts are pure detection logic and unit tests with no UI or data-fetching layers.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 19 detection tests pass | `uv run pytest tests/test_detection.py -v` | 19 passed in 0.14s | PASS |
| Full suite: 141+ passed, 1 skipped | `uv run pytest -q` | 146 passed, 1 skipped in 3.74s | PASS |
| Commits exist in git history | `git log --oneline -5` | `04d8027` (regex fixes), `c83c4d5` (regression tests) | PASS |

### Requirements Coverage

No requirement IDs declared in plan frontmatter (`requirements: []`). Phase is a backlog bug-fix item without formal requirement bindings.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/test_detection.py` | 163, 171, 180, 186, 194 | Redundant inline `from privguard.detection import detect` in each regression test (module-level import already present) | Info | Inconsistency with file style; no behavioral impact |

The inline import pattern was flagged in the code review (REVIEW.md IN-01) as a style inconsistency. It does not affect test behavior or goal achievement.

### Human Verification Required

None. All behavioral checks were verified programmatically:
- Regex pattern text verified directly against source (exact pattern matching)
- All regression tests exercised via `pytest` with confirmed PASSED status
- Git commits confirmed present in history
- Full test suite confirmed passing

### Gaps Summary

No gaps. All 6 must-haves are verified by direct source inspection and passing test execution.

**Regex patterns confirmed in `privguard/detection.py`:**

- BR_RG (line 177): `r"\b\d{1,2}\.\d{3}\.\d{3}-[\dXx]\b"` — matches plan spec exactly (suffix no longer optional)
- BR_PHONE (line 178): `r"(?:\+55\s?)?\(?\d{2}\)?\s?(?:9\d{4}|[2-5]\d{3})-?\d{4}\b"` — matches plan spec exactly (`\b` before DDD removed)
- BR_PLACA_OLD (line 181): `r"\b[A-Z]{3}-?\d{4}(?![-\d])\b"` — matches plan spec exactly (negative lookahead added)

**Regression tests confirmed in `tests/test_detection.py` (lines 161-198):**
- `test_rg_does_not_match_cnpj_prefix`
- `test_rg_still_detected_with_suffix`
- `test_phone_detected_without_space_after_country_code`
- `test_transaction_id_not_classified_as_plate`
- `test_real_plate_still_detected`

**Note from REVIEW.md:** Two warnings were identified during code review (WR-01: test uses invalid-checksum CNPJ so the assertion passes even without the RG fix present in isolation; WR-02: BR_PHONE pattern has no left-side word boundary when +55 prefix is absent). These are correctness nuances — they do not block the goals of this phase but may warrant follow-up in a future detection hardening phase.

---

_Verified: 2026-05-21T23:55:00Z_
_Verifier: Claude (gsd-verifier)_
