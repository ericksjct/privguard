# Phase 999.5: Detection Hardening v2 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 999.5-detection-hardening-v2
**Areas discussed:** CNPJ leniency flag design, IBAN false positive fix, Name detection data bundling, Barcode false positive scope

---

## CNPJ Leniency Flag Design

### Flag scope

| Option | Description | Selected |
|--------|-------------|----------|
| Shared flag (PII_GUARD_LENIENT covers both) | One toggle activates leniency for CPF + CNPJ | ✓ |
| Separate flag (PII_GUARD_LENIENT_CNPJ=true) | Fine-grained control per identifier type | |
| Both flags work | Shared OR dedicated | |

**User's choice:** Shared flag — `PII_GUARD_LENIENT` covers both CPF and CNPJ.

### Bare 14-digit in lenient mode

| Option | Description | Selected |
|--------|-------------|----------|
| Same as CPF — bare 14-digit stays strict | Only formatted CNPJs get lenient treatment | ✓ |
| Bare 14-digit also goes lenient | Any 14-digit run treated as potential CNPJ | |

**User's choice:** Stays strict — mirrors CPF decision, avoids FP overlap.

---

## IBAN False Positive Fix

### Fix approach

| Option | Description | Selected |
|--------|-------------|----------|
| Extend IBAN regex to match space-separated format | New variant consumes full span before BR_PHONE runs | ✓ |
| Add a guard on BR_PHONE | Negative lookbehind for 2-letter country code prefix | |

**User's choice:** Extend IBAN regex.

### Regex strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Strict 4-char groups (CC99 XXXX XXXX...) | Canonical IBAN print format | ✓ |
| Permissive (any whitespace within length bounds) | More flexible, more FP risk | |

**User's choice:** Strict 4-char groups.

---

## Name Detection Data Bundling

### Data location

| Option | Description | Selected |
|--------|-------------|----------|
| Hardcoded frozenset in detection.py | Zero install friction, pure stdlib | |
| Static data file inside package (privguard/data/) | Slim detection.py, needs package_data wiring | ✓ |
| Lazy download from IBGE API on first use | Always current but breaks offline, incompatible with fail-closed | |

**User's choice:** Static data file shipped with the package.

### Flag naming

| Option | Description | Selected |
|--------|-------------|----------|
| PII_GUARD_DETECT_NAMES / --detect-names (exact) | Matches backlog spec | ✓ |
| Different naming convention | Specify alternative | |

**User's choice:** Exact backlog spec names.

### Scoring

| Option | Description | Selected |
|--------|-------------|----------|
| Full tiered scoring (0.58 / 0.65 / 0.72) | Implements backlog spec exactly | ✓ |
| Flat score for now, refine later | Simpler but noisier | |

**User's choice:** Full tiered scoring as specified.

---

## Barcode False Positive Scope

### Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Add a BR_BOLETO pattern that wins by priority | High-score pattern consumes full barcode span | ✓ |
| Add digit-density lookbehind/lookahead guards | Complex to tune | |
| Defer — log as future backlog item | Skip in this phase | |

**User's choice:** Add `BR_BOLETO` pattern.

### Format coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Both formats (raw digits AND formatted boleto line) | Comprehensive coverage | ✓ |
| Raw digits only (44-47 digit run) | Simpler, add formatted later | |

**User's choice:** Both formats — raw 44-47 digit run and formatted boleto line.

---

## Claude's Discretion

- Exact score value for `BR_BOLETO` (floor is ≥ 0.90)
- Exact filename/format for names data file
- Whether `--detect-names` applies to `scan` only or also `mask` and `policy-check`
- IBGE data sourcing method (static snapshot vs. SIDRA API call at dev time)

## Deferred Ideas

None — discussion stayed within phase scope.
