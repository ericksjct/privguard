---
phase: 999.5-detection-hardening-v2
verified: 2026-05-24T15:30:00Z
status: passed
score: 28/28
overrides_applied: 0
---

# Phase 999.5: Detection Hardening v2 — Verification Report

**Phase Goal:** Harden detection for 5 real-world gaps — CEP dot-variant, CNPJ leniency (formatted-only), spaced IBAN false positive, opt-in Brazilian name detection, and BR_BOLETO barcode false-positive suppression.
**Verified:** 2026-05-24T15:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

**Plan 01 — Name Data Files and Package Infrastructure**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `privguard/data/names_first.txt` exists with at least 40 lowercase first names | VERIFIED | 57 non-comment names confirmed |
| 2 | `privguard/data/names_surnames.txt` exists with at least 25 lowercase surnames | VERIFIED | 25 non-comment names confirmed |
| 3 | `pyproject.toml` has `[tool.setuptools.package-data]` with `privguard = ["data/*"]` | VERIFIED | `tomllib` parse confirmed |
| 4 | Both data files are UTF-8, one name per line, lowercase, with #-prefixed comment lines | VERIFIED | File content inspection confirmed |
| 5 | `importlib.resources.files("privguard") / "data" / "names_first.txt"` resolves | VERIFIED | Runtime import resolves without error |

**Plan 02 — Detection PATTERNS Fixes**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 6 | `detect('14.025-580')` returns a BR_CEP hit (dot-variant CEP) | VERIFIED | `test_cep_dot_variant_detected` PASSES |
| 7 | `detect('01310-200')` still returns BR_CEP (standard format regression) | VERIFIED | `test_cep_standard_still_detected` PASSES |
| 8 | `detect('01310200')` still returns BR_CEP (bare format regression) | VERIFIED | `test_cep_bare_format_regression` PASSES |
| 9 | `detect('11.222.333/0001-00', lenient=True)` returns BR_CNPJ hit | VERIFIED | `test_lenient_cnpj_formatted_detected` PASSES |
| 10 | `detect('11222333000100', lenient=True)` returns NO BR_CNPJ hit (D-03) | VERIFIED | `test_lenient_cnpj_bare_stays_strict` PASSES |
| 11 | `detect('DE89 3704 0044 0532 0130 00')` returns IBAN, not BR_PHONE | VERIFIED | `test_iban_spaced_detected_not_phone` PASSES |
| 12 | `detect('836600...3400')` returns BR_BOLETO (raw 50-digit barcode) | VERIFIED | `test_boleto_raw_detected` PASSES |
| 13 | BR_BOLETO score is 0.92 (> BR_PIS_PASEP 0.91, > BR_PHONE 0.76) | VERIFIED | `test_boleto_score_beats_pis_and_phone` PASSES |

**Plan 03 — Brazilian Name Detection**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 14 | `detect('Maria Silva', detect_names=True)` returns BR_NAME score=0.72, reason='name_fullname' | VERIFIED | Runtime check and `test_name_detection_first_plus_surname` PASSES |
| 15 | `detect('Maria Silva')` with no flag returns NO BR_NAME hit (opt-in, off by default) | VERIFIED | Runtime check and `test_name_detection_off_by_default` PASSES |
| 16 | `PII_GUARD_DETECT_NAMES=true` activates name detection without explicit flag | VERIFIED | `test_name_detection_env_var` PASSES |
| 17 | `detect('Maria foi ao mercado', detect_names=True)` returns BR_NAME score=0.58, reason='name_first' | VERIFIED | `test_name_detection_first_only_score` PASSES — note: name hits bypass min_score filter (documented deviation) |
| 18 | `detect('Silva compareceu', detect_names=True)` returns BR_NAME score=0.65, reason='name_surname' | VERIFIED | `test_name_detection_surname_only_score` PASSES |
| 19 | `privguard scan --detect-names 'Maria Silva'` emits BR_NAME hit via CLI | VERIFIED | CLI exits 0 and prints `detections=1 counts={'BR_NAME': 1}` |
| 20 | `privguard mask --detect-names 'Maria Silva'` masks to `<BR_NAME>` | VERIFIED | CLI exits 0 and prints `<BR_NAME>` |
| 21 | `analyze_text()` accepts and forwards `detect_names` kwarg to `detect()` | VERIFIED | Runtime check and test confirm |
| 22 | `mask_text()` accepts and forwards `detect_names` kwarg to `detect()` | VERIFIED | Code inspection and runtime check confirm |

**Plan 04 — Regression Tests**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 23 | 14 required test function names exist in `tests/test_detection.py` | VERIFIED | All 14 names found via file parse |
| 24 | All 14 new tests pass (pytest exits 0) | VERIFIED | 69 total tests, 0 failures |
| 25 | All previously existing 46+ tests still pass (no regressions) | VERIFIED | 69 total pass (includes all prior tests) |
| 26 | Test names exactly match Plan 04 specification | VERIFIED | All 14 names confirmed present |
| 27 | Env-var tests use try/finally isolation | VERIFIED | `test_name_detection_env_var` and `test_lenient_cnpj_*` use try/finally or `os.environ.pop` |
| 28 | No test reads .env, data_sensivel/, or real sensitive files | VERIFIED | All test values are synthetic (public IBGE data, invalid checksums, public IBAN examples) |

**Score:** 28/28 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `privguard/data/names_first.txt` | IBGE first names (>=40) | VERIFIED | 57 non-comment entries |
| `privguard/data/names_surnames.txt` | Brazilian surnames (>=25) | VERIFIED | 25 non-comment entries |
| `pyproject.toml` | `[tool.setuptools.package-data]` | VERIFIED | `privguard = ["data/*"]` present |
| `privguard/detection.py` | Updated PATTERNS + `_LENIENT_KINDS` + name detection | VERIFIED | All patterns, constants, and functions present |
| `privguard/masking.py` | `detect_names` kwarg passthrough | VERIFIED | Param and forwarding confirmed |
| `privguard/cli.py` | `--detect-names` flag on scan/mask/policy-check | VERIFIED | All three subparsers and handlers confirmed |
| `tests/test_detection.py` | 14 new test functions | VERIFIED | All 14 names present, all pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `privguard/data/names_first.txt` | `_load_name_sets()` in detection.py | `importlib.resources.files('privguard') / 'data'` | VERIFIED | `_importlib_files` import and `_NAMES_DATA` path variable present |
| `_find_name_hits()` | `detect()` PATTERNS loop | `raw.extend(_find_name_hits(text))` when `use_detect_names` is True | VERIFIED | Code at detection.py line 316-317 |
| `cli.py --detect-names` | `cmd_scan / cmd_mask / cmd_policy_check` | `detect_names = True if args.detect_names else None` | VERIFIED | All three handlers confirmed in cli.py |
| `_LENIENT_KINDS frozenset` | `detect()` leniency guard | `entry.kind in _LENIENT_KINDS` | VERIFIED | `BR_CNPJ` in `_LENIENT_KINDS` frozenset at line 280 |
| `BR_BOLETO PatternEntry score 0.92` | overlap sort | `raw.sort(key=lambda h: (-h.score, ...))` | VERIFIED | Score 0.92 beats PIS (0.91) and PHONE (0.76); test confirms suppression |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `_load_name_sets()` | `first_names, surnames` frozensets | `privguard/data/names_first.txt`, `names_surnames.txt` via importlib.resources | Yes — reads committed data files at import time | FLOWING |
| `detect()` name hits | `raw` list extended by `_find_name_hits(text)` | tokenization of live `text` argument | Yes — live text input | FLOWING |
| `mask_text()` | `selected_hits` | `detect()` with `detect_names=detect_names` | Yes — forwarded live | FLOWING |
| CLI `--detect-names` | `detect_names` kwarg | `True if args.detect_names else None` | Yes — user flag | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CLI scan `--detect-names` exits 0 | `main(['scan', '--detect-names', 'Maria Silva'])` | `detections=1 counts={'BR_NAME': 1}`, exit 0 | PASS |
| CLI mask `--detect-names` outputs `<BR_NAME>` | `main(['mask', '--detect-names', 'Maria Silva'])` | `<BR_NAME>`, exit 0 | PASS |
| Full test suite passes | `uv run pytest tests/test_detection.py -x -q` | `69 passed in 0.14s` | PASS |
| Full project suite passes | `uv run pytest tests/ -x -q` | `199 passed, 1 skipped in 0.40s` | PASS |
| 18 phase-specific tests pass | `pytest -k "cep_dot or lenient_cnpj or iban_spaced or boleto or name_detection"` | 18 selected, 18 passed | PASS |
| All 14 Plan 04 canonical names present | file parse | All 14 FOUND | PASS |

### Requirements Coverage

Phase 999.5 plans declare no requirement IDs (`requirements: []`). These are backlog fixes to existing detection capabilities, not new v1 requirements. No REQUIREMENTS.md items are orphaned to this phase. The fixes harden existing DET-03 (CEP, CNPJ, IBAN detection) and extend it with new BR_BOLETO and BR_NAME capabilities.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| privguard/masking.py | 22 | `PLACEHOLDER_ASSIGNMENT` variable name contains "PLACEHOLDER" | Info | False alarm — this is a legitimate regex variable name for detecting safe residual placeholder values, not a stub |

No blocking anti-patterns found.

### Human Verification Required

None. All phase behaviors are verifiable programmatically via the test suite and runtime spot-checks.

### Gaps Summary

No gaps. All 28 must-have truths across Plans 01, 02, 03, and 04 are verified. The test suite passes completely (69 detection tests, 199 total project tests). All artifacts exist and are substantively implemented and fully wired.

**One documented deviation (non-blocking):** Plan 03 noted that `name_first` hits (score 0.58) are inserted after the `min_score >= 0.6` filter rather than before, so they always appear when `detect_names=True` regardless of `min_score`. This is intentional and correct behavior — all must-have behaviors verified against this implementation.

**One documented plan correction (non-blocking):** Plan 02 extended the raw boleto digit range from `{44,47}` to `{44,50}` because the plan's own verification script used a 50-digit test fixture. This is documented in the Plan 02 SUMMARY as a conscious fix to an inconsistency between the plan spec and its own test corpus.

---

_Verified: 2026-05-24T15:30:00Z_
_Verifier: Claude (gsd-verifier)_
