---
phase: 06-milestone-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md
  - .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md
autonomous: true
requirements:
  - CDX-01
  - CDX-02
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
requirements_addressed:
  - CDX-01
  - CDX-02
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
tags: [docs, summary-frontmatter, milestone-cleanup, traceability]
must_haves:
  truths:
    - "04-01-SUMMARY.md frontmatter declares requirements_completed: [CDX-01, CDX-02]"
    - "05-01-SUMMARY.md frontmatter declares requirements_completed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]"
    - "Frontmatter format mirrors the existing 04-02-SUMMARY.md pattern"
  artifacts:
    - path: ".planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md"
      provides: "CDX-01, CDX-02 traceability in frontmatter so future audits run on the matrix alone"
      contains: "requirements_completed: [CDX-01, CDX-02]"
    - path: ".planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md"
      provides: "TEST-01..06 traceability in frontmatter so future audits run on the matrix alone"
      contains: "requirements_completed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]"
  key_links:
    - from: ".planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md frontmatter"
      to: ".planning/v1.0-MILESTONE-AUDIT.md `Requirements Coverage` table CDX-01/CDX-02 rows"
      via: "requirements_completed: list"
      pattern: "requirements_completed: \\[CDX-01, CDX-02\\]"
    - from: ".planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md frontmatter"
      to: ".planning/v1.0-MILESTONE-AUDIT.md `Requirements Coverage` table TEST-01..06 rows"
      via: "requirements_completed: list"
      pattern: "requirements_completed: \\[TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06\\]"
---

<objective>
Backfill the `requirements_completed:` YAML frontmatter field in two Phase 4/5
SUMMARY files so the v1.0 milestone-audit cross-reference table is internally
consistent. The audit observed that 04-01 and 05-01 were missing this field,
forcing manual VERIFICATION.md cross-checks during the audit.

After this plan, every plan SUMMARY that closed a v1 requirement will declare
that requirement in its `requirements_completed:` list, and future audits will
run from the SUMMARY matrix alone.

Purpose: Tech-debt item #6 from `.planning/v1.0-MILESTONE-AUDIT.md`.

Output: Two SUMMARY files updated, atomically diffable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-milestone-cleanup/06-CONTEXT.md
@.planning/phases/04-codex-compatibility-evidence/04-VERIFICATION.md
@.planning/phases/05-synthetic-regression-gate/05-VERIFICATION.md
@.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md
@.planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md
@.planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md

<interfaces>
The reference frontmatter format (already used in `04-02-SUMMARY.md`) lives inside the YAML block at the top of the file. The relevant key is a YAML inline list:

```yaml
requirements-completed: [CDX-03]
```

NOTE: The existing `04-02-SUMMARY.md` uses the hyphenated key `requirements-completed:` (line 32 of that file). However the milestone-audit + roadmap and `06-CONTEXT.md` (M-06, M-07) both consistently spell the key `requirements_completed:` (underscore). The audit's `Requirements Coverage` table also reasons about it as `requirements_completed`. The intent is the underscore form — this plan adds the underscore form to 04-01 and 05-01 and ALSO normalizes 04-02-SUMMARY.md from the hyphenated form to the underscore form so all three Phase 4/5 SUMMARYs use the same canonical key. (Discretion call — see Decisions in this plan's `<output>` summary.)

After this plan, every Phase 4/5 SUMMARY uses the canonical underscore key:

```yaml
requirements_completed: [CDX-01, CDX-02]                      # 04-01
requirements_completed: [CDX-03]                              # 04-02 (normalized from hyphen)
requirements_completed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]   # 05-01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill 04-01-SUMMARY.md frontmatter with requirements_completed: [CDX-01, CDX-02]</name>
  <files>.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md (current frontmatter — note the `dependency_graph.affects` already lists `CDX-01, CDX-02 satisfied`; this is what we are surfacing into a structured field)
    - .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md (analog format example — the YAML frontmatter is the reference; note its key is currently the hyphen variant `requirements-completed:` at line 32)
    - .planning/phases/06-milestone-cleanup/06-CONTEXT.md (M-06)
    - .planning/v1.0-MILESTONE-AUDIT.md (audit `Requirements Coverage` table for CDX rows confirms CDX-01 + CDX-02 belong to plan 04-01)
  </read_first>
  <action>
Add a single new key to the existing YAML frontmatter block of `.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md`. The frontmatter starts with `---` on line 1 and ends with `---` on line 39.

Insert the following line into the frontmatter, alphabetized between `phase:` (line 2) and `subsystem:` (line 4) — i.e., immediately after the `plan: "01"` line. Place it before `subsystem:` to keep alphabetical-ish ordering with other top-level keys:

```yaml
requirements_completed: [CDX-01, CDX-02]
```

Recommended position: right after the existing `tags: [...]` line (line 5) so it sits next to other traceability metadata. Acceptable alternative: just before the `dependency_graph:` block on line 6. Pick one and be consistent.

Do NOT touch any other frontmatter key. Do NOT touch the body of the file (sections "What Was Built", "Verification", etc.). Do NOT change the `dependency_graph.affects` entry that mentions CDX-01/CDX-02 — that stays as-is; the new key is additive.

Underscore vs hyphen: use the underscore form `requirements_completed:` (matches `06-CONTEXT.md` M-06 and the v1.0 milestone audit's `Requirements Coverage` table key).
  </action>
  <verify>
    <automated>grep -c "^requirements_completed: \[CDX-01, CDX-02\]$" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md  # MUST output 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^requirements_completed: \[CDX-01, CDX-02\]$" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md` returns 1
    - `head -45 .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md | grep -c "^---$"` returns 2 (frontmatter open and close intact)
    - `grep -c "phase: 04-codex-compatibility-evidence" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md` returns 1 (existing key untouched)
    - `grep -c "plan: \"01\"" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md` returns 1 (existing key untouched)
    - YAML still parses: `python -c "import yaml,sys; doc=open('.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md').read().split('---')[1]; d=yaml.safe_load(doc); assert d['requirements_completed']==['CDX-01','CDX-02'], d; print('OK')"` exits 0 and prints `OK`
    - Body untouched: `grep -c "^# Phase 4 Plan 1: Codex Compatibility Matrix" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md` returns 1
  </acceptance_criteria>
  <done>
    `04-01-SUMMARY.md` frontmatter contains `requirements_completed: [CDX-01, CDX-02]` exactly once on its own line; YAML still parses; body content unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backfill 05-01-SUMMARY.md frontmatter with requirements_completed: [TEST-01..TEST-06]; normalize 04-02-SUMMARY.md key spelling</name>
  <files>.planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md, .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md (current frontmatter — frontmatter delimited by `---` on line 1 and line 38)
    - .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md (currently has the hyphenated form `requirements-completed: [CDX-03]` at line 32; this task normalizes it to the underscore form)
    - .planning/phases/06-milestone-cleanup/06-CONTEXT.md (M-07)
    - .planning/v1.0-MILESTONE-AUDIT.md (`Requirements Coverage` table TEST-01..06 rows all map to plan 05-01)
  </read_first>
  <action>
**(A) Add `requirements_completed:` to 05-01-SUMMARY.md frontmatter.**

Insert the following line into the YAML frontmatter of `.planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md`, placing it immediately after the existing `tags: [...]` line (line 5):

```yaml
requirements_completed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
```

Do NOT touch any other frontmatter key. Do NOT touch `dependency_graph.provides` or any body section. The new key is additive.

**(B) Normalize 04-02-SUMMARY.md key from hyphen to underscore.**

In `.planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md` line 32 (current):

```yaml
requirements-completed: [CDX-03]
```

Replace exactly with:

```yaml
requirements_completed: [CDX-03]
```

Only the key spelling changes; the value `[CDX-03]` is unchanged. This makes all three Phase 4/5 SUMMARYs use the same canonical underscore key, avoiding future grep-mismatch confusion in audits.

Do NOT touch any other line of `04-02-SUMMARY.md`. Do NOT touch its body sections.
  </action>
  <verify>
    <automated>grep -c "^requirements_completed: \[TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06\]$" .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md  # MUST output 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^requirements_completed: \[TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06\]$" .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md` returns 1
    - `grep -c "^requirements_completed: \[CDX-03\]$" .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md` returns 1
    - `grep -c "^requirements-completed:" .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md` returns 0 (hyphenated form gone)
    - YAML parses for both files: `python -c "import yaml; [print(yaml.safe_load(open(p).read().split('---')[1])['requirements_completed']) for p in ['.planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md','.planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md']]"` prints `['TEST-01', 'TEST-02', 'TEST-03', 'TEST-04', 'TEST-05', 'TEST-06']` then `['CDX-03']`
    - 05-01 body untouched: `grep -c "^# Phase 05 Plan 01: Synthetic Regression Gate Summary" .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md` returns 1
    - 04-02 body untouched: `grep -c "^# Phase 4 Plan 2: Codex Claim Gate Summary" .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md` returns 1
    - All three Phase 4/5 SUMMARYs now use canonical underscore key: `grep -lE "^requirements_completed:" .planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md | wc -l` returns 3
  </acceptance_criteria>
  <done>
    05-01-SUMMARY.md frontmatter contains the TEST-01..06 list under the underscore key; 04-02-SUMMARY.md key normalized from hyphen to underscore (value unchanged); both files still parse as YAML; bodies untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. Markdown/YAML frontmatter edits to planning artifacts; no runtime code, no I/O, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | I (Information disclosure) | Plan SUMMARY frontmatter | accept | No runtime behavior change. Edits only documentation/traceability metadata. No new authentication, input handling, or data flow paths. Surfaces requirement-ID lists already publicly recorded in REQUIREMENTS.md and VERIFICATION.md. |
</threat_model>

<verification>
- `pytest tests -q` is unaffected by this plan (no code touched). Verification belongs to plan 06-04.
- All three Phase 4/5 SUMMARYs use the canonical underscore `requirements_completed:` key.
- YAML frontmatter still parses cleanly in all three files.
</verification>

<success_criteria>
- M-06: 04-01-SUMMARY.md frontmatter declares `requirements_completed: [CDX-01, CDX-02]`.
- M-07: 05-01-SUMMARY.md frontmatter declares `requirements_completed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]`.
- Bonus: 04-02-SUMMARY.md key normalized from `requirements-completed:` to `requirements_completed:` (value unchanged); ensures all three SUMMARYs use the same canonical key per `06-CONTEXT.md` M-06/M-07 underscore convention.
- No file other than these three SUMMARY files is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-milestone-cleanup/06-03-SUMMARY.md` with frontmatter including:
- `requirements_completed: []` (this plan delivers traceability metadata; the v1 requirements being declared are recorded by their owning plans — 04-01 / 04-02 / 05-01 — not by this cleanup plan)
- `key_files.modified: [.planning/phases/04-codex-compatibility-evidence/04-01-SUMMARY.md, .planning/phases/04-codex-compatibility-evidence/04-02-SUMMARY.md, .planning/phases/05-synthetic-regression-gate/05-01-SUMMARY.md]`
- `decisions:` recording: (1) M-06 added `requirements_completed: [CDX-01, CDX-02]` to 04-01; (2) M-07 added `requirements_completed: [TEST-01..TEST-06]` to 05-01; (3) discretion call — normalized 04-02's pre-existing hyphenated key to the canonical underscore form so all three Phase 4/5 SUMMARYs share the same key spelling and future audits can grep `requirements_completed` in one pass.
</output>
