---
phase: 06-milestone-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
requirements_addressed: []
tags: [docs, roadmap, milestone-cleanup, sync]
must_haves:
  truths:
    - "ROADMAP.md Phase 5 phase-list checkbox is ticked [x]"
    - "ROADMAP.md progress table shows Phase 5 as 1/1 Complete dated 2026-05-05"
    - "ROADMAP.md no longer mentions `privacy-guard` (legacy command name)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Synced phase status, progress table, and canonical CLI naming for v1 close"
      contains: "[x] **Phase 5: Synthetic Regression Gate**"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 5 phase-list line)"
      to: ".planning/phases/05-synthetic-regression-gate/05-VERIFICATION.md"
      via: "[x] checkbox aligns with PASSED 2026-05-05 verdict"
      pattern: "- \\[x\\] \\*\\*Phase 5"
    - from: ".planning/ROADMAP.md (Progress table)"
      to: ".planning/phases/05-synthetic-regression-gate/05-01-PLAN.md"
      via: "row reads `1/1 ... Complete 2026-05-05` matching the single ticked plan"
      pattern: "Synthetic Regression Gate \\| 1/1 \\| Complete \\| 2026-05-05"
---

<objective>
Sync `.planning/ROADMAP.md` with verified milestone state. Three mechanical
edits per CONTEXT.md M-03/M-04/M-05:

1. Tick `[x]` on the Phase 5 line in the Phases list.
2. Update Progress table Phase 5 row from `0/TBD ... Not started` to
   `1/1 ... Complete 2026-05-05`.
3. Replace every `privacy-guard` legacy name reference with `privguard`
   (Overview line 28 / Phase 1 success criterion #1, plus any other hits).

Purpose: ROADMAP.md is currently inconsistent with the verified state of Phase 5
and still cites the pre-rename CLI name in two prominent places.

Output: A single ROADMAP.md edit landing M-03, M-04, and M-05 in one diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-milestone-cleanup/06-CONTEXT.md
@.planning/phases/05-synthetic-regression-gate/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tick Phase 5; correct Progress row; replace privacy-guard with privguard everywhere in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — note line 19 Phase 5 checkbox, line 28 success criterion, line 30 Phase 1 SC #1, and progress table around line 161)
    - .planning/phases/06-milestone-cleanup/06-CONTEXT.md (M-03, M-04, M-05)
    - .planning/phases/05-synthetic-regression-gate/05-VERIFICATION.md (Phase 5 PASSED 2026-05-05 — drives M-03/M-04 dates)
  </read_first>
  <action>
Make the following edits to `.planning/ROADMAP.md`. Verify exact line text with the Read tool first; line numbers below are guidance based on the audit but should be confirmed against current file content.

**(A) M-03 — Tick Phase 5 in the Phases list (around line 19).**

Replace:
```
- [ ] **Phase 5: Synthetic Regression Gate** - The full v1 surface is covered by synthetic tests proving no raw sensitive values leak through outputs, logs, hooks, masks, or failures.
```

With:
```
- [x] **Phase 5: Synthetic Regression Gate** - The full v1 surface is covered by synthetic tests proving no raw sensitive values leak through outputs, logs, hooks, masks, or failures.
```

Only the `[ ]` → `[x]` changes; the rest of the line is identical.

**(B) M-04 — Fix Phase 5 row in the Progress table (around line 161).**

Replace:
```
| 5. Synthetic Regression Gate | 0/TBD | Not started | - |
```

With:
```
| 5. Synthetic Regression Gate | 1/1 | Complete | 2026-05-05 |
```

Do NOT change the Phase 6 or Phase 7 rows in the same table — those remain `Not started` until those phases close.

**(C) M-05 — Replace `privacy-guard` with `privguard` everywhere in ROADMAP.md.** Per the audit there are at least three known occurrences:

  1. **Overview line 28** (Phase 1 description in Success Criteria): the bullet
     ```
     1. Developer can install the project locally with a reproducible dependency manifest and run the `privacy-guard` command.
     ```
     becomes
     ```
     1. Developer can install the project locally with a reproducible dependency manifest and run the `privguard` command.
     ```

  2. **Phase 6 Success Criteria #2 (line ~114):** `... no legacy `privacy-guard` drift.` — leave the literal `` `privacy-guard` `` quoted there because it is naming the legacy term being removed; instead, rewrite this success criterion line to read:
     ```
       2. ROADMAP.md Phase 5 status, progress row, and Phase 1 success criteria use the canonical command name `privguard` with no legacy drift.
     ```
     This matches the closed-state intent of Phase 6 itself: after this plan lands, there is no remaining `privacy-guard` token anywhere in ROADMAP.md.

  3. **Phase 6 Success Criteria #4 (line ~116):** `` `privacy-guard` console-script alias is either installed and tested or removed; ... `` becomes
     ```
       4. The `privacy-guard` console-script alias is removed (Phase 1 D-01 locked the canonical name to `privguard`); `pyproject.toml` and REQUIREMENTS.md PKG-02 text agree on the canonical name.
     ```
     Wait — this still references the legacy term to describe the removed thing. Per CONTEXT.md M-05 the goal is "no `privacy-guard` token anywhere in ROADMAP.md after this plan." So instead use:
     ```
       4. The legacy console-script alias is removed (Phase 1 D-01 locked the canonical name to `privguard`); `pyproject.toml` and REQUIREMENTS.md PKG-02 text agree on the canonical name.
     ```

  Then run `rg "privacy-guard" -n .planning/ROADMAP.md` to verify zero hits remain. If any other occurrence is found, replace each `privacy-guard` token with `privguard` (or, if the sentence is naming the legacy thing being eliminated, rephrase to drop the legacy token entirely as in (C.2) and (C.3) above).

Do NOT touch Phase 6 SC #1, #3, #5, or #6 beyond the verbatim `privacy-guard` substitutions called out above. Do NOT touch Phase 1's `[x]` (already correct). Do NOT touch the "Plans" sub-list of Phase 5 (already shows `[x] 05-01-PLAN.md`).
  </action>
  <verify>
    <automated>grep -c "privacy-guard" .planning/ROADMAP.md  # MUST output 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*Phase 5: Synthetic Regression Gate" .planning/ROADMAP.md` returns 1
    - `grep -c "^- \[ \] \*\*Phase 5:" .planning/ROADMAP.md` returns 0
    - `grep -c "| 5. Synthetic Regression Gate | 1/1 | Complete | 2026-05-05 |" .planning/ROADMAP.md` returns 1
    - `grep -c "| 5. Synthetic Regression Gate | 0/TBD | Not started | - |" .planning/ROADMAP.md` returns 0
    - `grep -c "privacy-guard" .planning/ROADMAP.md` returns 0
    - `grep -c "run the \`privguard\` command" .planning/ROADMAP.md` returns 1
    - Phase 6 / Phase 7 progress rows still show `Not started`: `grep -c "Milestone Cleanup | 0/TBD | Not started" .planning/ROADMAP.md` returns 1 and `grep -c "Project README + Repo Hygiene | 0/TBD | Not started" .planning/ROADMAP.md` returns 1
    - Phase 1 line still ticked: `grep -c "^- \[x\] \*\*Phase 1: Package Foundation" .planning/ROADMAP.md` returns 1
  </acceptance_criteria>
  <done>
    Phase 5 phase-list line shows `[x]`; Progress table Phase 5 row reads `1/1 | Complete | 2026-05-05`; `rg "privacy-guard" .planning/ROADMAP.md` returns zero hits; Phase 6 and Phase 7 progress rows are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. The change is a markdown file edit; no runtime code, no I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | I (Information disclosure) | `.planning/ROADMAP.md` | accept | No runtime behavior change. Edits only documentation. No new authentication, input handling, or data flow paths. |
</threat_model>

<verification>
- `pytest tests -q` is unaffected by this plan (no code touched). Verification belongs to plan 06-04.
- ROADMAP.md Phase 5 status, progress row, and CLI naming all reflect the verified state of the system.
</verification>

<success_criteria>
- M-03, M-04, M-05 all delivered in `.planning/ROADMAP.md`.
- `rg "privacy-guard" -n .planning/ROADMAP.md` returns zero hits.
- No file other than `.planning/ROADMAP.md` is modified.
- Phase 6 / Phase 7 rows remain `Not started` (this plan does not retroactively close Phase 6 itself; the orchestrator does that on phase completion).
</success_criteria>

<output>
After completion, create `.planning/phases/06-milestone-cleanup/06-02-SUMMARY.md` with frontmatter including:
- `requirements_completed: []` (this plan is doc-sync support and addresses no v1 requirement directly; CDX/TEST closure lives in plan 06-01, packaging in 06-04)
- `key_files.modified: [.planning/ROADMAP.md]`
- `decisions:` recording that two Phase 6 own success criteria (#2, #4) were rephrased to remove the legacy `privacy-guard` token entirely rather than leave it quoted, since CONTEXT.md M-05 mandates zero hits.
</output>
