---
plan: 999.2-01
phase: 999.2-audit-log-minimo-viavel
title: "Audit Log: _audit_log() + instrumentação + testes"
wave: 1
autonomous: true
gap_closure: false
files_modified:
  - privguard/hooks.py
  - tests/test_claude_hooks.py
requirements: []
---

## Objective

Adicionar `_audit_log()` em `privguard/hooks.py` e instrumentar todos os pontos de bloqueio/warn para escrever uma linha JSON em `~/.privguard/audit.log`. A função deve ser fire-and-forget (nunca propaga exceção). Adicionar 2 testes de contrato em `tests/test_claude_hooks.py`.

## Context

- **Research:** `.planning/phases/999.2-audit-log-minimo-viavel/999.2-RESEARCH.md`
- **Arquivo principal:** `privguard/hooks.py` (303 linhas)
- **Testes de referência:** `tests/test_claude_hooks.py` — padrão com `monkeypatch`, `run_pre_tool()`, `capsys`

## must_haves

- [ ] `_audit_log()` nunca propaga exceção — qualquer `Exception` é silenciada
- [ ] Bloqueios em `deny()` e `_deny_pre_tool()` escrevem no log
- [ ] Warn em `main_user_prompt()` (modo warn) escreve no log
- [ ] Log path é injetável via parâmetro (facilita testes sem monkeypatch de Path)
- [ ] 2 testes de contrato passam: (1) block escreve JSON válido; (2) log injetável/silencioso quando path inválido
- [ ] `uv run pytest` passa 139 passed, 1 skipped (sem regressões)

## Tasks

<task id="1">
<name>Adicionar _audit_log() e imports em hooks.py</name>

<read_first>
- privguard/hooks.py (estado atual — ler inteiro antes de editar)
</read_first>

<action>
Adicionar no topo de `privguard/hooks.py`, após `from .policy import classify_command, classify_path`:

```python
import datetime
import pathlib
```

Adicionar a função `_audit_log()` logo após as imports, antes de `deny()`:

```python
def _audit_log(
    *,
    event: str,
    action: str,
    reason_code: str,
    category: str = "",
    log_path: "pathlib.Path | None" = None,
) -> None:
    """Append one JSON line to ~/.privguard/audit.log. Never raises."""
    try:
        if log_path is None:
            log_dir = pathlib.Path.home() / ".privguard"
            log_dir.mkdir(parents=True, exist_ok=True)
            log_path = log_dir / "audit.log"
        entry = {
            "ts": datetime.datetime.utcnow().isoformat() + "Z",
            "event": event,
            "action": action,
            "reason_code": reason_code,
            "category": category,
        }
        with log_path.open("a", encoding="utf-8") as fh:
            fh.write(json.dumps(entry) + "\n")
    except Exception:
        pass
```

`json` já está importado (linha 5). `datetime` e `pathlib` são stdlib — adicionar junto aos imports existentes.
</action>

<acceptance_criteria>
- `privguard/hooks.py` contém `import datetime` e `import pathlib`
- `privguard/hooks.py` contém `def _audit_log(`
- `privguard/hooks.py` contém `except Exception:` seguido de `pass` (fire-and-forget)
- `privguard/hooks.py` contém `log_path: "pathlib.Path | None" = None`
</acceptance_criteria>
</task>

<task id="2">
<name>Instrumentar deny(), _deny_pre_tool() e warn path em main_user_prompt()</name>

<read_first>
- privguard/hooks.py (após task 1)
</read_first>

<action>
**Em `deny()` (linha 15–17):** adicionar chamada ANTES do return:

```python
def deny(prefix: str, reason_code: str) -> int:
    _audit_log(event="UserPromptSubmit", action="block", reason_code=reason_code)
    sys.stderr.write(f"[{prefix} BLOQUEADO] reason={reason_code}\n")
    return 2
```

**Em `_deny_pre_tool()` (linha 20–39):** adicionar chamada ANTES de `sys.stderr.write(...)`:

```python
def _deny_pre_tool(
    *,
    reason_code: str,
    category: str,
    path_count: int = 0,
    command_count: int = 0,
) -> int:
    _audit_log(event="PreToolUse", action="block", reason_code=reason_code, category=category)
    details = [
        f"reason={reason_code}",
        ...
    ]
```

**Em `main_user_prompt()`, modo warn (linhas 168–170):** adicionar chamada após `if mode == "warn":`:

```python
if mode == "warn":
    _audit_log(event="UserPromptSubmit", action="warn", reason_code="pii_detected")
    print(_prompt_json_context(reason_code="pii_detected", hits=hits, mode=mode))
    return 0
```

Não instrumentar os outros bloqueios em `main_user_prompt()` separadamente — eles já passam por `deny()` indiretamente via `sys.stderr.write` direta; instrumentar `deny()` cobre o caso geral. O modo scrub e pii_detected block em `main_user_prompt()` NÃO chamam `deny()` — adicionar `_audit_log()` nesses dois blocos também:

Bloco scrub (linhas 172–186):
```python
if mode == "scrub":
    _audit_log(event="UserPromptSubmit", action="block", reason_code="scrub_unsupported")
    sys.stderr.write(...)
    return 2
```

Bloco pii_detected final (linhas 188–193):
```python
_audit_log(event="UserPromptSubmit", action="block", reason_code="pii_detected")
sys.stderr.write(...)
return 2
```
</action>

<acceptance_criteria>
- `privguard/hooks.py`: `deny()` contém `_audit_log(event="UserPromptSubmit", action="block"`
- `privguard/hooks.py`: `_deny_pre_tool()` contém `_audit_log(event="PreToolUse", action="block"`
- `privguard/hooks.py`: `main_user_prompt()` modo warn contém `_audit_log(event="UserPromptSubmit", action="warn"`
- `privguard/hooks.py`: bloco scrub contém `_audit_log(` com `reason_code="scrub_unsupported"`
- `privguard/hooks.py`: bloco pii_detected final contém `_audit_log(` com `reason_code="pii_detected"`
</acceptance_criteria>
</task>

<task id="3">
<name>Adicionar 2 testes de contrato em test_claude_hooks.py</name>

<read_first>
- tests/test_claude_hooks.py (ler inteiro — entender padrão run_pre_tool, monkeypatch, capsys)
- privguard/hooks.py (após tasks 1 e 2 — verificar assinatura de _audit_log)
</read_first>

<action>
Adicionar no final de `tests/test_claude_hooks.py`:

```python
import pathlib
from privguard.hooks import _audit_log


def test_audit_log_block_writes_json_line(tmp_path: pathlib.Path) -> None:
    """A block event appends a valid JSON line to the log."""
    log_file = tmp_path / "audit.log"
    _audit_log(
        event="PreToolUse",
        action="block",
        reason_code="unknown_tool",
        category="unknown_tool",
        log_path=log_file,
    )
    assert log_file.exists()
    import json
    entry = json.loads(log_file.read_text(encoding="utf-8").strip())
    assert entry["event"] == "PreToolUse"
    assert entry["action"] == "block"
    assert entry["reason_code"] == "unknown_tool"
    assert entry["category"] == "unknown_tool"
    assert entry["ts"].endswith("Z")


def test_audit_log_silently_ignores_write_failure(tmp_path: pathlib.Path) -> None:
    """_audit_log never raises even if the log path is unwritable."""
    bad_path = tmp_path / "nonexistent_dir" / "subdir" / "audit.log"
    # Passing a path whose parent doesn't exist — open("a") will raise FileNotFoundError
    # The fire-and-forget wrapper must swallow it.
    _audit_log(
        event="UserPromptSubmit",
        action="block",
        reason_code="pii_detected",
        log_path=bad_path,
    )
    # If we reach here, no exception was raised — test passes
```

Também adicionar `import json` e `import pathlib` e `from privguard.hooks import _audit_log` no topo do arquivo se ainda não presentes (verificar antes de adicionar).
</action>

<acceptance_criteria>
- `tests/test_claude_hooks.py` contém `def test_audit_log_block_writes_json_line(`
- `tests/test_claude_hooks.py` contém `def test_audit_log_silently_ignores_write_failure(`
- `uv run pytest tests/test_claude_hooks.py -q` passa sem falhas
- `uv run pytest -q` passa 141 passed, 1 skipped (2 novos testes adicionados ao total)
</acceptance_criteria>
</task>

<task id="4">
<name>Commit</name>

<read_first>
- Nenhum — apenas verificar git status
</read_first>

<action>
```bash
git add privguard/hooks.py tests/test_claude_hooks.py
git commit -m "feat(999.2): add fire-and-forget audit log to hook block/warn events"
```
</action>

<acceptance_criteria>
- `git log --oneline -1` mostra commit com mensagem `feat(999.2): add fire-and-forget audit log`
- `uv run pytest -q` passa 141 passed, 1 skipped
</acceptance_criteria>
</task>

## Verification

```bash
uv run pytest tests/test_claude_hooks.py -v -k "audit"
# Expected: 2 passed

uv run pytest -q
# Expected: 141 passed, 1 skipped
```

Smoke test manual:
```bash
# Trigger a block — CPF sintético
echo '{"prompt": "cpf 123.456.789-09"}' | uv run privguard-user-prompt
cat ~/.privguard/audit.log | tail -1
# Expected: JSON line com event=UserPromptSubmit, action=block, reason_code=pii_detected
```
