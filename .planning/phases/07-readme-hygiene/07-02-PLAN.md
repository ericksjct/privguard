---
phase: 07-readme-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
tags: [docs, readme, english, brazil-first, code]
must_haves:
  truths:
    - "First-time user can land on the GitHub repo and read a top-level English README that explains what privguard is, what it does NOT do, how to install it, and how to wire it into Claude Code."
    - "User can copy the Quickstart code block from the README and run a `privguard mask` invocation against three synthetic Brazilian fixtures and see typed placeholders (`<BR_CPF>`, `<BR_CNPJ>`, plus a secret token placeholder) in the output — no inline literal CPF/CNPJ values appear in the final masked text."
    - "User following the Claude Code hook setup section pastes a `.claude/settings.json` snippet that uses the console-script names `privguard-user-prompt` and `privguard-pre-tool` (D-15) and the snippet works after `pip install privguard`."
    - "User reading the capabilities matrix sees four rows with status column wording verbatim (`block-supported` for the two Claude rows, `experimental block-only` for the two Codex rows) — never `rewrite-capable` or `automatic masking`."
    - "User reading the FAQ finds answers to all four required questions (Codex support, missed CPF, block-vs-warn, extending cleanup patterns)."
    - "User reading the 'What privguard does NOT do' section sees the five enumerated non-goals (hosted SaaS, deanonymization, LangChain/LlamaIndex adapters, unsupported clients, real-data fixtures)."
    - "User reading the synthetic-fixture-only policy section understands no real Brazilian PII is used in tests, examples, or commits."
    - "Coding agent following the 'For coding agents' line at the bottom finds AGENTS.md."
    - "Reader sees the cross-language switcher row immediately above the H1 with both flag-prefixed links present."
    - "README links to `docs/codex-compatibility.md` (matrix evidence footer) and `docs/install.md` (install detail link, summarize-and-link strategy)."
  artifacts:
    - path: "README.md"
      provides: "Top-level English README satisfying DOC-01 English half: cross-language switcher (D-02), all 9 D-04 sections, D-06 capabilities matrix verbatim, D-15 console-script hook snippet, D-04 §6/§7/§8 non-goals/policy/FAQ, D-05 AGENTS.md link."
      contains: "[🇺🇸 English](README.md) | [🇧🇷 Português](README.pt-BR.md)"
      min_lines: 180
  key_links:
    - from: "README.md (Capabilities matrix section)"
      to: "docs/codex-compatibility.md"
      via: "markdown link in matrix footer"
      pattern: "docs/codex-compatibility\\.md"
    - from: "README.md (Install section)"
      to: "docs/install.md"
      via: "markdown link (summarize-and-link strategy)"
      pattern: "docs/install\\.md"
    - from: "README.md (For coding agents line)"
      to: "AGENTS.md"
      via: "markdown link per D-05"
      pattern: "\\(AGENTS\\.md\\)"
    - from: "README.md (Claude Code hook setup section)"
      to: "pyproject.toml [project.scripts] (D-15)"
      via: "JSON snippet uses `privguard-user-prompt` and `privguard-pre-tool` (the two console scripts plan 07-01 added)"
      pattern: "privguard-(user-prompt|pre-tool)"
    - from: "README.md (Quickstart section)"
      to: "tests/test_v1_regression_gate.py:45-47 (synthetic fixtures)"
      via: "fixtures are read from the test file at execution time and copied into the quickstart code block; placeholders use Phase 2 vocabulary from privguard/masking.py:32"
      pattern: "<BR_CPF>"
---

<objective>
Land DOC-01 English half: ship a top-level `README.md` that lets a first-time user understand, install, and integrate privguard with Claude Code, with locked language for the capabilities matrix (D-06), the Claude hook setup snippet (D-15 console scripts, NOT `python -m privguard.hooks…`), the synthetic-fixture-only policy, and the four required FAQ entries (D-04 §8).

Purpose:
- Closes Phase 7 success criterion #1 (English half) — `README.md` exists at repo root and covers all 9 D-04 sections.
- Anchors the EN canonical text that plan 07-03 translates to pt-BR (D-03 paired-update rule).
- Documents the working hook command form (`privguard-user-prompt` / `privguard-pre-tool`) that plan 07-01 just shipped in `pyproject.toml [project.scripts]` — closes the loop on D-15.
- Locks the capabilities-matrix vocabulary so Codex surfaces are never claimed as `rewrite-capable` or `automatic masking` (T2 in threat model; CDX-03 hard constraint from `docs/codex-compatibility.md:71-83`).

Output: One new file `README.md` at repo root. Approximately 180-260 lines (length target — text size will fluctuate; the verifiable contract is the section headings, the locked-vocabulary blocks, and the link targets).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-readme-hygiene/07-CONTEXT.md
@.planning/phases/07-readme-hygiene/07-RESEARCH.md
@.planning/phases/07-readme-hygiene/07-PATTERNS.md
@.planning/phases/07-readme-hygiene/07-01-PLAN.md
@AGENTS.md
@docs/install.md
@docs/codex-compatibility.md
@privguard/masking.py
@privguard/detection.py
@tests/test_v1_regression_gate.py
@pyproject.toml

<interfaces>
<!-- Key contracts the executor MUST honor verbatim. Extracted from the source artifacts
     so the executor does NOT have to re-derive them. -->

From `tests/test_v1_regression_gate.py:45-47` — the three synthetic fixtures the masking
demo MUST reuse. The executor reads this file and copies the values into the README's
quickstart code block. Do NOT invent new values. (The values themselves are not pasted
into this plan because the repo's pre-tool guard refuses inline Brazilian PII even in
plan text — TEST-01 / RESEARCH.md §"Existing synthetic fixtures to reuse".)

From `privguard/masking.py:32` (placeholder vocabulary):
```python
out.append(f"<{h.kind}>")  # produces <BR_CPF>, <BR_CNPJ>, <TOKEN>, <API_KEY>, etc.
```
The placeholder format is mechanical: `<{kind}>` with the detector's `kind` string verbatim.
The README MUST use `<BR_CPF>` and `<BR_CNPJ>` (with the `BR_` prefix), NEVER `<CPF>` /
`<CNPJ>` without the prefix.

From `privguard/detection.py:167-192` — the detector kinds that produce placeholders:
```python
PatternEntry("BR_CNPJ", ...)        # produces <BR_CNPJ>
PatternEntry("BR_CPF", ...)         # produces <BR_CPF>
PatternEntry("BR_CNH", ...)         # produces <BR_CNH>
PatternEntry("API_KEY", ...)        # produces <API_KEY>  (sk- prefixed keys)
PatternEntry("TOKEN", ...)          # produces <TOKEN>    (ghp_/glpat_/xox prefixed)
```

From `pyproject.toml [project.scripts]` (after plan 07-01 lands):
```toml
[project.scripts]
privguard             = "privguard.cli:main"
privguard-user-prompt = "privguard.hooks:main_user_prompt"
privguard-pre-tool    = "privguard.hooks:main_pre_tool"
```
The README hook setup snippet MUST use the two console-script names
(`privguard-user-prompt`, `privguard-pre-tool`) — NEVER `python -m privguard.hooks.…`.

From `docs/codex-compatibility.md:52-65` — verified Codex labels (D-06 source of truth).
The README condensed matrix uses these exact words: `block-supported`, `experimental
block-only`. NEVER `rewrite-capable`, NEVER `automatic masking`, NEVER `supported`
without a qualifier.

From `docs/install.md` — referenced via summarize-and-link (planner's choice per
"Claude's Discretion" / RESEARCH.md Open Question #2 recommendation: lower drift
risk than absorbing). README install section is 5-10 lines and links to
`docs/install.md` for full Python 3.14 gating detail.

From AGENTS.md — referenced via single link `[AGENTS.md](AGENTS.md)` per D-05.
NOT modified by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README.md — sections 1-5 (switcher + H1 + tagline + Install + Quickstart + CLI usage + Hook setup + Capabilities matrix)</name>
  <files>README.md</files>
  <read_first>
    - tests/test_v1_regression_gate.py (READ lines 45-47 for the three synthetic fixtures `SYNTH_CPF`, `SYNTH_CNPJ`, `SYNTH_CNH`; copy the EXACT string values from the source into the README quickstart code block — do not retype, do not modify, do not invent)
    - privguard/masking.py (READ line 32 — placeholder format is `<{h.kind}>` so the README MUST use `<BR_CPF>` not `<CPF>`)
    - privguard/detection.py (READ lines 167-192 — confirms detector kinds `BR_CPF`, `BR_CNPJ`, `BR_CNH`, `API_KEY`, `TOKEN`; the README quickstart uses `<BR_CPF>` and `<BR_CNPJ>` plus a secret-token placeholder)
    - docs/codex-compatibility.md (READ lines 52-65 to confirm the verbatim Codex labels — `experimental block-only` and `block-supported`; the README matrix wording must agree with `privguard/codex.py CODEX_COMPATIBILITY` row labels)
    - docs/install.md (READ all 54 lines — the README install section uses summarize-and-link strategy: a 5-10 line summary that points to this file for full detail; do not copy the install.md content into README, just link to it)
    - pyproject.toml (READ lines 23-25 AFTER plan 07-01 lands — confirm the two console-script names `privguard-user-prompt` and `privguard-pre-tool` are declared; the README snippet uses these EXACTLY, including the hyphens, no underscores)
    - AGENTS.md (READ first 50 lines for tone reference only — the README does NOT promote AGENTS.md content, only links to it)
    - .planning/phases/07-readme-hygiene/07-CONTEXT.md (READ §"Decisions" lines 31-202 — D-01 through D-15 lock the structure; D-04 §1-§5 cover the sections this task ships; "Claude's Discretion" §"Section ordering" line 191 lets the planner pick exact heading text)
    - .planning/phases/07-readme-hygiene/07-RESEARCH.md (READ §"Quickstart synthetic-CPF/CNPJ masking demo (drop-in for README)" at lines 736-770 for the masking demo template; READ §"Capabilities matrix (drop-in for README §5)" at lines 831-850 for the locked 4-row table)
    - .planning/phases/07-readme-hygiene/07-PATTERNS.md (READ §"No Analog Found" lines 504-510 — README has no in-repo prose analog; mirror tone of `docs/install.md` (terse, technical English, fenced ```bash blocks for CLI invocations); do NOT mirror AGENTS.md GSD-block structure)
  </read_first>
  <action>
**This task creates `README.md` from scratch and writes sections 1 through 5 of the 9-section structure mandated by D-04. Sections 6-9 are written in Task 2.**

**The file is created and committed in this task with sections 1-5 fully written and sections 6-9 left as TODO-marker stubs the next task replaces. This is acceptable because Task 2 runs immediately after in the same wave.**

**File header order (top of `README.md` first 5 lines, in this exact order):**

1. **Cross-language switcher row (D-02)** — write this EXACT line as line 1:
   ```markdown
   [🇺🇸 English](README.md) | [🇧🇷 Português](README.pt-BR.md)
   ```
   The line is byte-identical to what plan 07-03 will put on top of `README.pt-BR.md`. The flag-prefix-emoji + plain-markdown-link form is locked by D-02.

2. **Blank line** (line 2).

3. **H1 with project name** (line 3):
   ```markdown
   # privguard
   ```
   No tagline on the H1 line itself. Use a short subtitle paragraph below.

4. **Blank line** (line 4).

5. **One-paragraph tagline (line 5+, ~2-4 sentences)** describing what privguard is. Draft from PROJECT.md "Core value" + "Brazil-first" + "fail-closed" + "local-only". Tone: terse, factual, mirrors `docs/install.md:3` ("privguard is a local-first Python package. There is no hosted service component — everything runs in your developer environment.").

   The paragraph must contain the phrases: "local", "Brazilian", "Claude Code", "fail-closed" (or "fails closed"). It must NOT claim "automatic masking" for any external surface or use the word "rewrite-capable".

**Then write sections in this order with these EXACT heading texts (the acceptance grep below uses this text, so it MUST match):**

### Section 1 — `## Install`

Strategy: summarize-and-link (per RESEARCH.md Open Question #2 recommendation). Section length: 5-10 lines.

Write:
```markdown
## Install

```bash
pip install privguard            # baseline (no third-party runtime deps)
pip install "privguard[full]"    # adds Presidio analyzer for richer detection
```

Requires Python ≥ 3.10. For the `[full]` extra's Python 3.14 gating notes, see [`docs/install.md`](docs/install.md).
```

That's the whole section. Do not duplicate the Python-3.14-gating prose from `docs/install.md` — the link is the canonical source. This satisfies D-04 §1.

### Section 2 — `## Quickstart`

Heading text MUST be exactly `## Quickstart` (the D-04 spec says "Quickstart in 60 seconds" but the planner picks the shorter heading).

Section content: a single fenced ```bash block showing `privguard mask` consuming three lines of synthetic input and producing typed-placeholder output.

**Executor instructions (READ this carefully):**

1. Open `tests/test_v1_regression_gate.py` and read lines 45-47. You will see three constant assignments: `SYNTH_CPF = "..."`, `SYNTH_CNPJ = "..."`, `SYNTH_CNH = "..."`. Each holds a synthetic, checksum-valid Brazilian identifier value.
2. Copy each of the three values VERBATIM into the README quickstart code block. Do not retype, do not modify, do not adjust formatting.
3. The masked output uses Phase 2 placeholder vocabulary from `privguard/masking.py:32`. The placeholder format is `<{kind}>`. So `SYNTH_CPF` produces `<BR_CPF>`, `SYNTH_CNPJ` produces `<BR_CNPJ>`, `SYNTH_CNH` produces `<BR_CNH>`. The placeholders MUST have the `BR_` prefix.

The structure of the fenced code block is:

```
$ echo "<text containing SYNTH_CPF>" | privguard mask
<text with <BR_CPF> in place of SYNTH_CPF>

$ echo "<text containing SYNTH_CNPJ>" | privguard mask
<text with <BR_CNPJ> in place of SYNTH_CNPJ>
```

Add ONE additional line demonstrating a fake secret token (use `FAKE_SECRET_GHP` from `tests/test_v1_regression_gate.py:55` — a `ghp_…` value — which masks to `<TOKEN>`). The masking demo shows three placeholder kinds: `<BR_CPF>`, `<BR_CNPJ>`, `<TOKEN>`.

After the code block, write a single sentence explaining that all values shown are synthetic test fixtures (not real Brazilian PII), and link to the synthetic-fixture-only policy section below: "All values shown above are synthetic test fixtures — see [Synthetic-fixture-only policy](#synthetic-fixture-only-policy)."

Closes D-04 §2.

### Section 3 — `## CLI usage`

Heading text exactly `## CLI usage`. Section length: ~15-25 lines.

Write a fenced ```bash block listing each subcommand with a one-line description and a representative invocation. The five subcommands required by D-04 §3 are:

```bash
privguard info             # version + module surface
privguard scan <text>      # detection only — entity types, counts, offsets, reason codes
privguard mask <text>      # detection + irreversible placeholder substitution
privguard policy-check     # decide whether a payload may leave (fail-closed default)
privguard claude doctor    # Claude hook installation + effective-policy diagnostics
privguard cleanup          # dry-run preview of cleanable artifacts (use --apply to delete)
```

After the code block, add a single sentence pointing to `--help`: "Run any subcommand with `--help` to see all flags."

Notes:
- `privguard cleanup` is included even though the user-visible explanation lives under FAQ §"How do I extend the cleanup patterns?" — including it in the CLI list satisfies D-04 §3 which explicitly names it.
- Do NOT include the `--apply` flag in the inline list; the `# use --apply to delete` comment is enough.

Closes D-04 §3.

### Section 4 — `## Claude Code hook setup`

Heading text exactly `## Claude Code hook setup`. Section length: ~20-35 lines.

Section structure:
1. One-line intro: "After installing privguard, wire two hooks into your Claude Code project's `.claude/settings.json`:"
2. A fenced ```json block containing the EXACT JSON below (verbatim — the acceptance criterion greps for `privguard-user-prompt` and `privguard-pre-tool` strings):

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          { "type": "command", "command": "privguard-user-prompt" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          { "type": "command", "command": "privguard-pre-tool" }
        ]
      }
    ]
  }
}
```

3. After the JSON block, two short paragraphs:
   - First paragraph: "These two console scripts ship with `pip install privguard` (declared in `pyproject.toml [project.scripts]`). They are install-path-independent — Claude Code finds them on `PATH` after install."
   - Second paragraph: "Verify your installation with `privguard claude doctor` — it reports whether the hooks are wired without reading any protected file."

**CRITICAL:** The hook commands MUST be `privguard-user-prompt` and `privguard-pre-tool` (with hyphens). DO NOT write `python -m privguard.hooks.main_user_prompt` or any `python -m` form. D-15 explicitly overrides the original `python -m` phrasing in CONTEXT.md "Specifics" because `privguard/hooks.py` is a single-file module exposing `main_user_prompt` and `main_pre_tool` as functions, not as runnable submodules; the `python -m` form would not work. Plan 07-01 added the two console scripts to `pyproject.toml [project.scripts]` precisely so this README section documents a working command.

Closes D-04 §4.

### Section 5 — `## Capabilities matrix`

Heading text exactly `## Capabilities matrix`. Section length: ~12-15 lines.

Write a one-line intro: "Privacy interception status by surface (Phase 3 + Phase 4 evidence):"

Then write this EXACT 4-row markdown table (the wording is locked by D-06 — every status-column value MUST appear verbatim; the acceptance criterion greps for `block-supported` and `experimental block-only` and counts ≥ 4 hits):

```markdown
| Surface | Status | Notes |
|---|---|---|
| Claude Code `UserPromptSubmit` | block-supported | Phase 3 verified |
| Claude Code `PreToolUse` | block-supported | Phase 3 verified |
| Codex prompt hook | experimental block-only | Phase 4 evidence |
| Codex tool hook | experimental block-only | Phase 4 evidence |
```

After the table, add a single sentence linking to the evidence document: "For full evidence and remaining gaps, see [`docs/codex-compatibility.md`](docs/codex-compatibility.md)."

**LOCKED VOCABULARY (D-06 + CDX-03):**
- The status column has only two distinct values: `block-supported` and `experimental block-only`. NO row says `rewrite-capable`. NO row says `automatic masking`. NO row says `supported` without a qualifier.
- This vocabulary is enforced by `docs/codex-compatibility.md:71-83` ("automatic Codex masking is unsupported until verified outbound payload replacement is proven") — a hard constraint on all privguard documentation.

Closes D-04 §5.

### Sections 6-9 — placeholder

Write the next four heading lines as bare placeholders for Task 2 to fill in:

```markdown
## What privguard does NOT do

<!-- TODO: filled by Task 2 -->

## Synthetic-fixture-only policy

<!-- TODO: filled by Task 2 -->

## FAQ

<!-- TODO: filled by Task 2 -->

## For coding agents

<!-- TODO: filled by Task 2 -->
```

This keeps the file structurally complete (all 9 D-04 headings present) so the acceptance grep for the heading list passes even before Task 2 runs. Task 2 replaces each `<!-- TODO -->` with real prose.

**Section ordering (D-04 / Claude's Discretion):**

The 9 sections appear in the order Install → Quickstart → CLI usage → Claude Code hook setup → Capabilities matrix → What privguard does NOT do → Synthetic-fixture-only policy → FAQ → For coding agents. This puts immediately-actionable content (install, quickstart, CLI) first, integration content (hooks, matrix) second, scope-and-trust content (non-goals, fixture policy, FAQ) third, and the coding-agent pointer last. No table of contents (planner choice — README is short enough that a TOC adds friction).
  </action>
  <verify>
    <automated>python -c "import pathlib, sys, re; t = pathlib.Path('README.md').read_text(encoding='utf-8'); assert '[🇺🇸 English](README.md) | [🇧🇷 Português](README.pt-BR.md)' in t, 'D-02 switcher missing'; assert t.lstrip().startswith('[🇺🇸 English]'), 'switcher must be first non-blank line'; idx = t.index('# privguard'); switcher = t.index('[🇺🇸 English]'); assert switcher < idx, 'switcher must precede H1'; assert 'privguard-user-prompt' in t and 'privguard-pre-tool' in t, 'D-15 console scripts missing'; assert 'python -m privguard.hooks' not in t, 'D-15: python -m form must NOT appear'; assert '<BR_CPF>' in t and '<BR_CNPJ>' in t, 'Phase 2 placeholders missing'; assert re.search(r'(?<!BR_)<CPF>', t) is None, 'bare <CPF> without BR_ prefix is forbidden'; assert re.search(r'(?<!BR_)<CNPJ>', t) is None, 'bare <CNPJ> without BR_ prefix is forbidden'; assert t.count('block-supported') >= 2, 'matrix must have 2+ block-supported rows'; assert t.count('experimental block-only') >= 2, 'matrix must have 2+ experimental block-only rows'; assert 'rewrite-capable' not in t, 'D-06: rewrite-capable claim is forbidden'; assert 'automatic masking' not in t, 'CDX-03: automatic masking claim is forbidden'; assert 'docs/codex-compatibility.md' in t, 'matrix evidence link missing'; assert 'docs/install.md' in t, 'install link missing'; print('Task 1 sections 1-5 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - **D-02 switcher:** `grep -F "[🇺🇸 English](README.md) | [🇧🇷 Português](README.pt-BR.md)" README.md` exits 0.
    - **Switcher precedes H1:** the python check above (`switcher < idx of "# privguard"`) passes.
    - **D-15 console-script form (positive):** `grep -F "privguard-user-prompt" README.md` exits 0 AND `grep -F "privguard-pre-tool" README.md` exits 0.
    - **D-15 console-script form (negative — broken form NOT present):** `! grep -F "python -m privguard.hooks" README.md` exits 0 (i.e. the broken form is absent).
    - **Phase 2 placeholder vocabulary (positive):** `grep -F "<BR_CPF>" README.md` exits 0 AND `grep -F "<BR_CNPJ>" README.md` exits 0.
    - **Phase 2 placeholder vocabulary (negative — bare form rejected):** `! grep -E "(^|[^A-Za-z_])<CPF>" README.md` exits 0 AND `! grep -E "(^|[^A-Za-z_])<CNPJ>" README.md` exits 0 (the regex anchors avoid false matches inside `<BR_CPF>`).
    - **Five of nine D-04 section headings present (Task 1 covers sections 1-5; Task 2 adds sections 6-9):** `for s in "## Install" "## Quickstart" "## CLI usage" "## Claude Code hook setup" "## Capabilities matrix"; do grep -Fq "$s" README.md || exit 1; done` exits 0.
    - **All nine D-04 heading lines present (Task 2 fills in 6-9 prose, but headings are written as stubs by Task 1):** `for s in "## Install" "## Quickstart" "## CLI usage" "## Claude Code hook setup" "## Capabilities matrix" "## What privguard does NOT do" "## Synthetic-fixture-only policy" "## FAQ" "## For coding agents"; do grep -Fq "$s" README.md || exit 1; done` exits 0.
    - **D-06 capabilities matrix vocabulary verbatim:** `[ $(grep -c "block-supported" README.md) -ge 2 ]` exits 0 AND `[ $(grep -c "experimental block-only" README.md) -ge 2 ]` exits 0. AND the two forbidden phrases are absent: `! grep -F "rewrite-capable" README.md` exits 0 AND `! grep -F "automatic masking" README.md` exits 0.
    - **D-06 evidence link:** `grep -F "docs/codex-compatibility.md" README.md` exits 0.
    - **Install summarize-and-link strategy:** `grep -F "docs/install.md" README.md` exits 0.
    - **Quickstart fixtures present (derived from `tests/test_v1_regression_gate.py:45-47` at execution time — NO inline literal CPF/CNPJ values appear in this acceptance criterion):** `python -c "import re, pathlib; t = pathlib.Path('README.md').read_text(encoding='utf-8'); test_src = pathlib.Path('tests/test_v1_regression_gate.py').read_text(encoding='utf-8'); m_cpf = re.search(r'SYNTH_CPF\s*=\s*\"([^\"]+)\"', test_src); m_cnpj = re.search(r'SYNTH_CNPJ\s*=\s*\"([^\"]+)\"', test_src); assert m_cpf and m_cnpj, 'fixture regex did not match test source — file structure changed?'; assert m_cpf.group(1) in t, 'CPF fixture from test_v1_regression_gate.py:45 not present in README quickstart'; assert m_cnpj.group(1) in t, 'CNPJ fixture from test_v1_regression_gate.py:46 not present in README quickstart'; print('Quickstart fixtures match test_v1_regression_gate.py')"` exits 0.
    - **Hook setup JSON shape:** `python -c "import json, re, pathlib; t = pathlib.Path('README.md').read_text(encoding='utf-8'); blocks = re.findall(r'\`\`\`json\n(.*?)\n\`\`\`', t, re.DOTALL); assert any('UserPromptSubmit' in b and 'PreToolUse' in b and 'privguard-user-prompt' in b and 'privguard-pre-tool' in b for b in blocks), 'hook setup JSON block missing or malformed'"` exits 0.
    - **File length sanity:** `[ $(wc -l < README.md) -ge 80 ]` exits 0 (Task 1 writes ~80-130 lines covering sections 1-5; Task 2 brings the total to ≥180).
  </acceptance_criteria>
  <done>
    `README.md` exists at repo root. Cross-language switcher row is the first non-blank line. H1 is `# privguard` followed by a 2-4-sentence tagline. Sections 1-5 (Install, Quickstart, CLI usage, Claude Code hook setup, Capabilities matrix) are fully written with the locked vocabulary from D-06 (matrix), D-15 (console scripts), D-04 §1-§5 (section coverage), and Phase 2 placeholder vocabulary (`<BR_CPF>`, `<BR_CNPJ>`, `<TOKEN>`). Sections 6-9 are present as heading-only stubs ready for Task 2. Quickstart code block contains the three synthetic fixture values copied verbatim from `tests/test_v1_regression_gate.py:45-47`. The Phase 2 broken hook form (`python -m privguard.hooks…`) and the forbidden vocabulary (`rewrite-capable`, `automatic masking`) are absent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author README.md — sections 6-9 (non-goals, synthetic-fixture policy, FAQ, coding-agents pointer)</name>
  <files>README.md</files>
  <read_first>
    - README.md (just-written from Task 1 — read the full file to confirm the four `<!-- TODO -->` stubs from Task 1 are still in place at sections 6-9; this task replaces them)
    - tests/test_v1_regression_gate.py (READ lines 1-100 — the synthetic-fixture-only policy section paraphrases the docstring at lines 1-13 and the FORBIDDEN_OUTPUT corpus at lines 74-99)
    - .planning/REQUIREMENTS.md (READ §"Out of Scope" lines 92-104 — the five rows of this table inform the "What privguard does NOT do" section verbatim)
    - .planning/PROJECT.md (READ for "Core value" and "Constraints" — the FAQ "Why does it block instead of warn?" answer paraphrases the fail-closed constraint)
    - docs/codex-compatibility.md (READ §"Assessment Summary" lines 11-29 — the FAQ "Does this work with Codex?" answer summarizes this and links to the matrix)
    - privguard/cleanup.py (READ the full file from plan 07-01 — the FAQ "How do I extend the cleanup patterns?" answer references `[tool.privguard.cleanup]` in `pyproject.toml` and points the reader to add new patterns there AND to `.gitignore`; D-08 + Pitfall 4 in 07-RESEARCH.md)
    - AGENTS.md (READ first 20 lines — the "For coding agents" section is a single-line pointer; do NOT promote AGENTS.md content into README per D-05)
    - .planning/phases/07-readme-hygiene/07-CONTEXT.md (READ §"Decisions" §"D-04" lines 50-69 for the four required FAQ entries in §8 and the five required non-goals in §6; READ §"Specifics" lines 386-389 for FAQ-answer guidance)
    - .planning/phases/07-readme-hygiene/07-RESEARCH.md (READ §"Existing synthetic fixtures to reuse" lines 852-870 for the synthetic-fixture-only policy talking points)
  </read_first>
  <action>
**This task replaces the four `<!-- TODO: filled by Task 2 -->` stubs from Task 1 with real prose. The four sections appear in the order Task 1 wrote the headings: §6 non-goals → §7 synthetic-fixture policy → §8 FAQ → §9 coding-agents.**

### Section 6 — `## What privguard does NOT do`

Length: ~10-15 lines (a bullet list, not paragraphs).

Replace the `<!-- TODO -->` line with a one-line intro followed by exactly five bullets — one for each of the non-goals from D-04 §6. Use this EXACT bullet list (the wording is paraphrased from `.planning/REQUIREMENTS.md` §"Out of Scope" lines 92-104):

```markdown
privguard is a focused, local-first tool. It deliberately does NOT:

- **Run as a hosted SaaS or cloud proxy.** The privacy boundary stays on your machine; raw data never leaves it via privguard.
- **Deanonymize masked output.** v1 uses irreversible masking — there is no key-management or retention surface to reverse a `<BR_CPF>` placeholder back to the original.
- **Adapt to LangChain, LlamaIndex, or generic SDK pipelines.** The supported integration is Claude Code (`block-supported`); Codex is `experimental block-only`. See [Capabilities matrix](#capabilities-matrix).
- **Protect unsupported clients.** Clients without tested interception (no documented hook event, no synthetic interception proof) are not labeled as protected — they are labeled `unsupported`.
- **Use real Brazilian PII or production secrets in tests, fixtures, examples, or commits.** Every value in this repository's tests is synthetic. See [Synthetic-fixture-only policy](#synthetic-fixture-only-policy).
```

The five bullets MUST cover, in order: hosted SaaS, deanonymization, LangChain/LlamaIndex adapters, unsupported clients, real-data fixtures. Phrasing variations are fine; the five categories are not.

Closes D-04 §6.

### Section 7 — `## Synthetic-fixture-only policy`

Length: ~6-10 lines.

Replace the `<!-- TODO -->` line with three short paragraphs that establish the policy:

```markdown
privguard's test suite, code examples, and documentation use only synthetic Brazilian identifiers and synthetic secret-shaped strings. The synthetic CPF, CNPJ, and CNH values shown in [Quickstart](#quickstart) come from `tests/test_v1_regression_gate.py` and are checksum-valid but obviously fabricated.

We do this because real PII in a public repo would reproduce the exfiltration risk privguard is built to prevent. The repository's `.gitignore` and the cleanup tool's hardcoded `_PROTECTED` list (in `privguard/cleanup.py`) treat `.env`, `data_sensivel/`, and the Brazilian-flagged paths as untouchable.

If you contribute a test, fixture, or example, use the existing constants in `tests/test_v1_regression_gate.py` rather than inventing new values — that file is the single source of truth for synthetic fixtures.
```

The section MUST include the strings `synthetic`, `tests/test_v1_regression_gate.py`, and `_PROTECTED` (the last makes the cross-link to plan 07-01's deliverable explicit).

Closes D-04 §7.

### Section 8 — `## FAQ`

Length: ~30-50 lines (four Q&A pairs).

Replace the `<!-- TODO -->` line with exactly four `### Q` subsection headings, in the order locked by D-04 §8. Use these EXACT subsection heading texts (the acceptance criterion below greps for each):

#### `### Does this work with Codex?`

Answer (~3-5 lines): Codex hooks are labeled `experimental block-only` in the [Capabilities matrix](#capabilities-matrix) — privguard can block sensitive prompts and tool payloads, but does NOT claim automatic masking for Codex. The supporting evidence and remaining gaps are documented in [`docs/codex-compatibility.md`](docs/codex-compatibility.md). Treat Codex protection as best-effort blocking until outbound payload replacement is proven.

**MUST contain the phrase `experimental block-only` and the link to `docs/codex-compatibility.md`. MUST NOT contain `automatic masking` as a positive claim** (a sentence saying "does NOT claim automatic masking" is fine because the negative phrase `does NOT claim automatic masking` is allowed; but a sentence saying `Codex automatic masking is supported` would be forbidden — the acceptance grep is on the literal string `automatic masking` in the README's overall body, so put the negation outside the FAQ answer or rephrase the FAQ to avoid the literal phrase entirely; safer phrasing: "privguard does not claim Codex masking — only blocking. See [...] for the gap analysis." This avoids the literal "automatic masking" string).

#### `### What if a CPF is missed?`

Answer (~3-5 lines): privguard's detection is fail-closed by design — when a CPF (or any PII) cannot be confidently masked, the hook BLOCKS the prompt rather than letting it through. If you suspect a missed CPF, run `privguard claude doctor` to verify hook installation and effective policy, and `privguard scan "<text>"` to see what the detector sees. Do NOT promise 100% recall — privguard is one layer of defense, not a guarantee.

**MUST contain the phrase `fail-closed` (or `fails closed`) and reference `privguard claude doctor`.**

#### `### Why does it block instead of warn?`

Answer (~3-5 lines): Warning-only mode is explicitly out of scope (see [What privguard does NOT do](#what-privguard-does-not-do)). The core value of privguard is preventing sensitive data from reaching an external LLM provider — a warning that the user can ignore would not satisfy that goal. Strict/fail-closed is the default for external-provider workflows; rewrite is only used on surfaces where outbound payload replacement is verified.

#### `### How do I extend the cleanup patterns?`

Answer (~5-8 lines): Add the new pattern to `[tool.privguard.cleanup].patterns` in `pyproject.toml`, AND add the same pattern to `.gitignore` so transient artifacts cannot be committed before the next cleanup run. The trailing-slash convention matters: `__pycache__/` matches a directory tree recursively; `*.py[cod]` matches files by basename glob.

Include this short fenced ```toml example showing the edit:

```toml
[tool.privguard.cleanup]
patterns = [
    "__pycache__/",
    "*.py[cod]",
    # ...your new pattern here...
]
```

Then a one-line postscript: "The cleanup tool reads this list at runtime; the hardcoded protected list (`.env`, `data_sensivel/`, source dirs) cannot be overridden."

**MUST contain the strings `[tool.privguard.cleanup]` AND `.gitignore` AND `__pycache__/` (the last for the trailing-slash example) AND the phrase about the protected list NOT being overridable (closes the loop on D-09).**

Closes D-04 §8.

### Section 9 — `## For coding agents`

Length: ~2-3 lines.

Replace the `<!-- TODO -->` line with this EXACT line per D-05 (no anchor coupling, no content promotion):

```markdown
For coding agents working in this repo, see [AGENTS.md](AGENTS.md).
```

A single sentence is sufficient. Do NOT add bullets, sub-headings, or anchors. The link target is the bare repo-root `AGENTS.md`. The acceptance criterion greps for the literal substring `[AGENTS.md](AGENTS.md)` (or equivalently `(AGENTS.md)`).

Closes D-04 §9.

### Section ordering & closing

After section 9, the file ends. No footer, no copyright line (deferred per CONTEXT.md "Deferred Ideas — LICENSE file"), no badges (deferred per D-02), no contact info.
  </action>
  <verify>
    <automated>python -c "import pathlib, re; t = pathlib.Path('README.md').read_text(encoding='utf-8'); assert '<!-- TODO' not in t, 'TODO stubs from Task 1 must be replaced'; sections = ['## Install','## Quickstart','## CLI usage','## Claude Code hook setup','## Capabilities matrix','## What privguard does NOT do','## Synthetic-fixture-only policy','## FAQ','## For coding agents']; missing = [s for s in sections if s not in t]; assert not missing, f'missing sections: {missing}'; faqs = ['### Does this work with Codex?','### What if a CPF is missed?','### Why does it block instead of warn?','### How do I extend the cleanup patterns?']; missing_q = [q for q in faqs if q not in t]; assert not missing_q, f'missing FAQ entries: {missing_q}'; nogos = ['hosted SaaS','deanonymization','LangChain','unsupported clients','synthetic']; missing_n = [n for n in nogos if n.lower() not in t.lower()]; assert not missing_n, f'missing non-goals: {missing_n}'; assert '[AGENTS.md](AGENTS.md)' in t, 'AGENTS.md link missing'; assert 'fail-closed' in t.lower() or 'fails closed' in t.lower(), 'fail-closed phrase missing in FAQ §2'; assert 'privguard claude doctor' in t, 'claude doctor reference missing in FAQ §2'; assert '[tool.privguard.cleanup]' in t, 'cleanup table reference missing in FAQ §4'; assert '.gitignore' in t, '.gitignore reference missing in FAQ §4'; assert 'rewrite-capable' not in t, 'forbidden vocabulary'; assert 'automatic masking' not in t, 'forbidden vocabulary (phrase must be avoided entirely — rephrase if needed)'; print('Task 2 sections 6-9 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - **All 9 D-04 sections present (final state, after Task 2):** `for s in "## Install" "## Quickstart" "## CLI usage" "## Claude Code hook setup" "## Capabilities matrix" "## What privguard does NOT do" "## Synthetic-fixture-only policy" "## FAQ" "## For coding agents"; do grep -Fq "$s" README.md || exit 1; done` exits 0.
    - **No TODO stubs left:** `! grep -F "<!-- TODO" README.md` exits 0.
    - **All 4 D-04 §8 FAQ entries present:** `for q in "### Does this work with Codex?" "### What if a CPF is missed?" "### Why does it block instead of warn?" "### How do I extend the cleanup patterns?"; do grep -Fq "$q" README.md || exit 1; done` exits 0.
    - **All 5 non-goals from D-04 §6 / REQUIREMENTS.md "Out of Scope" present:** `for n in "hosted SaaS" "deanonymization" "LangChain" "unsupported" "synthetic"; do grep -Fiq "$n" README.md || exit 1; done` exits 0.
    - **D-05 AGENTS.md link form:** `grep -F "[AGENTS.md](AGENTS.md)" README.md` exits 0.
    - **FAQ §"What if a CPF is missed?" answers correctly:** `grep -iE "fail.?closed" README.md` exits 0 AND `grep -F "privguard claude doctor" README.md` exits 0.
    - **FAQ §"How do I extend the cleanup patterns?" references both source-of-truth files:** `grep -F "[tool.privguard.cleanup]" README.md` exits 0 AND `grep -F ".gitignore" README.md` exits 0 AND `grep -F "__pycache__/" README.md` exits 0.
    - **Synthetic-fixture-only policy section names the source of truth:** `grep -F "tests/test_v1_regression_gate.py" README.md` exits 0.
    - **Forbidden vocabulary still absent (CDX-03 / D-06 hard constraint must hold for the WHOLE file, including the FAQ):** `! grep -F "rewrite-capable" README.md` exits 0 AND `! grep -F "automatic masking" README.md` exits 0.
    - **Phase 2 vocabulary still correct (regression check from Task 1):** `grep -F "<BR_CPF>" README.md` exits 0 AND `grep -F "<BR_CNPJ>" README.md` exits 0.
    - **D-15 console scripts still correct (regression check from Task 1):** `grep -F "privguard-user-prompt" README.md` exits 0 AND `grep -F "privguard-pre-tool" README.md` exits 0 AND `! grep -F "python -m privguard.hooks" README.md` exits 0.
    - **File length sanity:** `[ $(wc -l < README.md) -ge 180 ]` exits 0 (the combined file is ~180-260 lines after both tasks).
  </acceptance_criteria>
  <done>
    `README.md` is a complete, ship-ready top-level English README. All 9 D-04 sections are written with prose. The four required FAQ entries are present with correct answers (fail-closed reference in §2, cleanup-table + `.gitignore` references in §4, Codex labeling consistent with the matrix in §1). The five non-goals are bullet-listed. The synthetic-fixture-only policy names `tests/test_v1_regression_gate.py` as the source of truth. The "For coding agents" section is the single locked sentence linking to AGENTS.md per D-05. Forbidden vocabulary (`rewrite-capable`, `automatic masking`) is absent throughout the file. The README is ready for plan 07-03 to translate to pt-BR (depends_on: ["02"]).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader → installed `privguard` CLI | User copies the hook-setup JSON snippet. If the snippet documents a non-working command form (e.g. `python -m privguard.hooks.main_user_prompt`), the user's Claude Code installation silently fails to gate prompts. |
| README reader → privguard's privacy posture | User reads the capabilities matrix to decide whether to trust privguard with sensitive data. If the matrix mis-represents Codex (e.g. claims `rewrite-capable` or `automatic masking`), the user trusts unsafe surfaces. |
| README example code → user shell | User copies the masking demo from Quickstart. If the example uses non-synthetic CPF/CNPJ values, the README itself becomes a real-data leak vector — exactly the failure mode TEST-01 prevents. |
| README writer → executor (this plan) | Plan writes "use the values from `tests/test_v1_regression_gate.py:45-47`" — the executor must NOT invent new values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-T1 | Tampering / Information Disclosure | hook-setup JSON snippet in README §4 | mitigate | The snippet uses `privguard-user-prompt` and `privguard-pre-tool` (D-15 console scripts shipped by plan 07-01 in `pyproject.toml [project.scripts]`), NOT `python -m privguard.hooks.main_user_prompt` (which would not work because `privguard/hooks.py` is a single-file module). Acceptance criteria enforce: positive grep for both console-script names, AND negative grep `! grep "python -m privguard.hooks" README.md`. Severity: medium — a broken hook command silently disables Claude Code prompt protection. |
| T-07-02-T2 | Misrepresentation (Spoofing of capability) | capabilities matrix in README §5 | mitigate | The matrix wording is locked verbatim by D-06 — only two status values appear (`block-supported` for Claude rows, `experimental block-only` for Codex rows). Acceptance criteria enforce: `grep -c "block-supported" >= 2`, `grep -c "experimental block-only" >= 2`, AND `! grep "rewrite-capable" README.md`, AND `! grep "automatic masking" README.md`. CDX-03 (in `docs/codex-compatibility.md:71-83`) is a hard constraint on all privguard documentation — this matches it. Severity: medium — a `rewrite-capable` claim would mislead users into trusting Codex with sensitive prompts that are NOT actually masked. |
| T-07-02-T3 | Information Disclosure (real PII in public docs) | Quickstart code block in README §2 | mitigate | The synthetic fixtures from `tests/test_v1_regression_gate.py:45-47` are reused verbatim — TEST-01 / RESEARCH.md §"Existing synthetic fixtures to reuse" mandates this. The plan instructs the executor to read the test file at execution time and copy values literally; no inline literal CPF/CNPJ values appear in the plan text itself (the repo's pre-tool guard would reject them). The acceptance criterion derives the verification at runtime from `tests/test_v1_regression_gate.py` rather than embedding fixture strings. Severity: medium — inventing new "fake" CPF/CNPJ values risks accidentally matching a real Brazilian record. |
| T-07-02-T4 | Spoofing (out-of-date matrix) | capabilities matrix drift from `docs/codex-compatibility.md` | accept | The README matrix is a 4-row condensed view of the 8-row source-of-truth matrix in `docs/codex-compatibility.md:52-65`. Drift between the two is possible if `CODEX_COMPATIBILITY` (in `privguard/codex.py`) is updated and the README is not. v1 enforcement is social (D-03 paired-update rule); a CI drift check is explicitly deferred to v2 per CONTEXT.md "Deferred Ideas — Drift-prevention regression test". The README links to the canonical doc as the authoritative reference. |

</threat_model>

<verification>

Run the consolidated checks from both tasks against the final `README.md`:

```bash
# 1. All 9 D-04 sections present
for s in "## Install" "## Quickstart" "## CLI usage" "## Claude Code hook setup" "## Capabilities matrix" "## What privguard does NOT do" "## Synthetic-fixture-only policy" "## FAQ" "## For coding agents"; do
  grep -Fq "$s" README.md || echo "MISSING: $s"
done

# 2. D-15 console-script form (positive + negative)
grep -F "privguard-user-prompt" README.md && grep -F "privguard-pre-tool" README.md \
  && (! grep -F "python -m privguard.hooks" README.md) \
  && echo "D-15 OK"

# 3. D-06 matrix vocabulary (locked)
[ "$(grep -c "block-supported" README.md)" -ge 2 ] \
  && [ "$(grep -c "experimental block-only" README.md)" -ge 2 ] \
  && (! grep -F "rewrite-capable" README.md) \
  && (! grep -F "automatic masking" README.md) \
  && echo "D-06 OK"

# 4. Phase 2 placeholder vocabulary
grep -F "<BR_CPF>" README.md && grep -F "<BR_CNPJ>" README.md \
  && echo "Phase 2 placeholders OK"

# 5. Quickstart fixtures derived from test source (no inline literals)
python -c "
import re, pathlib
t = pathlib.Path('README.md').read_text(encoding='utf-8')
src = pathlib.Path('tests/test_v1_regression_gate.py').read_text(encoding='utf-8')
m_cpf = re.search(r'SYNTH_CPF\s*=\s*\"([^\"]+)\"', src)
m_cnpj = re.search(r'SYNTH_CNPJ\s*=\s*\"([^\"]+)\"', src)
assert m_cpf.group(1) in t, 'CPF fixture absent'
assert m_cnpj.group(1) in t, 'CNPJ fixture absent'
print('quickstart fixtures match test source')
"

# 6. FAQ has all 4 required entries
for q in "### Does this work with Codex?" "### What if a CPF is missed?" "### Why does it block instead of warn?" "### How do I extend the cleanup patterns?"; do
  grep -Fq "$q" README.md || echo "MISSING FAQ: $q"
done

# 7. AGENTS.md link
grep -F "[AGENTS.md](AGENTS.md)" README.md && echo "D-05 OK"

# 8. File length sanity
[ "$(wc -l < README.md)" -ge 180 ] && echo "Length OK"
```

All eight checks should print their `OK` line and no `MISSING:` lines.

</verification>

<success_criteria>

- `README.md` exists at repo root with the cross-language switcher row immediately above the H1 (D-02).
- All 9 D-04 sections are present with correct heading text.
- All 4 D-04 §8 FAQ entries are present and answered correctly (fail-closed reference, cleanup-table reference, Codex labeling consistent).
- All 5 D-04 §6 non-goals are present (hosted SaaS, deanonymization, LangChain/LlamaIndex adapters, unsupported clients, real-data fixtures).
- D-06 capabilities matrix has 4 rows with status column wording verbatim (`block-supported` ×2 for Claude, `experimental block-only` ×2 for Codex).
- D-15 hook setup snippet uses `privguard-user-prompt` and `privguard-pre-tool` (NOT `python -m privguard.hooks…`).
- Phase 2 placeholder vocabulary is correct (`<BR_CPF>`, `<BR_CNPJ>`, NOT bare `<CPF>` / `<CNPJ>`).
- Quickstart code block reuses the three synthetic fixtures from `tests/test_v1_regression_gate.py:45-47` verbatim.
- README links to `docs/codex-compatibility.md` (matrix evidence), `docs/install.md` (install detail), AGENTS.md (coding-agents pointer).
- Forbidden vocabulary (`rewrite-capable`, `automatic masking`) is absent throughout the file.
- File length is at least 180 lines.
- Plan 07-03 (pt-BR) can begin translating from this committed English source.

</success_criteria>

<output>
After completion, create `.planning/phases/07-readme-hygiene/07-02-SUMMARY.md` with frontmatter `requirements_completed: [DOC-01]` (English half) and a brief description naming the new file (`README.md`), the section count (9), the matrix-vocabulary lock (D-06), the hook-setup form (D-15 console scripts), and the link to the next plan (07-03 translates this file).
</output>
</content>
</invoke>